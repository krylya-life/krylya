---
phase: 09-seo
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/audit/CHECKLIST.md
  - .planning/audit/BASELINE.md
autonomous: true
requirements: [AUDIT-01]
must_haves:
  truths:
    - "Существует чек-лист аудита с ошибками, разбитыми по приоритету критично/важно/желательно"
    - "Каждая ошибка имеет: где, как проверить, статус исправления"
    - "Существует пустая замороженная таблица базлайна с 16 запросами групп 1–3"
  artifacts:
    - path: ".planning/audit/CHECKLIST.md"
      provides: "чек-лист SEO-аудита с приоритетами и статусами"
      contains: "КРИТИЧНО"
    - path: ".planning/audit/BASELINE.md"
      provides: "скелет таблицы базлайна (заполняется в 09-05)"
      contains: "ивент агентство калининград"
  key_links:
    - from: ".planning/audit/CHECKLIST.md"
      to: "src/components/Seo.astro / ContactForm.astro / astro.config.mjs"
      via: "колонка «Где» ссылается на реальные файлы"
      pattern: "Seo.astro"
---

<objective>
Собрать чек-лист SEO-аудита боевого сайта крылья.life: провести технический аудит (краулинг Screaming Frog, Core Web Vitals через PageSpeed Insights, валидация JSON-LD), занести КАЖДУЮ найденную ошибку в `.planning/audit/CHECKLIST.md` с приоритетом (критично / важно / желательно), указанием где она и как проверить, и создать пустой скелет таблицы базлайна `.planning/audit/BASELINE.md` с замороженным списком из 16 запросов групп 1–3.

Purpose: AUDIT-01 — «честная фотография» технического состояния. Критичные/важные ошибки из чек-листа чинятся в этой фазе (планы 01/03), «желательно» уходит в бэклог (D-05). Замороженный список запросов (D-01, D-02) — основа для снятия позиций в плане 09-05.

Output: два файла в `.planning/audit/`. Этот план — автономный (аналитика + запись файлов, без кабинетов). Заполнение цифр базлайна из кабинетов — план 09-05.

Границы: сюда входят ошибки, которые Claude может обнаружить статически (краулинг, view-source, dist/, PageSpeed по публичному URL). Уже известные из RESEARCH.md критичные ошибки (canonical Punycode, /thanks/ в sitemap, нет reachGoal) — сразу заносятся в чек-лист как K-1…K-5 со статусом «чинится в 09-01».
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-seo/09-CONTEXT.md
@.planning/phases/09-seo/09-RESEARCH.md
@seo/keywords.md

<interfaces>
<!-- Известные ошибки из RESEARCH.md (уже верифицированы) — заносятся в чек-лист без повторной проверки: -->
- K-1: canonical в Punycode вместо кириллицы — src/components/Seo.astro — view-source любой страницы — чинится в 09-01
- K-2: og:url в Punycode — src/components/Seo.astro — view-source — чинится в 09-01
- K-3: BreadcrumbList @id в Punycode — src/components/Breadcrumbs.astro — Rich Results Test — чинится в 09-01
- K-4: /thanks/ в sitemap — astro.config.mjs — dist/sitemap.xml — чинится в 09-01
- K-5: нет JS-события form_submitted — src/components/ContactForm.astro — Метрика→Цели — чинится в 09-01 (код) + 09-04 (кабинет)

<!-- Замороженный список базлайна — 16 запросов групп 1–3 из seo/keywords.md (D-01, D-02). -->
<!-- Запрос «организация корпоратива калининград» входит в группы 1 и 2 — одна строка (группа «1+2»). -->
Структура таблицы базлайна — точный формат в RESEARCH.md §«Baseline Table Structure».
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Технический аудит сайта и сборка CHECKLIST.md</name>
  <files>.planning/audit/CHECKLIST.md</files>
  <read_first>
    - .planning/phases/09-seo/09-RESEARCH.md §«Critical Findings», §«Standard Stack», §«Структура чек-листа аудита» (готовые находки + структура файла)
    - .planning/phases/09-seo/09-CONTEXT.md §D-05 (что чиним: критично+важно; желательно → бэклог)
    - src/components/Seo.astro, src/components/Breadcrumbs.astro, src/components/ContactForm.astro, astro.config.mjs (объекты аудита)
    - dist/index.html, dist/sitemap.xml (или dist/sitemap-0.xml до фикса) — собранный вывод для проверки on-page
  </read_first>
  <action>
    Провести технический SEO-аудит и записать результат в `.planning/audit/CHECKLIST.md` (AUDIT-01).

    Шаги аудита:
    1. `npm run build` — получить свежий `dist/`. Пройтись по `dist/*/index.html`: проверить наличие и уникальность `<title>`, `<meta name="description">`, единственный `<h1>` на страницу, `alt` у ключевых `<img>`, наличие canonical. Зафиксировать проблемы.
    2. Прогнать публичный сайт `крылья.life` через PageSpeed Insights (pagespeed.web.dev) — мобильная версия главной и одной страницы услуги. Записать Core Web Vitals (LCP, CLS, INP) и общий Performance-балл. Если LCP/CLS в красной/жёлтой зоне — занести с приоритетом (критично, если красная; важно, если жёлтая).
    3. Валидация JSON-LD: проверить главную и одну страницу услуги через Rich Results Test (search.google.com/test/rich-results) — Organization/LocalBusiness, Service, BreadcrumbList. Занести ошибки/предупреждения.
    4. Битые ссылки и редиректы: если Screaming Frog доступен — прогнать `крылья.life` (22 URL, укладывается в free-лимит 500). Если недоступен — отметить в чек-листе как «проверить Screaming Frog вручную» (желательно) и сделать доступную проверку: `grep`-обход внутренних ссылок в `dist/` на 404-кандидаты.

    Записать `.planning/audit/CHECKLIST.md` строго по структуре (шапка + три таблицы по приоритетам). Обязательно внести уже известные ошибки K-1…K-5 (см. `<interfaces>`) в раздел КРИТИЧНО со статусом «→ чинится в 09-01». Все найденные при аудите ошибки классифицировать по D-05:
    - КРИТИЧНО — ломает индексацию/canonical/разметку/CWV в красной зоне
    - ВАЖНО — ухудшает SEO, но не ломает (напр. дубль/пустой description, LCP в жёлтой зоне, отсутствующий alt на важных картинках)
    - ЖЕЛАТЕЛЬНО — мелочь, уходит в бэклог, НЕ чинится в Phase 9

    Формат файла:
    ```markdown
    # Чек-лист аудита крылья.life — Phase 9

    **Дата аудита:** 2026-07-01
    **Страниц проаудировано:** 22 (индексируемых 13+ маршрутов, /thanks/ и /api/ — noindex)
    **Инструменты:** dist/ view-source, PageSpeed Insights (mobile), Rich Results Test[, Screaming Frog]

    ## КРИТИЧНО (чиним в Phase 9 — планы 01/03)
    | # | Ошибка | Где | Как проверить | Статус |
    |---|--------|-----|---------------|--------|
    | K-1 | Canonical в Punycode вместо кириллицы | src/components/Seo.astro | view-source: `<link rel=canonical>` | → 09-01 |
    | K-2 | og:url в Punycode | src/components/Seo.astro | view-source: og:url | → 09-01 |
    | K-3 | BreadcrumbList @id в Punycode | src/components/Breadcrumbs.astro | Rich Results Test | → 09-01 |
    | K-4 | /thanks/ в sitemap | astro.config.mjs | dist/sitemap.xml grep thanks | → 09-01 |
    | K-5 | Нет JS-события form_submitted | src/components/ContactForm.astro | Метрика → Цели | → 09-01 (код) + 09-04 (кабинет) |
    | K-… | (найденное при аудите) | … | … | [ ] |

    ## ВАЖНО (чиним в Phase 9)
    | # | Ошибка | Где | Как проверить | Статус |
    |---|--------|-----|---------------|--------|
    | V-1 | … | … | … | [ ] |

    ## ЖЕЛАТЕЛЬНО (бэклог, будущие фазы — D-05)
    | # | Пожелание | Где | Приоритет |
    |---|-----------|-----|-----------|
    | G-1 | … | … | низкий |
    ```
    Если по итогам аудита раздел ВАЖНО/ЖЕЛАТЕЛЬНО пуст — оставить таблицу с пометкой «— на дату аудита не выявлено —» (не выдумывать ошибки).
  </action>
  <acceptance_criteria>
    - `test -f .planning/audit/CHECKLIST.md` — существует
    - `grep -c "КРИТИЧНО" .planning/audit/CHECKLIST.md` — ≥ 1
    - `grep -c "ЖЕЛАТЕЛЬНО" .planning/audit/CHECKLIST.md` — ≥ 1
    - все пять строк K-1…K-5 присутствуют: `grep -cE "K-[1-5]" .planning/audit/CHECKLIST.md` — ≥ 5
    - колонка «Где» ссылается на реальные файлы: `grep -c "Seo.astro" .planning/audit/CHECKLIST.md` — ≥ 1
    - зафиксирован CWV: `grep -icE "LCP|Core Web Vitals|PageSpeed" .planning/audit/CHECKLIST.md` — ≥ 1
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/audit/CHECKLIST.md && grep -q "КРИТИЧНО" .planning/audit/CHECKLIST.md && [ $(grep -cE "K-[1-5]" .planning/audit/CHECKLIST.md) -ge 5 ] && echo OK</automated>
  </verify>
  <done>Чек-лист аудита создан, содержит все известные K-1…K-5 + найденные при аудите ошибки, разбит по трём приоритетам, каждая строка имеет где/как проверить/статус, зафиксированы Core Web Vitals.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Скелет замороженной таблицы базлайна BASELINE.md</name>
  <files>.planning/audit/BASELINE.md</files>
  <read_first>
    - seo/keywords.md (группы 1–3 — источник 16 запросов; группа 4 НЕ входит)
    - .planning/phases/09-seo/09-CONTEXT.md §D-01, §D-02 (какие запросы, обе поисковые системы, список замораживается)
    - .planning/phases/09-seo/09-RESEARCH.md §«Baseline Table Structure» (точный формат колонок)
  </read_first>
  <action>
    Создать `.planning/audit/BASELINE.md` — ПУСТОЙ скелет таблицы базлайна (цифры заполняются Марией/из кабинетов в плане 09-05). Список запросов замораживается здесь и далее не меняется (D-02).

    Взять ВСЕ запросы групп 1–3 из `seo/keywords.md`. Запрос «организация корпоратива калининград» дублируется в группах 1 и 2 — в таблице ОДНА строка с пометкой группы «1+2». Итого 16 строк запросов. Группа 4 (информационные) НЕ включается (D-01, отложено в Phase 14).

    Формат файла (пустые ячейки данных — заполняются в 09-05):
    ```markdown
    # Базлайн позиций — крылья.life
    **Дата снятия:** _заполняется в плане 09-05_
    **Состояние сайта:** после исправления canonical (Phase 9, план 09-01/03)
    **Список запросов ЗАМОРОЖЕН на старте фазы — не менять (D-02).**

    ## Общая индексация
    | Параметр | Яндекс.Вебмастер | Google Search Console |
    |----------|-----------------|----------------------|
    | Проиндексированных страниц | | |
    | Страниц в sitemap | 13+ (без /thanks/) | — |
    | Ошибок индексации | | |

    ## Показы и клики (последние 30 дней, все запросы)
    | | Яндекс.Вебмастер | Google Search Console |
    |--|--|--|
    | Суммарные показы | | |
    | Суммарные клики | | |

    ## Позиции по коммерческим запросам (группы 1–3)
    | # | Запрос | Группа | Позиция Яндекс | Показы Я (28д) | Клики Я (28д) | Позиция Google | Показы G (28д) | Клики G (28д) |
    |---|--------|--------|---------------|----------------|---------------|----------------|----------------|---------------|
    | 1 | ивент агентство калининград | 1 | | | | | | |
    | 2 | организация мероприятий калининград | 1 | | | | | | |
    | 3 | организация корпоратива калининград | 1+2 | | | | | | |
    | 4 | event-агентство калининград | 1 | | | | | | |
    | 5 | агентство праздников калининград | 1 | | | | | | |
    | 6 | новогодний корпоратив калининград под ключ | 2 | | | | | | |
    | 7 | организация конференции калининград | 2 | | | | | | |
    | 8 | организация презентации объекта калининград | 2 | | | | | | |
    | 9 | организация тимбилдинга калининград | 2 | | | | | | |
    | 10 | ведущий и команда на корпоратив калининград | 2 | | | | | | |
    | 11 | координация свадьбы / юбилея калининград | 2 | | | | | | |
    | 12 | организовать мероприятие в калининграде из москвы | 3 | | | | | | |
    | 13 | выездной корпоратив в калининграде | 3 | | | | | | |
    | 14 | провести конференцию в калининграде | 3 | | | | | | |
    | 15 | корпоратив на море калининградская область | 3 | | | | | | |
    | 16 | площадки для корпоратива калининград | 3 | | | | | | |

    *Позиции: число (1, 2…), н/д — нет данных в кабинете, >100 — вне топ-100.*
    *Для запросов с н/д в кабинете — проверить вручную в инкогнито (план 09-05).*
    ```
  </action>
  <acceptance_criteria>
    - `test -f .planning/audit/BASELINE.md` — существует
    - ровно 16 строк запросов: `grep -cE "^\| [0-9]+ \|" .planning/audit/BASELINE.md` — 16
    - первый запрос присутствует: `grep -c "ивент агентство калининград" .planning/audit/BASELINE.md` — ≥ 1
    - есть колонки для обеих систем: `grep -c "Позиция Яндекс" .planning/audit/BASELINE.md` — ≥ 1 и `grep -c "Позиция Google" .planning/audit/BASELINE.md` — ≥ 1
    - группа 4 не попала: `grep -c "как выбрать площадку" .planning/audit/BASELINE.md` — 0
    - зафиксирована заморозка: `grep -ci "заморожен" .planning/audit/BASELINE.md` — ≥ 1
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/audit/BASELINE.md && [ $(grep -cE "^\| [0-9]+ \|" .planning/audit/BASELINE.md) -eq 16 ] && grep -q "Позиция Яндекс" .planning/audit/BASELINE.md && grep -q "Позиция Google" .planning/audit/BASELINE.md && echo OK</automated>
  </verify>
  <done>Скелет таблицы базлайна создан с 16 замороженными запросами групп 1–3, колонками под Яндекс и Google (позиции/показы/клики) и строками общей индексации; группа 4 исключена; список помечен как замороженный.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Публичный сайт → внешние аудит-инструменты (PageSpeed, Rich Results) | Отдаём публичный URL стороннему сервису для анализа — только публичные данные |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-05 | Information Disclosure | Прогон сайта через PageSpeed/Rich Results | accept | Анализируются только публично доступные страницы; PII/секретов на страницах нет (форма — на клиенте, обработка в CF Function). |
| T-09-06 | Information Disclosure | CHECKLIST/BASELINE в .planning/ | accept | Артефакты лежат в приватном `.planning/` (не в `dist/`, не публикуются); чувствительных данных не содержат. |

Аутентификация/криптография — N/A: план не трогает код, только читает и пишет аудит-артефакты.
</threat_model>

<verification>
```bash
test -f .planning/audit/CHECKLIST.md \
  && test -f .planning/audit/BASELINE.md \
  && grep -q "КРИТИЧНО" .planning/audit/CHECKLIST.md \
  && [ $(grep -cE "^\| [0-9]+ \|" .planning/audit/BASELINE.md) -eq 16 ] \
  && echo "PLAN 02 GREEN"
```
</verification>

<success_criteria>
- CHECKLIST.md содержит все K-1…K-5 + найденные ошибки, разбит по критично/важно/желательно (AUDIT-01)
- Зафиксированы Core Web Vitals (mobile) по данным PageSpeed
- BASELINE.md — пустой скелет с 16 замороженными запросами, колонки под Яндекс+Google
</success_criteria>

<output>
После завершения создать `.planning/phases/09-seo/09-02-SUMMARY.md` (сколько ошибок найдено по каждому приоритету, каков CWV главной, подтверждение 16 замороженных запросов).
</output>
