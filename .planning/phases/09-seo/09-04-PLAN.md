---
phase: 09-seo
plan: 04
type: execute
wave: 3
depends_on: ["09-03"]
files_modified:
  - .planning/audit/CHECKLIST.md
autonomous: false
requirements: [AUDIT-02, AUDIT-03]
must_haves:
  truths:
    - "В Метрике создана JS-цель form_submitted и она срабатывает при тестовой отправке формы"
    - "Свой трафик Марии исключён на компьютере И на телефоне («не учитывать мои визиты»)"
    - "Счётчик Метрики один (без дубля), вебвизор пишет сессии"
    - "В GSC добавлен property, sitemap подан, видно число проиндексированных страниц"
  artifacts:
    - path: ".planning/audit/CHECKLIST.md"
      provides: "отметки о выполнении кабинетных проверок Метрики и GSC"
      contains: "Метрика"
  key_links:
    - from: "ContactForm reachGoal form_submitted"
      to: "Метрика: цель «Отправка заявки»"
      via: "JS-событие form_submitted"
      pattern: "form_submitted"
---

<objective>
Настроить и проверить внешние кабинеты (действия Марии, пошагово): в Яндекс.Метрике — создать JS-цель `form_submitted`, исключить свой трафик на компьютере и телефоне, убедиться что счётчик один и вебвизор пишет; в Google Search Console — проверить/добавить property, подать sitemap, снять число проиндексированных страниц.

Purpose: AUDIT-02 (корректная Метрика) и AUDIT-03 (корректный GSC). Выполняется ПОСЛЕ деплоя фиксов (09-03), потому что JS-цель проверяется тестовой отправкой формы, а reachGoal должен быть уже на живом сайте. Мария — не программист, поэтому все шаги — «открой…, нажми…, должно появиться…».

Output: настроенные кабинеты + отметки в чек-листе. План НЕ автономный — каждый шаг делает Мария в браузере; Claude ведёт её и фиксирует результат.

Зависит от 09-03 (reachGoal задеплоен и живёт на сайте).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/09-seo/09-RESEARCH.md
@.planning/audit/CHECKLIST.md

<interfaces>
<!-- Счётчик Метрики: 99532899 (business.ts). Вебвизор включён в коде (webvisor: true). -->
<!-- reachGoal 'form_submitted' уже на живом сайте (задеплоено в 09-03). -->
<!-- GSC: верификационный файл public/google0793dafce34f21f1.html уже загружен (HTML-verification). -->
<!-- IDN GSC: добавлять ОДИН property (Punycode URL-prefix ИЛИ Domain через DNS) — НЕ оба, чтобы не двоить данные (RESEARCH.md Pitfall 4). -->
<!-- Точные клик-пути — в RESEARCH.md §«Пошаговые инструкции для кабинетов». -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Метрика — JS-цель form_submitted + проверка счётчика и вебвизора</name>
  <files>.planning/audit/CHECKLIST.md (внешний кабинет Яндекс.Метрика)</files>
  <read_first>
    - .planning/phases/09-seo/09-RESEARCH.md §«Яндекс.Метрика — Создать JS-цель form_submitted» (пошагово)
    - .planning/phases/09-seo/09-CONTEXT.md §D-05 (что чиним)
  </read_first>
  <action>Чекпоинт для Марии (не-программист). Мария создаёт в кабинете Метрики JS-цель form_submitted, проверяет её тестовой отправкой формы, а также счётчик и вебвизор. Пошаговые инструкции — в блоке <how-to-verify>. Claude ведёт Марию и по её ответам фиксирует результат в .planning/audit/CHECKLIST.md.</action>
  <what-built>
    В коде сайта (09-01, задеплоено 09-03) при успешной отправке формы уже вызывается JS-событие Метрики `form_submitted`. Теперь Мария создаёт под это событие цель в кабинете Метрики и проверяет, что счётчик один, а вебвизор пишет сессии (AUDIT-02).
  </what-built>
  <how-to-verify>
    Мария, делаем по шагам. Открой сайт metrika.yandex.ru в браузере на компьютере, войди под своим логином.

    **Создать цель «Отправка заявки»:**
    1. Слева выбери счётчик с номером 99532899 (это счётчик крылья.life).
    2. В левом меню нажми «Настройка».
    3. Вверху появятся вкладки — нажми вкладку «Цели».
    4. Нажми кнопку «Добавить цель».
    5. В поле «Тип условия» выбери «JavaScript-событие».
    6. В поле «Идентификатор» впиши ровно: `form_submitted` (латиницей, без пробелов).
    7. В поле «Название» впиши: `Отправка заявки`.
    8. Нажми «Добавить цель» внизу. Цель появится в списке.

    **Проверить, что цель ловит отправку:**
    9. Открой сайт крылья.life в новой вкладке. Прокрути до формы заявки.
    10. Заполни форму тестовыми данными (имя, свой телефон, любое сообщение от 10 символов), поставь галочку согласия, нажми «Оставить заявку». Должна открыться страница «Спасибо».
    11. Вернись в Метрику. Слева нажми «Отчёты» → «Конверсии» (или открой цель «Отправка заявки»). В течение нескольких минут там должно появиться достижение цели — цифра «1» или больше. **Должно появиться: минимум одно достижение цели «Отправка заявки».**

    **Проверить счётчик и вебвизор:**
    12. В левом меню «Вебвизор» — открой. Должны быть видны записи визитов (в т.ч. твой только что). **Должно появиться: список записанных сессий.**
    13. Дубль счётчика: открой крылья.life, нажми правой кнопкой → «Просмотр кода страницы» (View source), нажми Ctrl+F, найди `mc.yandex.ru`. Должно быть немного вхождений и ровно ОДИН вызов инициализации счётчика 99532899 (не два разных счётчика). Если сомневаешься — пришли Claude что нашла, он подскажет.

    Отпиши Claude результат каждого пункта (появилась ли цель, засчиталась ли тестовая отправка, пишет ли вебвизор, один ли счётчик).
  </how-to-verify>
  <acceptance_criteria>
    - Цель «Отправка заявки» типа «JavaScript-событие» с идентификатором `form_submitted` создана в кабинете
    - Тестовая отправка формы засчиталась как достижение цели (наблюдаемо: цифра ≥ 1 в отчёте «Конверсии»)
    - Вебвизор показывает записанные сессии
    - Счётчик 99532899 в коде один (нет второго стороннего счётчика)
    - Claude отметил результаты в .planning/audit/CHECKLIST.md (строка K-5 → «✅ цель настроена и проверена»)
  </acceptance_criteria>
  <resume-signal>Напиши «цель работает» и подтверди по пунктам, или опиши, что пошло не так (например, цель не засчиталась или счётчиков два).</resume-signal>
  <verify>
    <automated>MANUAL — Ручная проверка в кабинете (внешний сервис): тестовая отправка формы засчитана как достижение цели «Отправка заявки» (≥1 конверсия в отчёте «Конверсии»); вебвизор пишет сессии; счётчик один. Отметка в CHECKLIST.md.</automated>
  </verify>
  <done>Цель form_submitted создана и проверена тестовой отправкой; вебвизор пишет; дубля счётчика нет; результат отмечен в чек-листе.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Метрика — исключить свой трафик на компьютере и телефоне</name>
  <files>.planning/audit/CHECKLIST.md (внешний кабинет Яндекс.Метрика)</files>
  <read_first>
    - .planning/phases/09-seo/09-RESEARCH.md §«Яндекс.Метрика — Не учитывать мои визиты» (пошагово + Pitfall 6)
    - .planning/phases/09-seo/09-CONTEXT.md §D-03, §D-04 (браузерная метка на каждом устройстве; IP-фильтр НЕ используем)
  </read_first>
  <action>Чекпоинт для Марии. Мария включает «Не учитывать мои визиты» в фильтрах счётчика и проставляет браузерную метку на компьютере и телефоне (D-03; IP-фильтр НЕ используем — D-04). Пошагово — в <how-to-verify>. Claude фиксирует выполнение в CHECKLIST.md.</action>
  <what-built>
    Свой трафик Марии не должен попадать в статистику (иначе цифры базлайна завышены). Используем встроенную метку Метрики «не учитывать мои визиты» — её нужно поставить в браузере компьютера И в браузере телефона отдельно (D-03). Фильтр по IP НЕ используем — у мобильного IP меняется (D-04).
  </what-built>
  <how-to-verify>
    **На компьютере:**
    1. Открой metrika.yandex.ru, войди под своим логином. Выбери счётчик 99532899.
    2. Левое меню → «Настройка». Вверху вкладки → нажми «Фильтры».
    3. Найди строку «Не учитывать мои визиты» и поставь галочку рядом.
    4. Нажми «Сохранить» внизу.
    5. Подожди 15 минут. Зайди на крылья.life в этом же браузере компьютера. Потом в Метрике открой «Вебвизор» — этого твоего визита там быть НЕ должно (он отфильтрован). **Должно появиться: твой визит НЕ записан в вебвизоре.**

    **На телефоне:**
    6. Открой на телефоне ТОТ ЖЕ браузер, которым ты обычно заходишь на сайт (например Safari или Chrome).
    7. Зайди на metrika.yandex.ru, войди под тем же логином, что на компьютере.
    8. Просто открой страницу Метрики и подожди пару секунд — этого достаточно, метка запомнится в этом браузере. Можно закрыть.
    9. Если на телефоне ты пользуешься двумя браузерами (например и Safari, и Chrome) — повтори пункты 6–8 в каждом.

    Важно: галочку «Не учитывать мои визиты» в кабинете (пункт 3) ставим ОДИН раз — она общая. А вот «зайти в Метрику под своим логином» (пункты 6–8) надо сделать в КАЖДОМ браузере на КАЖДОМ устройстве, с которого ты ходишь на сайт.

    Отпиши Claude: поставила галочку на компьютере (да/нет), зашла в Метрику на телефоне (да/нет, в каких браузерах).
  </how-to-verify>
  <acceptance_criteria>
    - Галочка «Не учитывать мои визиты» включена в фильтрах счётчика 99532899
    - Метрика открыта под логином Марии в браузере компьютера и в браузере(ах) телефона (метка проставлена на всех устройствах, с которых она ходит на сайт)
    - Контрольная проверка: визит Марии с компьютера не появляется в вебвизоре после 15 минут
    - Claude отметил выполнение в .planning/audit/CHECKLIST.md
  </acceptance_criteria>
  <resume-signal>Напиши «свой трафик исключён» и укажи, на каких устройствах/браузерах проставила метку.</resume-signal>
  <verify>
    <automated>MANUAL — Ручная проверка: после включения метки визит Марии с компьютера не появляется в вебвизоре (через 15 мин). Отметка в CHECKLIST.md.</automated>
  </verify>
  <done>Метка «не учитывать мои визиты» включена и проставлена на всех устройствах Марии; контрольный визит не пишется; отмечено в чек-листе.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Google Search Console — property, sitemap, число проиндексированных страниц</name>
  <files>.planning/audit/CHECKLIST.md (внешний кабинет GSC)</files>
  <read_first>
    - .planning/phases/09-seo/09-RESEARCH.md §«Google Search Console — Проверить и настроить property» (пошагово + Pitfall 4)
    - .planning/audit/CHECKLIST.md
  </read_first>
  <action>Чекпоинт для Марии. Мария проверяет/добавляет один property в GSC (без дубля кириллица/Punycode — RESEARCH.md Pitfall 4), подаёт новый sitemap.xml, снимает число проиндексированных страниц. Пошагово — в <how-to-verify>. Claude фиксирует результат в CHECKLIST.md и передаёт число проиндексированных в базлайн (09-05).</action>
  <what-built>
    Проверяем, что сайт корректно подключён к Google Search Console: property на месте, свежий кириллический sitemap подан, видно сколько страниц проиндексировано. Важно не создать дубль property кириллица/Punycode (RESEARCH.md Pitfall 4) — данные раздвоятся.
  </what-built>
  <how-to-verify>
    Мария, открой search.google.com/search-console, войди под своей почтой Gmail.

    **Проверить property:**
    1. Слева вверху выпадающий список сайтов. Посмотри, есть ли там `xn--j1aco8bgs.life` или `крылья.life`. Если есть — сайт уже подключён, переходи к пункту 4.
    2. Если сайта нет: нажми «Добавить ресурс» → выбери «Префикс URL» → впиши `https://xn--j1aco8bgs.life` → нажми «Продолжить».
    3. Способ подтверждения — «Файл HTML»: он у нас уже загружен на сайт, поэтому просто нажми «Подтвердить». Должна появиться галочка «Право собственности подтверждено».
    Важно: НЕ добавляй второй ресурс отдельно кириллицей — иначе данные раздвоятся на два отчёта. Достаточно одного.

    **Подать sitemap:**
    4. Левое меню → «Индексирование» → «Файлы Sitemap».
    5. В поле «Добавьте новый файл Sitemap» впиши: `sitemap.xml` (просто так, без домена — Google подставит домен сам). Нажми «Отправить».
    6. Если раньше был подан старый `sitemap-index.xml` — можешь оставить его, Google сам поймёт что он пустой/устаревший; главное чтобы новый `sitemap.xml` был со статусом «Успешно». **Должно появиться: строка sitemap.xml со статусом «Успешно» (может занять несколько часов на обработку).**

    **Снять число проиндексированных:**
    7. Левое меню → «Индексирование» → «Страницы».
    8. Вверху увидишь число «Проиндексировано». Запиши эту цифру и пришли Claude — она пойдёт в таблицу базлайна (план 09-05). **Должно появиться: число проиндексированных страниц.**

    Отпиши Claude: какой property подключён (кириллица/Punycode/оба), статус sitemap, число проиндексированных страниц.
  </how-to-verify>
  <acceptance_criteria>
    - В GSC ровно ОДИН property для сайта (нет дубля кириллица+Punycode как двух отдельных ресурсов) — AUDIT-03
    - Новый `sitemap.xml` подан; статус «Успешно» (или «Обрабатывается» с последующим успехом)
    - Зафиксировано число проиндексированных страниц (передано Claude для базлайна)
    - Claude отметил результаты в .planning/audit/CHECKLIST.md
  </acceptance_criteria>
  <resume-signal>Напиши «GSC проверен», укажи какой property, статус sitemap и число проиндексированных страниц.</resume-signal>
  <verify>
    <automated>MANUAL — Ручная проверка в GSC: ровно один property; sitemap.xml со статусом «Успешно»/«Обрабатывается»; снято число проиндексированных страниц. Отметка в CHECKLIST.md.</automated>
  </verify>
  <done>Один property подтверждён, sitemap.xml подан, число проиндексированных страниц снято и передано для базлайна; отмечено в чек-листе.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Мария → внешние кабинеты (Метрика, GSC) | Аутентифицированный доступ к аналитике сайта под учёткой Марии |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-09 | Information Disclosure | Вебвизор пишет содержимое формы (ПДн: имя/телефон в заявке) | mitigate | Вебвизор Метрики по умолчанию маскирует значения полей ввода; при тестовой отправке использовать свой номер, не клиентский. Отметить в чек-листе: проверить, что маскировка полей формы включена (Настройка → Вебвизор → «не записывать содержимое полей»). |
| T-09-10 | Spoofing | Двойной property в GSC | mitigate | Явная инструкция добавлять один ресурс; предотвращает раздвоение данных и путаницу в атрибуции. |
| T-09-11 | Elevation of Privilege | Доступ к кабинетам | accept | Кабинеты под личной учёткой Марии; доступы вне нашего кода (Claude's Discretion в CONTEXT.md — допущение, что логины есть). |

Наш код здесь не меняется (кроме отметок в чек-листе). ASVS V2/V6 — вне периметра (внешние сервисы Яндекс/Google).
</threat_model>

<verification>
Ручная проверка (кабинеты — внешние). Наблюдаемые критерии:
- Метрика: цель «Отправка заявки» засчитала тестовую отправку (≥1 конверсия)
- Метрика: визит Марии не пишется в вебвизор после включения метки
- GSC: один property, sitemap.xml «Успешно», число проиндексированных снято
- `.planning/audit/CHECKLIST.md` содержит отметки о выполнении: `grep -c "Метрика" .planning/audit/CHECKLIST.md` ≥ 1
</verification>

<success_criteria>
- JS-цель form_submitted создана и проверена тестовой отправкой (AUDIT-02)
- Свой трафик Марии исключён на всех её устройствах через браузерную метку (D-03, AUDIT-02)
- Счётчик один, вебвизор пишет, содержимое полей формы маскируется (AUDIT-02)
- GSC: один property, sitemap подан, число проиндексированных страниц снято (AUDIT-03)
</success_criteria>

<output>
После завершения создать `.planning/phases/09-seo/09-04-SUMMARY.md` (что настроено в Метрике и GSC, результат тестовой отправки, число проиндексированных страниц из GSC для передачи в базлайн).
</output>
