---
phase: 09-seo
plan: 05
type: execute
wave: 4
depends_on: ["09-04"]
files_modified:
  - .planning/audit/BASELINE.md
autonomous: false
requirements: [AUDIT-04]
must_haves:
  truths:
    - "В таблице базлайна заполнены позиции по 16 запросам в Яндексе и Google (или н/д)"
    - "Зафиксировано число проиндексированных страниц в Я.Вебмастере и GSC"
    - "Зафиксированы суммарные показы и клики за 30 дней в обеих системах"
    - "Список запросов заморожен — это точка отсчёта для всего этапа v2.0"
  artifacts:
    - path: ".planning/audit/BASELINE.md"
      provides: "заполненная замороженная таблица базлайна с датой снятия"
      contains: "Дата снятия"
  key_links:
    - from: "Я.Вебмастер группа «Базлайн Phase 9»"
      to: ".planning/audit/BASELINE.md"
      via: "перенос позиций/показов/кликов в таблицу"
      pattern: "Позиция Яндекс"
---

<objective>
Снять и зафиксировать стартовый базлайн метрик в `.planning/audit/BASELINE.md`: позиции по 16 замороженным запросам в Яндексе и Google, число проиндексированных страниц в Я.Вебмастере и GSC, суммарные показы и клики за 30 дней. Действия Марии в кабинетах Я.Вебмастера и GSC — пошагово; Claude переносит цифры в таблицу.

Purpose: AUDIT-04 — точка отсчёта для сравнения роста на всём этапе v2.0. Это ПОСЛЕДНЯЯ волна фазы: базлайн снимается уже после исправления canonical (09-01/03) и настройки кабинетов (09-04), поэтому фиксирует КОРРЕКТНОЕ состояние сайта (иначе заморозили бы кривую картину — RESEARCH.md).

Output: заполненная замороженная таблица базлайна. План НЕ автономный — данные только из кабинетов Марии.

Зависит от 09-04 (property GSC на месте, свой трафик исключён — иначе показы/клики завышены).

Важно: часть данных требует накопления (Я.Вебмастер группе запросов нужно 24–48 ч). План можно начать сразу (создать группу в Вебмастере, снять GSC и ручные позиции), а показы/клики по группе досниать через 1–2 дня — это нормально для базлайна.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/09-seo/09-RESEARCH.md
@.planning/phases/09-seo/09-04-SUMMARY.md
@.planning/audit/BASELINE.md
@seo/keywords.md

<interfaces>
<!-- Таблица-скелет уже создана в 09-02: 16 замороженных запросов, колонки Яндекс+Google. Заполняем ячейки. -->
<!-- Число проиндексированных в GSC уже снято в 09-04 (в 09-04-SUMMARY) — перенести в таблицу. -->
<!-- Скудные данные у нового сайта: если по запросу <10 показов, кабинет не покажет → ставить «н/д» + ручная проверка инкогнито (RESEARCH.md Pitfall 5). Не выдумывать цифры. -->
<!-- Точные клик-пути — RESEARCH.md §«Яндекс.Вебмастер — Группы запросов» и §«GSC → Performance». -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Я.Вебмастер — группа запросов, число проиндексированных, показы/клики</name>
  <files>.planning/audit/BASELINE.md (данные из кабинета Я.Вебмастер)</files>
  <read_first>
    - .planning/phases/09-seo/09-RESEARCH.md §«Яндекс.Вебмастер — Группы запросов для базлайна» (пошагово) и §Pitfall 5 (скудные данные)
    - .planning/audit/BASELINE.md (таблица-скелет, куда вносим цифры)
    - seo/keywords.md (группы 1–3 — тот же список из 16 запросов)
  </read_first>
  <action>Чекпоинт для Марии. Мария создаёт в Я.Вебмастере группу «Базлайн Phase 9» с 16 запросами, снимает число проиндексированных, показы/клики и позиции по запросам. Пошагово — в <how-to-verify>. Claude вносит цифры в .planning/audit/BASELINE.md.</action>
  <what-built>
    Скелет таблицы базлайна с 16 замороженными запросами уже готов (09-02). Теперь Мария снимает по ним данные из Яндекс.Вебмастера, а Claude вносит цифры в таблицу.
  </what-built>
  <how-to-verify>
    Мария, открой webmaster.yandex.ru, войди под своим логином, выбери сайт крылья.life.

    **Создать группу запросов:**
    1. Левое меню → «Поисковые запросы» (или «Эффективность») → «Управление группами».
    2. Нажми «Создать группу». Назови её `Базлайн Phase 9`.
    3. Добавь в группу по одному все 16 запросов. Список пришлёт Claude (он возьмёт их из таблицы базлайна) — просто копируй каждый и добавляй. Нажми «Сохранить».

    **Снять число проиндексированных страниц:**
    4. Левое меню → «Индексирование» → «Страницы в поиске» (или «Статистика обхода»). Найди число страниц в поиске. Запиши цифру, пришли Claude. **Должно появиться: число проиндексированных страниц.**

    **Снять показы/клики (это можно через 1–2 дня, если сейчас пусто):**
    5. Вернись в «Управление группами» → открой группу «Базлайн Phase 9». Там будет средняя позиция, показы и клики по группе. Если данных ещё нет (группа только создана) — вернёшься сюда через 24–48 часов. Запиши: суммарные показы, суммарные клики. Пришли Claude.
    6. Для позиций по отдельным запросам: «Поисковые запросы» → «Мониторинг запросов» → найди каждый запрос → посмотри среднюю позицию. Для запросов, по которым Вебмастер пишет «мало данных» — поставим «н/д» (проверим вручную в Task 3).

    Отпиши Claude по каждому запросу: позиция в Яндексе (число или «н/д»), и отдельно — число проиндексированных страниц, суммарные показы и клики за период.
  </how-to-verify>
  <acceptance_criteria>
    - Группа «Базлайн Phase 9» с 16 запросами создана в Вебмастере
    - Число проиндексированных страниц в Я.Вебмастере снято и передано Claude
    - По каждому из 16 запросов есть позиция Яндекса (число или «н/д»)
    - Суммарные показы и клики за 30 дней (Яндекс) сняты (или помечено «досниать через 24–48 ч»)
    - Claude внёс данные в колонки «Позиция Яндекс / Показы Я / Клики Я» и строки индексации в .planning/audit/BASELINE.md
  </acceptance_criteria>
  <resume-signal>Напиши позиции по запросам в Яндексе + число проиндексированных + показы/клики (или «данные группы соберутся через 1–2 дня»).</resume-signal>
  <verify>
    <automated>MANUAL — Ручная проверка: группа с 16 запросами создана; по каждому запросу есть позиция Яндекса (число/«н/д»); число проиндексированных и показы/клики сняты и внесены в BASELINE.md.</automated>
  </verify>
  <done>Позиции Яндекса по 16 запросам, число проиндексированных (Вебмастер) и показы/клики внесены в таблицу базлайна.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Google Search Console — позиции, показы, клики по 16 запросам</name>
  <files>.planning/audit/BASELINE.md (данные из кабинета GSC)</files>
  <read_first>
    - .planning/phases/09-seo/09-RESEARCH.md §«GSC → Performance» (пошагово)
    - .planning/phases/09-seo/09-04-SUMMARY.md (число проиндексированных в GSC уже снято здесь — перенести)
    - .planning/audit/BASELINE.md
  </read_first>
  <action>Чекпоинт для Марии. Мария снимает в GSC → Эффективность позиции/показы/клики по 16 запросам за 28 дней и суммарные показы/клики. Пошагово — в <how-to-verify>. Claude вносит данные в BASELINE.md и переносит число проиндексированных из 09-04.</action>
  <what-built>
    Снимаем данные Google по тем же 16 запросам. Число проиндексированных в GSC уже получено в плане 09-04 — Claude перенесёт его в таблицу. Здесь добираем позиции/показы/клики по запросам.
  </what-built>
  <how-to-verify>
    Мария, открой search.google.com/search-console, выбери свой сайт.

    1. Левое меню → «Эффективность» → «Результаты поиска».
    2. Вверху выбери период: нажми на «Дата» → «Последние 28 дней».
    3. Убедись, что включены галочки «Клики», «Показы», «Средняя позиция» (кнопки-плашки сверху графика).
    4. Ниже нажми вкладку «Запросы». Появится список поисковых запросов, по которым сайт показывался.
    5. По каждому из 16 наших запросов (список у Claude): найди его в списке (можно через фильтр/поиск по запросу) и запиши позицию, показы, клики. Если запроса в списке нет — значит по нему меньше ~10 показов, ставим «н/д» (проверим вручную в Task 3).
    6. Общие показы/клики: вверху графика видны суммарные «Клики» и «Показы» за 28 дней — запиши их (это строка «Суммарные показы/клики» в таблице).

    Отпиши Claude по каждому запросу: позиция/показы/клики в Google (или «н/д»), плюс суммарные показы и клики.
  </how-to-verify>
  <acceptance_criteria>
    - По каждому из 16 запросов есть данные Google (позиция/показы/клики или «н/д»)
    - Суммарные показы и клики (Google, 28 дней) сняты
    - Число проиндексированных страниц GSC (из 09-04) внесено в таблицу
    - Claude внёс данные в колонки «Позиция Google / Показы G / Клики G» в .planning/audit/BASELINE.md
  </acceptance_criteria>
  <resume-signal>Напиши позиции/показы/клики по запросам в Google + суммарные показы и клики.</resume-signal>
  <verify>
    <automated>MANUAL — Ручная проверка: по каждому из 16 запросов есть данные Google (позиция/показы/клики или «н/д»); суммарные показы/клики сняты; всё внесено в BASELINE.md.</automated>
  </verify>
  <done>Данные Google по 16 запросам + суммарные показы/клики + число проиндексированных (GSC) внесены в таблицу базлайна.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Ручная доснятие «н/д» позиций и заморозка базлайна</name>
  <files>.planning/audit/BASELINE.md (ручная проверка инкогнито)</files>
  <read_first>
    - .planning/phases/09-seo/09-RESEARCH.md §Pitfall 5 (ручная проверка инкогнито)
    - .planning/audit/BASELINE.md (строки с «н/д» после Task 1–2)
  </read_first>
  <action>Чекпоинт для Марии. По запросам, где в кабинетах «н/д», Мария снимает позицию вручную в инкогнито на ya.ru и google.com. Пошагово — в <how-to-verify>. Claude вносит значения и замораживает таблицу (дата снятия + пометка).</action>
  <what-built>
    По части запросов у нового сайта в кабинетах может не быть данных («н/д»). Для них снимаем позицию вручную в режиме инкогнито — это единственный способ получить хоть какую-то отправную точку. После этого таблица базлайна замораживается.
  </what-built>
  <how-to-verify>
    Мария, Claude пришлёт список запросов, где стоит «н/д». По каждому:
    1. Открой НОВОЕ окно в режиме инкогнито (в Chrome: три точки справа вверху → «Новое окно в режиме инкогнито»; в Яндекс.Браузере — аналогично).
    2. Зайди на ya.ru, впиши запрос точь-в-точь как в списке. Пролистай выдачу и найди, на какой позиции появляется крылья.life (считая по порядку, начиная с 1). Если сайта нет в первых 3 страницах — пиши «>30». Запиши позицию.
    3. То же самое на google.com в этом же инкогнито-окне.
    4. Пришли Claude позиции по каждому такому запросу. Claude впишет их в таблицу с пометкой «(ручная проверка, дата)».

    Когда все 16 строк заполнены (числом, «н/д» или ручной проверкой) — Claude проставит в шапке таблицы «Дата снятия» и пометку «БАЗЛАЙН ЗАМОРОЖЕН». После этого список запросов и цифры не меняем — это точка отсчёта на весь этап (D-02).

    Отпиши Claude позиции по «н/д»-запросам, либо «в кабинетах все данные есть, ручная проверка не нужна».
  </how-to-verify>
  <acceptance_criteria>
    - Все 16 строк таблицы заполнены (число / «н/д» / ручная проверка) в обеих системах
    - В шапке BASELINE.md проставлена «Дата снятия» и пометка о заморозке
    - В таблице нет пустых ячеек позиций без пометки: каждая ячейка = число, «н/д» или значение ручной проверки
    - Claude обновил .planning/audit/BASELINE.md финально
  </acceptance_criteria>
  <resume-signal>Напиши позиции по «н/д»-запросам из инкогнито, или подтверди, что все данные уже в кабинетах.</resume-signal>
  <verify>
    <automated>MANUAL — Ручная проверка: все 16 строк заполнены (число/«н/д»/ручная проверка) в обеих системах; в шапке BASELINE.md проставлена дата снятия и пометка о заморозке.</automated>
  </verify>
  <done>Все строки базлайна заполнены; таблица заморожена с датой снятия — точка отсчёта этапа v2.0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Мария → Я.Вебмастер / GSC | Аутентифицированный доступ к данным поисковой статистики сайта |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-12 | Information Disclosure | BASELINE.md в .planning/ | accept | Содержит только агрегированные SEO-метрики (позиции/показы/клики), без ПДн; лежит в приватном `.planning/`, не публикуется. |
| T-09-13 | Repudiation | Достоверность базлайна | mitigate | Список запросов заморожен с датой снятия; «н/д» и ручные проверки помечаются явно — цифры не выдумываются (RESEARCH.md Pitfall 5), базлайн воспроизводим. |

Наш код не меняется. Аутентификация/криптография кабинетов — вне периметра (внешние сервисы).
</threat_model>

<verification>
Ручная (данные из кабинетов). Наблюдаемые критерии:
- `grep -c "Дата снятия" .planning/audit/BASELINE.md` ≥ 1 и дата проставлена (не «заполняется…»)
- Все 16 строк запросов заполнены значением/«н/д» в колонках Яндекс и Google
- Строки «Проиндексированных страниц» заполнены для Вебмастера и GSC
- Строки «Суммарные показы/клики» заполнены для обеих систем
</verification>

<success_criteria>
- Базлайн зафиксирован: позиции по 16 запросам (Яндекс + Google), число проиндексированных (Вебмастер + GSC), показы+клики 30д — всё в таблице (AUDIT-04)
- Запросы без данных помечены «н/д» + доснаты вручную; ничего не выдумано
- Таблица заморожена с датой снятия — точка отсчёта для всего этапа v2.0
</success_criteria>

<output>
После завершения создать `.planning/phases/09-seo/09-05-SUMMARY.md` (снятые цифры базлайна: сколько запросов с данными/н/д, число проиндексированных в обеих системах, суммарные показы/клики, дата заморозки).
</output>
