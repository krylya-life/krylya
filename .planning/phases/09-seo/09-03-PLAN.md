---
phase: 09-seo
plan: 03
type: execute
wave: 2
depends_on: ["09-01", "09-02"]
files_modified:
  - .planning/audit/CHECKLIST.md
autonomous: true
requirements: [AUDIT-05]
must_haves:
  truths:
    - "Исправления кода из 09-01 задеплоены на боевой крылья.life"
    - "На живом сайте canonical в кириллице, sitemap кириллический без /thanks/"
    - "Core Web Vitals главной на мобильном — в зелёной зоне (или зафиксировано отклонение)"
    - "В чек-листе статусы всех критичных ошибок K-1…K-4 переведены в «исправлено»"
  artifacts:
    - path: ".planning/audit/CHECKLIST.md"
      provides: "чек-лист с обновлёнными статусами после деплоя"
      contains: "исправлено"
  key_links:
    - from: "git push"
      to: "Cloudflare Pages крылья.life"
      via: "автодеплой из main"
      pattern: "крылья.life"
---

<objective>
Задеплоить исправления кода из плана 09-01 (git push → Cloudflare Pages автодеплой), проверить на ЖИВОМ сайте крылья.life, что фиксы применились (canonical в кириллице, sitemap кириллический без /thanks/, reachGoal в HTML), перепроверить Core Web Vitals главной на мобильном после деплоя, и обновить статусы критичных ошибок K-1…K-4 в `.planning/audit/CHECKLIST.md` на «исправлено».

Purpose: AUDIT-05 — критические/важные ошибки не просто исправлены в коде, но проверены на боевом сайте. Это последний шаг перед настройкой кабинетов (09-04) и снятием базлайна (09-05): базлайн должен сниматься уже с корректного живого сайта.

Output: задеплоенный сайт + обновлённый чек-лист. Автономный план (git + curl + PageSpeed по публичному URL + правка файла).

Зависит от 09-01 (правки кода) и 09-02 (чек-лист, где обновляем статусы).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-seo/09-RESEARCH.md
@.planning/phases/09-seo/09-01-SUMMARY.md
@.planning/audit/CHECKLIST.md

<interfaces>
<!-- Хостинг: Cloudflare Pages, автодеплой из GitHub main (STATE.md). Деплой = git push. -->
<!-- Домен: крылья.life (кириллица, primary) + xn--j1aco8bgs.life (Punycode, редирект 301). -->
<!-- curl по IDN-домену: использовать Punycode-хост, т.к. curl/DNS работают с ACE-формой: -->
<!--   curl -sL https://xn--j1aco8bgs.life/ -->
<!-- Cloudflare Pages деплой занимает ~1-3 минуты после push. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Деплой и проверка фиксов на живом сайте</name>
  <files>src/ (деплой через git push), .planning/audit/CHECKLIST.md</files>
  <read_first>
    - .planning/phases/09-seo/09-01-SUMMARY.md (что именно исправлено в коде)
    - .planning/STATE.md §«Хостинг» (Cloudflare Pages, автодеплой из main)
    - .planning/phases/09-seo/09-RESEARCH.md §«Как работает IDN-домен» (canonical/sitemap/robots — ожидаемое состояние после фикса)
  </read_first>
  <action>
    Задеплоить и проверить (AUDIT-05).

    1. Финальная локальная проверка билда перед деплоем:
    ```bash
    npm run build
    grep 'rel="canonical"' dist/index.html | grep крылья.life   # не xn--
    ! grep -q xn-- dist/sitemap.xml && echo sitemap-ok
    grep -c thanks dist/sitemap.xml                              # 0
    grep -q reachGoal dist/index.html && echo goal-ok
    ```
    Если хоть одна проверка красная — вернуться к 09-01, не деплоить.

    2. Закоммитить и запушить в main (запускает автодеплой Cloudflare Pages):
    ```bash
    git add -A
    git commit -m "fix(09-seo): кириллический canonical/sitemap + JS-цель form_submitted"
    git push
    ```
    Если рабочая ветка не main — сначала создать ветку от текущей и запушить её (следовать git-политике проекта: branching_strategy=none → работаем в текущей ветке; если это не main и push в main требует PR — открыть PR через gh и отметить это в SUMMARY).

    3. Подождать завершения деплоя (~2-3 мин). Проверить ЖИВОЙ сайт (curl по Punycode-хосту, т.к. DNS/curl работают с ACE-формой; сервер отдаёт тот же контент):
    ```bash
    curl -sL https://xn--j1aco8bgs.life/ | grep -o 'rel="canonical"[^>]*'
    # ожидаем: href="https://крылья.life/" (кириллица в теле ответа)
    curl -sL https://xn--j1aco8bgs.life/sitemap.xml | grep -c "крылья.life"   # ≥ 13
    curl -sL https://xn--j1aco8bgs.life/sitemap.xml | grep -c "thanks"        # 0
    curl -sL https://xn--j1aco8bgs.life/ | grep -c "reachGoal"                # ≥ 1
    ```
    Если старый sitemap-index.xml ещё отдаётся из кэша Cloudflare — отметить в SUMMARY (кэш обновится; главное, что новый `/sitemap.xml` доступен и корректен). Проверить, что `/sitemap-index.xml` больше не нужен (robots указывает на `/sitemap.xml`).
  </action>
  <acceptance_criteria>
    - `curl -sL https://xn--j1aco8bgs.life/` содержит `rel="canonical"` со значением `крылья.life`, НЕ содержит `xn--` в canonical
    - `curl -sL https://xn--j1aco8bgs.life/sitemap.xml` содержит `крылья.life`, НЕ содержит `thanks`, НЕ содержит `xn--`
    - `curl -sL https://xn--j1aco8bgs.life/` содержит `reachGoal`
    - `git log -1 --oneline` показывает коммит с фиксом
  </acceptance_criteria>
  <verify>
    <automated>curl -sL https://xn--j1aco8bgs.life/ | grep 'rel="canonical"' | grep -q "крылья.life" && curl -sL https://xn--j1aco8bgs.life/sitemap.xml | grep -q "крылья.life" && ! curl -sL https://xn--j1aco8bgs.life/sitemap.xml | grep -q "thanks" && echo LIVE-OK</automated>
  </verify>
  <done>Фиксы задеплоены; на живом сайте canonical в кириллице, sitemap кириллический без /thanks/, reachGoal присутствует в HTML главной; коммит в истории.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Проверка Core Web Vitals после деплоя + обновление статусов чек-листа</name>
  <files>.planning/audit/CHECKLIST.md</files>
  <read_first>
    - .planning/audit/CHECKLIST.md (файл правки — статусы K-1…K-5 и раздел ВАЖНО)
    - .planning/phases/09-seo/09-02-SUMMARY.md (какой CWV был зафиксирован до деплоя)
    - .planning/phases/09-seo/09-RESEARCH.md §«Core Web Vitals» (LCP-риски: обложки кейсов)
  </read_first>
  <action>
    Закрыть контур AUDIT-05 (критично + важно исправлено И проверено, per D-05).

    1. Прогнать главную и одну страницу услуги через PageSpeed Insights (pagespeed.web.dev), мобильная версия, ПОСЛЕ деплоя. Записать LCP, CLS, INP, Performance-балл. Определить зону:
       - зелёная: LCP ≤ 2.5s, CLS ≤ 0.1, INP ≤ 200ms
       - если что-то в жёлтой/красной зоне — зафиксировать конкретное значение и причину (обычно LCP от обложек кейсов) в чек-листе как ВАЖНО с планом фикса; если фикс лёгкий (добавить width/height, `loading=lazy`, preload hero) и укладывается в эту фазу — исправить в коде, задеплreployить (git push) и перепроверить. Если фикс тянет на отдельную задачу — занести в бэклог «желательно» с явной пометкой.

    2. Обновить `.planning/audit/CHECKLIST.md`:
       - K-1, K-2, K-3, K-4 — статус «✅ исправлено (09-01, проверено на живом сайте 09-03)»
       - K-5 — статус «✅ код исправлено (09-01); настройка цели в кабинете — 09-04»
       - Все ошибки ранга ВАЖНО, исправленные в этой фазе — статус «✅ исправлено»
       - Ошибки ВАЖНО, которые по итогу отложены — переместить в раздел ЖЕЛАТЕЛЬНО/бэклог с обоснованием (согласно D-05: в Phase 9 чиним критично+важно; если важное реально не влезает — явно задокументировать перенос)
       - Добавить строку итога CWV после деплоя (значения LCP/CLS/INP, зона)
  </action>
  <acceptance_criteria>
    - `grep -c "исправлено" .planning/audit/CHECKLIST.md` — ≥ 4 (K-1…K-4)
    - в чек-листе нет критичной ошибки со статусом `[ ]` (незакрытой): раздел КРИТИЧНО не содержит незаполненных чекбоксов `| \[ \] |`
    - зафиксирован CWV после деплоя: `grep -icE "LCP|Core Web Vitals" .planning/audit/CHECKLIST.md` — ≥ 1
  </acceptance_criteria>
  <verify>
    <automated>[ $(grep -c "исправлено" .planning/audit/CHECKLIST.md) -ge 4 ] && ! grep -A20 "КРИТИЧНО" .planning/audit/CHECKLIST.md | grep -qE "\| \[ \] \|" && echo CHECKLIST-OK</automated>
  </verify>
  <done>CWV главной на мобильном перепроверены после деплоя и зафиксированы; статусы K-1…K-5 и всех «важных» ошибок обновлены в чек-листе; ни одна критичная ошибка не осталась открытой.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Локальный git → GitHub → Cloudflare Pages | Пуш кода запускает автодеплой в прод |
| Публичный сайт → PageSpeed Insights | Публичный URL отдаётся стороннему сервису |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-07 | Tampering | git push в прод | mitigate | Деплой только после зелёного локального билда и grep-проверок; коммит атомарный, содержит только фиксы плана 09-01. |
| T-09-08 | Denial of Service | публичный sitemap-endpoint | accept | Статичный XML из ~13 захардкоженных маршрутов, без БД/пользовательского ввода — нагрузки на генерацию нет, Cloudflare кэширует. |

Секретов в коммите нет (токены в env Cloudflare, не в git — INFRA-05). Аутентификация/криптография — N/A.
</threat_model>

<verification>
```bash
curl -sL https://xn--j1aco8bgs.life/ | grep 'rel="canonical"' | grep -q "крылья.life" \
  && ! curl -sL https://xn--j1aco8bgs.life/sitemap.xml | grep -q "thanks" \
  && [ $(grep -c "исправлено" .planning/audit/CHECKLIST.md) -ge 4 ] \
  && echo "PLAN 03 GREEN"
```
</verification>

<success_criteria>
- Фиксы 09-01 задеплоены и подтверждены на живом крылья.life (canonical кириллица, sitemap без /thanks/, reachGoal в HTML)
- Core Web Vitals главной (mobile) перепроверены после деплоя и зафиксированы
- Статусы всех критичных/важных ошибок в чек-листе обновлены; критичных открытых не осталось (AUDIT-05)
</success_criteria>

<output>
После завершения создать `.planning/phases/09-seo/09-03-SUMMARY.md` (что задеплоено, результаты curl-проверок живого сайта, CWV после деплоя, какие статусы обновлены).
</output>
