---
phase: 05.1-dark-redesign
plan: 04
type: execute
wave: 4
depends_on: ["05.1-03"]
files_modified:
  - src/pages/services/index.astro
  - src/pages/services/[slug].astro
  - src/pages/pricing.astro
  - src/components/blocks/PricingTeaser.astro
  - src/components/Breadcrumbs.astro
autonomous: true
requirements: []
must_haves:
  truths:
    - "Витрина /services/ оформлена в тёмной палитре: тёмный фон, 6 карточек услуг через MagicCard (курсорный глоу), бренд-жёлтый акцент"
    - "6 подстраниц /services/[slug]/ оформлены в тёмной палитре: тёмный фон, тёмная типографика MDX-контента, бренд-жёлтый акцент на заголовках, breadcrumbs читаемы на тёмном фоне"
    - "Страница /pricing/ оформлена в тёмной палитре: тёмный фон, читаемый текст, акцент на 10%-модели"
    - "PricingTeaser (блок, используемый внутри страниц услуг) переписан под тёмную тему — гармонирует с body-фоном"
    - "Компонент Breadcrumbs работает на тёмном фоне: текст белый/светлый, активная страница жёлтая, разделители видимы"
    - "Все JSON-LD (Service-schema, BreadcrumbList) сохранены и валидны — не сломалось из-за переноса"
  artifacts:
    - path: src/pages/services/index.astro
      provides: "Витрина услуг в тёмной палитре"
      contains: "DarkLayout"
    - path: src/pages/services/[slug].astro
      provides: "6 страниц услуг в тёмной палитре"
      contains: "DarkLayout"
    - path: src/pages/pricing.astro
      provides: "Страница 10%-модели в тёмной палитре"
      contains: "DarkLayout"
    - path: src/components/Breadcrumbs.astro
      provides: "Хлебные крошки, работающие на тёмном фоне"
      contains: "text-white"
  key_links:
    - from: src/pages/services/index.astro
      to: src/layouts/DarkLayout.astro
      via: "import"
      pattern: "DarkLayout"
    - from: src/pages/services/[slug].astro
      to: src/components/blocks/PricingTeaser.astro
      via: "import + render внутри страницы"
      pattern: "PricingTeaser"
    - from: src/pages/services/[slug].astro
      to: src/components/Breadcrumbs.astro
      via: "import"
      pattern: "Breadcrumbs"
---

<objective>
Перенести раздел «Услуги» (витрина + 6 подстраниц) и страницу `/pricing/` на тёмную палитру, используя утверждённый язык из плана 03. Параллельно с планами 05 и 06 — у нас файлы не пересекаются.

**Purpose:** услуги — основной коммерческий раздел сайта (Phase 4 закрыта 6 страницами ≥800 слов под SEO-кластеры). После переноса они должны: (а) визуально идти в одном языке с главной; (б) сохранить SEO (JSON-LD Service, BreadcrumbList, заголовки H1/H2); (в) сохранить ссылку на форму и pricing-teaser.

**Output:** 8 файлов в тёмной палитре (1 витрина + 6 подстраниц через [slug] + pricing + PricingTeaser-блок + Breadcrumbs). Breadcrumbs переписаны универсально (используются также в кейсах — планы 05).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.1-dark-redesign/05.1-CONTEXT.md
@.planning/phases/05.1-dark-redesign/05.1-03-SUMMARY.md
@src/pages/services/index.astro
@src/pages/services/[slug].astro
@src/pages/pricing.astro
@src/components/blocks/PricingTeaser.astro
@src/components/Breadcrumbs.astro
@src/layouts/DarkLayout.astro
@src/components/dark/MagicCard.astro

<interfaces>
<!-- Текущая структура /services/[slug].astro:
     BaseLayout
       Seo + JsonLdGraph + JsonLdService + Breadcrumbs
       <Section><Container><Heading h1>...</Heading></Container></Section>
       <Section><Container><MDX-Content /></Container></Section>
       <PricingTeaser />
       <ContactBlock />
     После плана: BaseLayout → DarkLayout. Внутренняя разметка переписывается на inline-классы тёмной палитры
     (как в design-lab/index.astro для секций), Heading и Section можно либо оставить с тёмными override,
     либо заменить inline. Решение: оставить Container/Section, но переопределить bg-* и text-* через классы. -->

Структура services/[slug].astro (новая):
- DarkLayout title={seoTitle} description={seoDescription} noindex
- Seo + JsonLdGraph + JsonLdService + BreadcrumbList JSON-LD остаются
- Hero страницы: section с заголовком H1 на тёмном (text-white), подзаголовок description, breadcrumbs сверху
- Контент: prose-инверт (text-white/85, типографика читается на тёмном)
- PricingTeaser (тёмный вариант)
- ContactBlock (уже тёмный, переехал в плане 03)

Tailwind prose-инверт: используем класс `prose prose-invert max-w-3xl prose-headings:text-white prose-a:text-[#FFF200] prose-strong:text-white`. Tailwind v4 поддерживает @tailwindcss/typography через плагин — проверить, подключён ли. Если нет — сделать минимальные стили вручную через ручной CSS.

Breadcrumbs текущий:
```astro
<nav aria-label="Breadcrumbs" class="text-sm text-gray-dark">
  <ol class="flex flex-wrap gap-2">
    {crumbs.map((c, i) => <li>...{c.label}</li>)}
  </ol>
</nav>
```
Новый: text-white/55, активный — text-[#FFF200], разделители — белые/40.

PricingTeaser текущий (28 строк): светлый блок с текстом про 10%-модель и кнопкой «Подробнее → /pricing/». Переписать на тёмный bg (lab-bg-alt или просто bg-white/[0.03]), с border-y border-white/10, заголовок text-white, кнопка — тёмный CTA или ссылка с жёлтым подчёркиванием.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Breadcrumbs + PricingTeaser в тёмной палитре</name>
  <files>src/components/Breadcrumbs.astro, src/components/blocks/PricingTeaser.astro</files>
  <read_first>
    - Текущий src/components/Breadcrumbs.astro (65 строк) — сохранить логику + JSON-LD, поменять только цвета
    - Текущий src/components/blocks/PricingTeaser.astro (28 строк)
    - Образцы тёмной типографики в design-lab/index.astro (классы text-white/70, text-white/55, text-[#FFF200])
  </read_first>
  <action>
    1. **Breadcrumbs.astro:**
       - Сохранить логику принятия `crumbs: { label: string; href?: string }[]` и микроразметку BreadcrumbList (это критично для SEO).
       - Поменять Tailwind-классы в визуальной части:
         - `<nav class="text-sm text-white/55">` (было text-gray-dark)
         - Ссылка не-активная: `<a class="hover:text-white transition-colors">{label}</a>` (было text-gray-dark hover:text-black)
         - Разделитель `<span class="text-white/30">/</span>` (или `›`, как было — оставить тот же символ)
         - Активная (последняя) крошка: `<span class="text-[#FFF200]">` (было text-black)
       - JSON-LD BreadcrumbList script внутри компонента — оставить байт-в-байт.
       - Padding-обвязку компонента (если есть `py-4 ...`) — сохранить, чтобы страницы не «съезжали».

    2. **PricingTeaser.astro:**
       - Полностью переписать на тёмную палитру.
       - Новая структура:
         ```astro
         ---
         import { business } from "../../config/business.ts";
         ---
         <section class="lab-bg-alt relative px-6 py-14 md:px-12 md:py-20">
           <div class="mx-auto max-w-4xl text-center">
             <p class="mb-4 inline-flex items-center gap-2 text-xs font-semibold uppercase tracking-[0.25em] text-[#FFF200]">
               <span class="inline-block h-px w-8 bg-[#FFF200]"></span>
               Прозрачная модель
             </p>
             <h2 class="text-3xl font-semibold text-white md:text-5xl">
               Наши 10%&nbsp;— только за&nbsp;организационную работу
             </h2>
             <p class="mt-6 text-white/65 md:text-lg leading-relaxed">
               Подрядчики идут в&nbsp;смете отдельной строкой по&nbsp;реальной цене.
               Вы&nbsp;видите, куда уходит каждый рубль.
             </p>
             <a href="/pricing/" class="mt-8 inline-flex items-center gap-2 rounded-full border border-white/20 px-5 py-3 text-sm font-medium text-white/85 transition hover:border-[#FFF200] hover:text-white">
               Подробнее о&nbsp;модели
               <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M5 12h14M13 5l7 7-7 7"/></svg>
             </a>
           </div>
         </section>
         ```
       - Точные тексты — сверить с текущим PricingTeaser, чтобы не потерять смысл/факты.
  </action>
  <verify>
    <automated>grep -q "text-white" src/components/Breadcrumbs.astro && grep -q "text-\[#FFF200\]" src/components/Breadcrumbs.astro && grep -q "BreadcrumbList" src/components/Breadcrumbs.astro && grep -q "lab-bg-alt\|bg-white" src/components/blocks/PricingTeaser.astro && grep -q "text-white" src/components/blocks/PricingTeaser.astro</automated>
  </verify>
  <acceptance_criteria>
    - Breadcrumbs читаемы на тёмном фоне (text-white/55), активная — жёлтая
    - Breadcrumbs сохраняют JSON-LD BreadcrumbList (важно для SEO)
    - PricingTeaser на тёмном фоне, заголовок text-white, акцент #FFF200, ссылка на /pricing/
  </acceptance_criteria>
  <done>Два переиспользуемых компонента готовы — они будут использоваться и в плане 05 (кейсы) тоже.</done>
</task>

<task type="auto">
  <name>Task 2: Витрина /services/ + страница /pricing/ в тёмной палитре</name>
  <files>src/pages/services/index.astro, src/pages/pricing.astro</files>
  <read_first>
    - Текущий src/pages/services/index.astro целиком
    - Текущий src/pages/pricing.astro целиком
    - src/components/blocks/ContactBlock.astro (уже тёмный после плана 03) — переиспользуем
    - src/components/dark/MagicCard.astro — для карточек услуг
  </read_first>
  <action>
    1. **src/pages/services/index.astro:**
       - Заменить `import BaseLayout from "../../layouts/BaseLayout.astro"` → `import DarkLayout from "../../layouts/DarkLayout.astro"`.
       - Убрать импорт `Section`, `Heading` (или оставить, но передавать им классы для тёмной темы — проще убрать и сделать inline).
       - Добавить `import MagicCard from "../../components/dark/MagicCard.astro"`.
       - Структура страницы:
         ```astro
         <DarkLayout title={seoTitle} description={seoDescription} noindex>
           <Seo slot="seo" ... />
           <JsonLdGraph slot="seo" />

           <!-- Hero-секция страницы -->
           <section class="relative overflow-hidden px-6 pt-32 pb-14 md:px-12 md:pt-40 md:pb-20">
             <div class="lab-aurora pointer-events-none"></div>
             <div class="relative mx-auto max-w-6xl">
               <Breadcrumbs crumbs={crumbs} />
               <h1 class="mt-6 text-4xl font-semibold tracking-tight text-white md:text-6xl">
                 Услуги
               </h1>
               <span class="mt-4 inline-block h-1 w-12 bg-[#FFF200]"></span>
               <p class="mt-6 max-w-3xl text-white/65 md:text-lg leading-relaxed">
                 Полный цикл организации мероприятия в Калининграде и области:
                 корпоративы, деловые события, тимбилдинги, координация дня.
               </p>
             </div>
           </section>

           <div class="lab-divider-glow mx-auto max-w-6xl"></div>

           <!-- 6 услуг сеткой 2×3 -->
           <section class="relative px-6 py-14 md:px-12 md:py-20">
             <div class="mx-auto max-w-6xl">
               <div class="grid gap-5 md:grid-cols-2 lg:grid-cols-3">
                 {services.map((s) => (
                   <MagicCard href={`/services/${s.id}/`} class="flex min-h-[200px] flex-col justify-between p-6">
                     <div>
                       <h2 class="text-xl font-semibold text-white">{s.data.title}</h2>
                       <p class="mt-3 text-sm leading-relaxed text-white/60">{s.data.description}</p>
                     </div>
                     <span class="mt-6 inline-flex items-center gap-2 text-sm font-medium text-[#FFF200]">
                       Подробнее
                       <span class="inline-block h-px w-6 bg-[#FFF200] transition-all group-hover:w-10"></span>
                     </span>
                   </MagicCard>
                 ))}
               </div>
             </div>
           </section>

           <div class="lab-divider-glow mx-auto max-w-6xl"></div>

           <PricingTeaser />

           <div class="lab-divider-glow mx-auto max-w-6xl"></div>

           <ContactBlock />

           <MetrikaCounter slot="analytics" />
         </DarkLayout>
         ```
       - Все SEO-импорты (Seo, JsonLdGraph, Breadcrumbs, MetrikaCounter, ContactBlock, PricingTeaser) сохранить.
       - title/description/crumbs — оставить как есть в исходнике.

    2. **src/pages/pricing.astro:**
       - Аналогично переключить на DarkLayout.
       - Hero-секция: breadcrumbs, H1 «Сколько стоит мероприятие», подзаголовок про 10%-модель.
       - Контент про 10%-модель + блок «Что входит в наши 10%» (7 пунктов, см. STATE.md Phase 4 done) — переписать в тёмную типографику. Каждый пункт «что входит» — карточка `border border-white/10 bg-white/[0.03] rounded-2xl p-6` с h3 text-white и текстом text-white/65.
       - Сохранить структуру контента (текст не менять), поменять только обёртки/цвета.
       - В конце — ContactBlock.
       - Проверить, что текст «Подрядчики идут в смете отдельной строкой» и «10% за организационную работу» — сохранены дословно (это контент-ядро страницы).
  </action>
  <verify>
    <automated>grep -q "DarkLayout" src/pages/services/index.astro && grep -q "DarkLayout" src/pages/pricing.astro && ! grep -q "BaseLayout" src/pages/services/index.astro && ! grep -q "BaseLayout" src/pages/pricing.astro && grep -q "MagicCard" src/pages/services/index.astro</automated>
  </verify>
  <acceptance_criteria>
    - /services/ показывает 6 карточек через MagicCard с курсорным глоу, ссылается на /services/{slug}/
    - /pricing/ показывает блок про 10%-модель и 7 пунктов «что входит» в тёмной палитре
    - На обеих страницах: хлебные крошки сверху, ContactBlock снизу, фон тёмный, шрифт Jost
    - SEO-разметка (Seo, JsonLdGraph) присутствует
  </acceptance_criteria>
  <done>Витрина услуг и страница цен — тёмные.</done>
</task>

<task type="auto">
  <name>Task 3: 6 подстраниц /services/[slug]/ — тёмная типографика MDX</name>
  <files>src/pages/services/[slug].astro</files>
  <read_first>
    - Текущий src/pages/services/[slug].astro целиком (динамический шаблон через getStaticPaths)
    - Один из MDX-файлов в src/content/services/ (например, через `ls src/content/services/` и `head -50` первого файла) — посмотреть какие H2, H3, списки, цитаты встречаются в контенте, чтобы предусмотреть стили
    - src/components/dark/Spotlight.astro (опционально, для hero подстраниц)
  </read_first>
  <action>
    1. Заменить `BaseLayout` → `DarkLayout`.
    2. Структура подстраницы:
       ```astro
       <DarkLayout title={seoTitle} description={seoDescription} noindex>
         <Seo slot="seo" .../>
         <JsonLdGraph slot="seo" />
         <JsonLdService slot="seo" service={service.data} />

         <!-- Hero подстраницы -->
         <section class="relative overflow-hidden px-6 pt-32 pb-14 md:px-12 md:pt-40 md:pb-20">
           <div class="lab-aurora pointer-events-none"></div>
           <div class="relative mx-auto max-w-4xl">
             <Breadcrumbs crumbs={crumbs} />
             <h1 class="mt-6 text-4xl font-semibold tracking-tight text-white md:text-6xl">
               {title}
             </h1>
             <span class="mt-4 inline-block h-1 w-12 bg-[#FFF200]"></span>
             <p class="mt-6 text-white/65 md:text-lg leading-relaxed">
               {description}
             </p>
           </div>
         </section>

         <div class="lab-divider-glow mx-auto max-w-4xl"></div>

         <!-- MDX-контент -->
         <section class="relative px-6 py-14 md:px-12 md:py-20">
           <article class="mx-auto max-w-3xl text-white/80
             [&_h2]:mt-12 [&_h2]:text-2xl [&_h2]:font-semibold [&_h2]:text-white [&_h2]:tracking-tight md:[&_h2]:text-3xl
             [&_h3]:mt-8 [&_h3]:text-xl [&_h3]:font-semibold [&_h3]:text-white
             [&_p]:mt-5 [&_p]:leading-relaxed md:[&_p]:text-lg
             [&_ul]:mt-5 [&_ul]:space-y-2 [&_ul>li]:pl-5 [&_ul>li]:relative
             [&_ul>li]:before:absolute [&_ul>li]:before:left-0 [&_ul>li]:before:top-3 [&_ul>li]:before:h-1.5 [&_ul>li]:before:w-1.5 [&_ul>li]:before:rounded-full [&_ul>li]:before:bg-[#FFF200]
             [&_strong]:text-white
             [&_a]:text-[#FFF200] [&_a]:underline [&_a]:underline-offset-4 [&_a]:decoration-[#FFF200]/40 hover:[&_a]:decoration-[#FFF200]">
             <Content />
           </article>
         </section>

         <div class="lab-divider-glow mx-auto max-w-6xl"></div>

         <PricingTeaser />

         <div class="lab-divider-glow mx-auto max-w-6xl"></div>

         <ContactBlock />

         <MetrikaCounter slot="analytics" />
       </DarkLayout>
       ```

    3. **Важно:** не использовать Tailwind plugin `@tailwindcss/typography` (если не подключён), а описать стили вручную через arbitrary-селекторы `[&_h2]`. Это работает в Tailwind v4 и не требует плагина.

    4. JsonLdService импорт и слот — сохранить (это критично для SEO Service-schema).

    5. После реализации запустить `npm run dev`, открыть все 6 страниц `/services/{slug}/` (slug-и есть в `ls src/content/services/`):
       - corporate-parties
       - business-events
       - client-events
       - teambuilding
       - coordination
       - private
       Каждая должна: загружаться, иметь читаемый текст на тёмном, breadcrumbs, H1 жёлтое подчёркивание, MDX-контент со списками-маркерами `#FFF200`, ссылки жёлтые, в конце pricing-teaser + contact-block.

    6. `npm run build` — должен пройти, должны сгенерироваться 6 HTML-файлов в `dist/services/`.
  </action>
  <verify>
    <automated>grep -q "DarkLayout" src/pages/services/\[slug\].astro && ! grep -q "BaseLayout" src/pages/services/\[slug\].astro && grep -q "JsonLdService" src/pages/services/\[slug\].astro && grep -q "Breadcrumbs" src/pages/services/\[slug\].astro && grep -q "PricingTeaser" src/pages/services/\[slug\].astro && grep -q "Content" src/pages/services/\[slug\].astro && npm run build 2>&1 | tail -5 | grep -E "(Complete|built)"</automated>
  </verify>
  <acceptance_criteria>
    - [slug].astro использует DarkLayout, сохраняет JsonLdService, Breadcrumbs, PricingTeaser, ContactBlock
    - MDX-контент рендерится с тёмной типографикой: H2/H3 белые, текст text-white/80, ссылки [#FFF200], списки с жёлтыми маркерами
    - npm run build создаёт 6 HTML-страниц в dist/services/{slug}/
    - При открытии /services/corporate-parties/ виден H1 «Организация корпоративов» (или какой там реальный заголовок), breadcrumbs, текст ≥800 слов, в конце pricing + contact
  </acceptance_criteria>
  <done>Все 6 подстраниц услуг — в тёмной палитре, SEO сохранено.</done>
</task>

</tasks>

<verification>
- 8 файлов раздела «Услуги» + pricing — в тёмной палитре
- JSON-LD Service + BreadcrumbList сохранены
- npm run build проходит
- Все 6 страниц услуг + витрина + pricing визуально консистентны с главной
</verification>

<success_criteria>
- Все 6 must_haves.truths достижимы
- Раздел «Услуги» готов
- Breadcrumbs готовы для плана 05 (кейсы)
</success_criteria>

<output>
После завершения создать `.planning/phases/05.1-dark-redesign/05.1-04-SUMMARY.md`
</output>
