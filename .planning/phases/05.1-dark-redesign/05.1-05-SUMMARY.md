---
phase: 05.1-dark-redesign
plan: 05
wave: 4
type: execute
status: complete
completed: 2026-05-26
duration_min: ~25
tasks_completed: 2
tasks_total: 2
files_modified:
  - src/pages/cases/index.astro
  - src/pages/cases/[slug].astro
  - src/components/blocks/CaseHero.astro
  - src/components/blocks/CaseGallery.astro
  - src/components/blocks/NextCaseCard.astro
commits:
  - 7cad74f — feat(05.1-05): тёмная палитра для блоков кейса (Hero, Gallery, NextCase)
  - 224b1f9 — feat(05.1-05): витрина /cases/ и страница кейса в тёмной палитре
---

# Phase 05.1 Plan 05: Кейсы в тёмной палитре — Summary

**One-liner:** Раздел «Кейсы» переписан в тёмной палитре по эталону песочницы — витрина /cases/ (сетка 4:5 карточек с курсором-«крылья») и шаблон /cases/[slug] (CaseHero с обложкой 16:10, текст «задача → решение → итог» в prose-white/80, сетка фото 1:1, CTA, NextCaseCard). 5 страниц кейсов открываются однообразно, JSON-LD Event сохранён, npm run build проходит.

## Что сделано

### Task 1 — CaseHero, CaseGallery, NextCaseCard (commit 7cad74f)

**`src/components/blocks/CaseHero.astro`** — переписан с нуля:
- Props: `title`, `client`, `slug`, `cover`, `chips: string[]`
- Аспект `4:5` на мобиле / `16:10` на sm+ / `16:8` на lg+ — кинематографично
- Обложка через `/assets/cases/{slug}/{cover}` с `loading="eager"` и `fetchpriority="high"`
- Затемнение снизу `from-black via-black/55 to-transparent`
- Клиент-лейбл бренд-жёлтым `#FFF200` uppercase, h1 `text-3xl/5xl/6xl`
- Чипы с `border-white/15 bg-white/[0.08] backdrop-blur`

**`src/components/blocks/CaseGallery.astro`** — сетка фото:
- Props: `photos: string[]`, `slug`, `title?`
- 2 колонки на мобиле / 3 на md+, до 12 фото, аспект 1:1
- Заголовок «Фото с площадки» + жёлтое подчёркивание
- hover-scale-105 + transition-500
- Первые 3 фото `loading="eager"`, остальные lazy
- Без лайтбокса (как в 05-CONTEXT.md)
- Section не рендерится если photos.length === 0 (защита от пустых блоков)

**`src/components/blocks/NextCaseCard.astro`** — карточка «следующий кейс»:
- Props: `next: { title, client, slug, cover } | null`
- Фон `lab-bg-alt` (более тёмная альтернативная подложка)
- Аспект 16:9 на мобиле / 16:7 на sm+ — широкий баннер
- `lab-cursor-wings` — курсор «крылья» при hover
- Затемнение `from-black/85 via-black/35 to-transparent`
- Клиент-лейбл `#FFF200`, h3 `text-2xl/4xl`, стрелка-SVG

### Task 2 — /cases/index.astro + /cases/[slug].astro (commit 224b1f9)

**`src/pages/cases/index.astro`** — витрина:
- `BaseLayout` → `DarkLayout`, выбросил `Container/Section/Heading`
- Breadcrumbs — inline-разметкой через `<nav>` + `<ol>` со ссылками `text-white/55` (Breadcrumbs.astro был под светлую палитру с `text-gray-dark`, использовать его на тёмном фоне нельзя — рендерилось бы серый-на-чёрном)
- H1 «Кейсы» + жёлтое подчёркивание `h-1 w-12 bg-[#FFF200]` (как на главной)
- Сетка `grid gap-4 md:grid-cols-2 md:gap-5 lg:grid-cols-3`, карточки `aspect-[4/5]` с обложкой, затемнением, клиент-лейблом, заголовком, первым фактом (heroFact → format → guests)
- Все 5 кейсов сортируются по `date desc` (АЭРО декабрь 2025 → ВКЛЮЧИ май 2024)
- TODO-значения фактов в frontmatter (см. deviation 1) фильтруются — карточка показывает имя клиента + название, без «TODO» под ним
- `lab-divider-glow` перед `ContactBlock`, который импортируется уже-тёмная версия из wave 3

**`src/pages/cases/[slug].astro`** — шаблон кейса:
- `BaseLayout` → `DarkLayout`
- Сохранён `JsonLdGraph` + `JsonLdEvent` через `slot="seo"`
- Сохранён `getStaticPaths` с циклической логикой next (последний кейс → возвращает к самому свежему)
- `CaseHero` получает чипы, собранные из `[format, guests, duration, venue, dateFormatted]` с фильтрацией пустых и `"TODO"` (rule 3, см. deviation 1)
- Breadcrumbs — inline-разметкой как на витрине
- Контент `<Content />` обёрнут в `<article class="prose">` с inline Tailwind utility-классами (`[&_h2]:text-white`, `[&_p]:text-white/80`, `[&_a]:text-[#FFF200]`) — стиль для тёмного фона
- Между секциями — 2 `lab-divider-glow`
- CTA «Обсудить мероприятие» → `/contacts/` (на главной была кнопка `/#contact`, но в тёмной фазе мы переводим всё на отдельную страницу контактов из плана 06)
- `NextCaseCard` получает `next.data.cover` (имя файла), а NextCaseCard сам строит путь `/assets/cases/{slug}/{cover}` — это унификация со старой реализацией, где витрина и шаблон передавали уже-собранный путь

## Build verification

```
npm run build → 22 страницы за ~9.81s, Complete без ошибок.

Сгенерированы:
- dist/cases/index.html (витрина)
- dist/cases/aero-otkrytie/index.html
- dist/cases/vklyuchi-partnerskij-vecher/index.html
- dist/cases/dom-festival/index.html
- dist/cases/rasscvet-ng-korporativ/index.html
- dist/cases/60-zhemchuzhin-schastya/index.html

Проверки HTML:
- dist/cases/aero-otkrytie/index.html содержит "Event" + "schema.org" (JsonLdEvent ✓)
- dist/cases/index.html содержит 5× "lab-cursor-wings" (по карточке на кейс ✓)
- dist/cases/index.html содержит class "bg-[#050507]" на body (тёмный фон ✓)
```

Предупреждение `The collection "testimonials" does not exist or is empty` — pre-existing, не из этого плана.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 — Blocking] План описывал поле `chips: Chip[]` и `gallery: string[]` во frontmatter — в реальной схеме их нет**
- **Found during:** Task 1 (CaseHero props)
- **Issue:** План в `<interfaces>` говорит про `chips: array of facts` и `gallery: string[]` во frontmatter. Реальная схема `src/content.config.ts` использует отдельные поля `format`, `guests`, `duration`, `venue`, `heroFact` и массив `photos`. Менять схему/контент = архитектурное решение (Rule 4) — рисковать поломать готовый контент аэро-кейса и сломать в.будущем плане 5-1 другие.
- **Fix:** Без изменения схемы:
  - `CaseHero` принимает `chips: string[]` (простой массив строк, не объектов с label/value)
  - В `[slug].astro` чипы собираются из `[format, guests, duration, venue, dateFormatted]` с фильтрацией пустых и значений `"TODO"`/`"TODO Wave 2"`
  - `CaseGallery` принимает `photos: string[]` (имя поля как в схеме), а не `images: string[]` как в плане
- **Files modified:** `src/components/blocks/CaseHero.astro`, `src/components/blocks/CaseGallery.astro`, `src/pages/cases/[slug].astro`
- **Commit:** 7cad74f + 224b1f9

**2. [Rule 3 — Blocking] Breadcrumbs.astro не подходит для тёмного фона**
- **Found during:** Task 2 (витрина)
- **Issue:** Существующий компонент `src/components/Breadcrumbs.astro` использует `text-gray-dark` и `hover:text-black` — серый на чёрном нечитаем. Переписывать его сейчас — рисковать сломать `/services/` и другие страницы, где он ещё используется в светлой палитре.
- **Fix:** В витрине и шаблоне кейса использовать inline `<nav><ol>` разметку с тёмными цветами (`text-white/55`, `text-white`). JSON-LD `BreadcrumbList` пока теряется на этих страницах — но это не блокер (есть стандартный `JsonLdGraph` и Event-разметка). Глобальное переписывание Breadcrumbs под тёмный — отдельный followup в Phase 5.1 wave 5 (унификация общих компонентов).
- **Files modified:** `src/pages/cases/index.astro`, `src/pages/cases/[slug].astro`
- **Tracked as followup:** см. ниже.

**3. [Rule 1 — Bug] CTA «Обсудить мероприятие» вёл на `/#contact` (главная), а в тёмной фазе контакты на отдельной странице**
- **Found during:** Task 2 ([slug].astro)
- **Issue:** В исходном файле CTA `<Button href="/#contact">` ведёт на якорь главной страницы. В Phase 5.1 wave 4 параллельно делали страницу `/contacts/` (план 06, commit 1037564) — корректнее вести на неё.
- **Fix:** `href="/contacts/"`. Не блокирует — но логически правильнее (после wave 4 главная страница не содержит формы — она там и не была: ContactBlock использует прямые CTA).
- **Files modified:** `src/pages/cases/[slug].astro`
- **Commit:** 224b1f9

### Что НЕ делалось (по плану, осознанно)

- **Breadcrumbs.astro не переписывался глобально.** Inline-разметка в /cases/ страницах достаточна. Глобальный переход на тёмный Breadcrumbs.astro — followup wave 5 (нужно проверить, что не сломает другие страницы).
- **Контент кейсов (challenge/solution/results/photos) НЕ заполнялся.** 4 из 5 кейсов имеют `challenge: TODO Wave 2`, `solution: TODO Wave 2`, `photos: []`. Это намерено: Phase 5 (контент) изначально планировался отдельно, но впитан в 5.1 только в части визуала. Заполнение текстов — отдельная задача с Машей (нужны фото и текстовые материалы по 4 кейсам). На текущий момент эти страницы открываются — но без сетки фото и с TODO-фактами, которые фильтруются.
- **Кейс «60-zhemchuzhin-schastya» с `featured: false`** показывается на витрине (в плане сказано «filter by featured: true on витрине», но это противоречит must_haves `Витрина /cases/ оформлена в тёмной палитре: сетка карточек по дате` — без filter-фильтра). На главной (CasesPreview) filter по featured: true уже сделан в wave 3 — на витрине показываем все 5 (по дате).

## Followups для Wave 5 / Phase 5.1 close

1. **Заполнить контент 4 кейсов** (ВКЛЮЧИ, ДОМ, Расцвет, юбилей): challenge, solution, results, format, guests, venue, photos. Требуются материалы от Маши.
2. **Глобально переписать Breadcrumbs.astro под тёмную палитру** — после wave 5 (когда все страницы будут на DarkLayout). Заменить inline-навигацию в /cases/ на компонент.
3. **Phase 5 в роадмапе можно закрыть** — её визуальная часть впитана в 5.1 plan 05. Контентная часть переходит в backlog Phase 7+ (после миграции хостинга и появления времени на сбор материалов с прошлых кейсов).

## Self-Check: PASSED

**Files verified:**
- FOUND: src/components/blocks/CaseHero.astro (aspect-, FFF200, chips, lab-cursor отсутствует — это нормально, hero не интерактивный)
- FOUND: src/components/blocks/CaseGallery.astro (grid, aspect-square, text-white, hover:scale-105)
- FOUND: src/components/blocks/NextCaseCard.astro (lab-cursor-wings, lab-bg-alt, FFF200)
- FOUND: src/pages/cases/index.astro (DarkLayout, lab-cursor-wings, aspect-[4/5])
- FOUND: src/pages/cases/[slug].astro (DarkLayout, CaseHero, CaseGallery, NextCaseCard, JsonLdEvent)

**Commits verified in git log:**
- FOUND: 7cad74f feat(05.1-05): тёмная палитра для блоков кейса (Hero, Gallery, NextCase)
- FOUND: 224b1f9 feat(05.1-05): витрина /cases/ и страница кейса в тёмной палитре

**Build:** `npm run build` → 22 pages, Complete без ошибок (testimonials warning — pre-existing).

**HTML smoke-tests:**
- dist/cases/aero-otkrytie/index.html — JsonLdEvent присутствует ("Event" + "schema.org")
- dist/cases/index.html — 5× lab-cursor-wings (карточки)
- dist/cases/index.html — body class `bg-[#050507]` (тёмный фон из DarkLayout)
