---
phase: 05.1-dark-redesign
plan: 03
type: execute
wave: 3
depends_on: ["05.1-02"]
files_modified:
  - src/pages/index.astro
  - src/components/blocks/Hero.astro
  - src/components/blocks/Audience.astro
  - src/components/blocks/WhyTurnkey.astro
  - src/components/blocks/LocalExpertise.astro
  - src/components/blocks/ServicesPreview.astro
  - src/components/blocks/CasesPreview.astro
  - src/components/blocks/HowWeWork.astro
  - src/components/blocks/ContactBlock.astro
autonomous: false
requirements: []
must_haves:
  truths:
    - "На крылья.life (превью) главная страница `/` визуально идентична песочнице `/design-lab/`: тёмный фон, hero со spotlight+meteors, 6 одинаковых тёмных секций с жёлтыми разделителями-свечениями, силуэт города в Local Expertise, тёмный подвал"
    - "Главная использует DarkLayout (а не BaseLayout) — переключение оформлено правильно: SEO, JSON-LD, MetrikaCounter работают как раньше"
    - "Контент главной не изменился: те же заголовки, абзацы, факты, кнопки. Изменилась только подача (вёрстка/цвета/шрифты)"
    - "Маша лично подтвердила на десктопе + мобиле: «главная выглядит так, как должна — переноси на остальные страницы»"
    - "На главной работает форма-контакт (фолбэк ContactForm.astro): кнопки телефон+Telegram доступны и стилизованы под тёмную тему"
  artifacts:
    - path: src/pages/index.astro
      provides: "Главная на DarkLayout"
      contains: "DarkLayout"
    - path: src/components/blocks/Hero.astro
      provides: "Hero в тёмной палитре (адаптер из design-lab)"
      contains: "lab-gradient-text"
    - path: src/components/blocks/Audience.astro
      provides: "Аудитории через MagicCard"
      contains: "MagicCard"
    - path: src/components/blocks/LocalExpertise.astro
      provides: "Локальная экспертиза с силуэтом города"
      contains: "KaliningradSilhouette"
    - path: src/components/blocks/HowWeWork.astro
      provides: "Шаги с акцентным День Х"
      contains: "День Х"
  key_links:
    - from: src/pages/index.astro
      to: src/layouts/DarkLayout.astro
      via: "import"
      pattern: "DarkLayout"
    - from: src/components/blocks/Audience.astro
      to: src/components/dark/MagicCard.astro
      via: "import"
      pattern: "dark/MagicCard"
    - from: src/components/blocks/LocalExpertise.astro
      to: src/components/dark/KaliningradSilhouette.astro
      via: "import"
      pattern: "dark/KaliningradSilhouette"
    - from: src/components/blocks/Hero.astro
      to: src/components/dark/Spotlight.astro + Meteors.astro
      via: "import"
      pattern: "dark/(Spotlight|Meteors)"
---

<objective>
Переписать главную страницу основного сайта в тёмной палитре. Это **vertical slice** фазы 5.1 — единственная страница, через которую мы прогоняем все компоненты, эффекты и стилевые решения. Если на главной всё легло корректно — Wave 4 (остальные страницы) пойдёт быстро по утверждённой палитре. Если найдены проблемы — фиксим здесь, на одной странице, а не на восьми сразу.

**Purpose:** утвердить визуальный язык на одной показательной странице, оставив контент нетронутым (те же заголовки, абзацы, факты, ссылки на услуги и кейсы из коллекций). SEO-разметка, JSON-LD, Метрика — должны работать как раньше.

**Output:** главная `/` в тёмной палитре, утверждённая Машей на десктопе и мобиле. Sсе 8 блоков (Hero, Audience, WhyTurnkey, LocalExpertise, ServicesPreview, CasesPreview, HowWeWork, ContactBlock) переписаны как тонкие адаптеры над компонентами из `src/components/dark/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.1-dark-redesign/05.1-CONTEXT.md
@.planning/phases/05.1-dark-redesign/05.1-02-SUMMARY.md
@src/pages/design-lab/index.astro
@src/pages/index.astro
@src/layouts/DarkLayout.astro
@src/components/dark/Spotlight.astro
@src/components/dark/Meteors.astro
@src/components/dark/BorderBeam.astro
@src/components/dark/MagicCard.astro
@src/components/dark/KaliningradSilhouette.astro

<interfaces>
<!-- Текущие импорты в src/pages/index.astro: BaseLayout, Seo, JsonLdGraph, MetrikaCounter,
     и 8 блоков из src/components/blocks/. После плана: BaseLayout → DarkLayout, блоки
     остаются с теми же именами файлов, но переписаны под тёмную палитру. -->

Текущий шаблон index.astro:
```astro
<BaseLayout title={title} description={description} noindex>
  <Seo slot="seo" title={title} description={description} pathname="/" />
  <JsonLdGraph slot="seo" />
  <Hero />
  <Audience />
  <WhyTurnkey />
  <LocalExpertise />
  <ServicesPreview />
  <CasesPreview />
  <HowWeWork />
  <ContactBlock />
  <MetrikaCounter slot="analytics" />
</BaseLayout>
```

Новый шаблон (план):
```astro
<DarkLayout title={title} description={description} noindex>
  <Seo slot="seo" title={title} description={description} pathname="/" />
  <JsonLdGraph slot="seo" />
  <Hero />
  <div class="lab-divider-glow mx-auto max-w-6xl"></div>
  <Audience />
  <div class="lab-divider-glow mx-auto max-w-6xl"></div>
  <WhyTurnkey />
  <div class="lab-divider-glow mx-auto max-w-6xl"></div>
  <ServicesPreview />
  <div class="lab-divider-glow mx-auto max-w-6xl"></div>
  <LocalExpertise />
  <div class="lab-divider-glow mx-auto max-w-6xl"></div>
  <CasesPreview />
  <div class="lab-divider-glow mx-auto max-w-6xl"></div>
  <HowWeWork />
  <div class="lab-divider-glow mx-auto max-w-6xl"></div>
  <ContactBlock />
  <MetrikaCounter slot="analytics" />
</DarkLayout>
```

Сигнатуры блоков (все 8) — без props, читают данные из коллекций или business config.
Зависимости от Content Collections:
- ServicesPreview: getCollection("services"), filter (top 3 by order)
- CasesPreview: getCollection("cases"), filter featured, sort by date desc, slice 3

Контент из песочницы (массивы audiences, whyPoints, localMetrics, steps) — переезжает обратно в src/components/blocks/, чтобы блоки оставались полностью самодостаточными.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Переписать Hero, Audience, WhyTurnkey, ServicesPreview</name>
  <files>src/components/blocks/Hero.astro, src/components/blocks/Audience.astro, src/components/blocks/WhyTurnkey.astro, src/components/blocks/ServicesPreview.astro</files>
  <read_first>
    - src/pages/design-lab/index.astro строки 76-172 (Hero), 178-197 (Audience), 202-226 (WhyTurnkey), 231-271 (Services) — исходник разметки
    - Текущие src/components/blocks/Hero.astro, Audience.astro, WhyTurnkey.astro, ServicesPreview.astro — что мы перезаписываем (старая светлая вёрстка)
    - src/components/dark/Spotlight.astro, Meteors.astro, MagicCard.astro, BorderBeam.astro — что используем
    - src/config/business.ts — phone, phoneDisplay, telegramPersonal
  </read_first>
  <action>
    Каждый блок — самодостаточный Astro-компонент, без props, читает свои данные сам.

    1. **src/components/blocks/Hero.astro** — переписать полностью на основе design-lab строки 76-172, но БЕЗ внутренней `<header>` (шапка отдельно — в DarkHeader). То есть берём только `<section>` с Spotlight, Meteors, Aurora, Grid, контентом hero (бэйдж, h1, p, кнопки CTA, список фактов).
       - Импорты: `import Spotlight from "../dark/Spotlight.astro"`, `import Meteors from "../dark/Meteors.astro"`, `import { business } from "../../config/business.ts"`.
       - Структура:
         ```astro
         <section class="relative flex min-h-[80vh] w-full items-center overflow-hidden">
           <Spotlight class="-top-40 left-0 md:-top-20 md:left-60" fill="#FFF200" />
           <div class="lab-aurora pointer-events-none"></div>
           <div class="lab-grid absolute inset-0"></div>
           <div class="relative z-10 mx-auto w-full max-w-6xl px-6 pt-20 md:px-12 md:pt-24">
             <!-- бэйдж «Команда организаторов · Калининград» -->
             <!-- h1 с lab-gradient-text на «под ключ» -->
             <!-- описание -->
             <!-- 2 жёлтые кнопки tel: + Telegram -->
             <!-- список фактов в grid-cols-2 md:flex -->
           </div>
           <div class="pointer-events-none absolute inset-0 overflow-hidden">
             <Meteors number={14} />
           </div>
         </section>
         ```
       - min-h: уменьшаем с `92vh` (как в песочнице) до `80vh` — на основной сайте шапка уже отдельно сверху, не вылазит за hero.
       - Текст h1: «Организуем мероприятия под ключ» — оставляем как в текущем Hero.astro.
       - Подпись: «Мы отвечаем за концепцию...» — копируем как в текущем Hero.astro.
       - Факты: «Полный цикл организации», «Прозрачная смета», «Команда в день события».

    2. **src/components/blocks/Audience.astro** — переписать на основе design-lab строки 178-197.
       - Импорт `MagicCard`.
       - Массив `audiences` (3 элемента: «Бизнесу с публичным лицом», «Крупному бизнесу», «Иногородним клиентам») — копируем как в design-lab.
       - Структура: `<section id="audience" class="lab-glow-top relative px-6 py-14 md:px-12 md:py-20">` с заголовком «Для кого мы работаем», подчёркивание `bg-[#FFF200] h-1 w-12`, и сеткой `grid gap-5 md:grid-cols-3` где каждая карточка — `<MagicCard class="h-full p-6">`.

    3. **src/components/blocks/WhyTurnkey.astro** — переписать на основе design-lab строки 202-226.
       - Импорт `BorderBeam`.
       - Массив `whyPoints` (3 элемента про «Одна команда», «Прозрачная смета», «Команда в день события») — точные тексты из design-lab копируем.
       - Секция `<section class="lab-bg-alt relative px-6 py-14 md:px-12 md:py-20">`.
       - Каждая карточка: `<div class="relative overflow-hidden rounded-2xl border border-white/10 bg-white/[0.02] p-6"><BorderBeam duration={14} colorFrom="#FFF200" colorTo="#FFA500" />...</div>`.

    4. **src/components/blocks/ServicesPreview.astro** — переписать на основе design-lab строки 231-271.
       - Импорт `MagicCard`, `getCollection`.
       - `const services = (await getCollection("services")).sort((a, b) => a.data.order - b.data.order).slice(0, 3);`
       - Секция с заголовком «Чем занимаемся», подзаголовок, кнопка «Все направления» → /services/.
       - Сетка 3 карточки `<MagicCard href={`/services/${s.data.slug}/`} class="flex min-h-[200px] flex-col justify-between p-6">` с title, description и подвальной стрелкой «Подробнее».

    5. После каждого блока запустить `npm run dev` и открыть `/` (но index.astro ещё на BaseLayout, поэтому пока сломан стилистически — это ОК, главная переключится в Task 3). Можно временно проверить через тестовую страницу `/test-dark/`, добавив туда `import Hero from "../components/blocks/Hero.astro"; <Hero />`.
  </action>
  <verify>
    <automated>grep -q "Spotlight" src/components/blocks/Hero.astro && grep -q "Meteors" src/components/blocks/Hero.astro && grep -q "MagicCard" src/components/blocks/Audience.astro && grep -q "BorderBeam" src/components/blocks/WhyTurnkey.astro && grep -q "MagicCard" src/components/blocks/ServicesPreview.astro && grep -q "dark/" src/components/blocks/Hero.astro</automated>
  </verify>
  <acceptance_criteria>
    - Hero.astro импортирует dark/Spotlight + dark/Meteors, содержит lab-aurora, lab-grid и lab-gradient-text
    - Audience.astro использует MagicCard, на странице 3 карточки с курсорным глоу
    - WhyTurnkey.astro использует BorderBeam с colorFrom="#FFF200"
    - ServicesPreview.astro использует MagicCard с href={`/services/${slug}/`}, читает коллекцию services
    - Все 4 блока — БЕЗ ИМПОРТОВ из старых Container/Section/Heading светлой темы (используем inline-классы Tailwind, как в design-lab)
  </acceptance_criteria>
  <done>4 блока главной переписаны под тёмную тему. Контент сохранён, тексты те же.</done>
</task>

<task type="auto">
  <name>Task 2: Переписать LocalExpertise, CasesPreview, HowWeWork, ContactBlock</name>
  <files>src/components/blocks/LocalExpertise.astro, src/components/blocks/CasesPreview.astro, src/components/blocks/HowWeWork.astro, src/components/blocks/ContactBlock.astro</files>
  <read_first>
    - src/pages/design-lab/index.astro строки 276-317 (LocalExpertise с силуэтом), 322-370 (Cases с обложками), 375-426 (HowWeWork с шагами и «День Х»), 432-488 (Contact)
    - Текущие src/components/blocks/LocalExpertise.astro, CasesPreview.astro, HowWeWork.astro, ContactBlock.astro
    - src/components/dark/KaliningradSilhouette.astro
    - src/components/ContactForm.astro — фолбэк формы (фаза переезда хостинга). На тёмном фоне нужно адаптировать стили — таск 2 НЕ трогает ContactForm, только переписывает ContactBlock как обёртку
    - src/content/cases/ — 5 кейсов, у каждого `cover` (имя файла) и `slug` в data
  </read_first>
  <action>

    1. **src/components/blocks/LocalExpertise.astro** — переписать на основе design-lab строки 276-317.
       - Импорт `KaliningradSilhouette` из `../dark/KaliningradSilhouette.astro`.
       - Массив `localMetrics` (3 элемента: «Локально», «Площадки», «Команда») — копируем тексты.
       - Структура: `<section class="lab-bg-feature relative overflow-hidden px-6 py-14 md:px-12 md:py-20">` → внутри `<KaliningradSilhouette />` → `<div class="relative mx-auto max-w-6xl"><div class="grid gap-12 md:grid-cols-5 md:items-center">...`
       - Левая колонка `md:col-span-3`: бэйдж «Локальная экспертиза» с тонкой жёлтой линией, h2 «Знаем регион — знаем, что здесь работает» с `lab-gradient-text` на «что здесь работает», подчёркивание `h-1 w-12 bg-[#FFF200]`, 2 абзаца текста.
       - Правая колонка `md:col-span-2`: список метрик `<ul class="space-y-3">` с карточками `border-l-2 border-[#FFF200] bg-white/[0.03]`.

    2. **src/components/blocks/CasesPreview.astro** — переписать на основе design-lab строки 322-370.
       - Импорт `getCollection`.
       - `const cases = (await getCollection("cases")).filter(c => c.data.featured).sort((a, b) => b.data.date.getTime() - a.data.date.getTime()).slice(0, 3);`
       - Секция `<section id="cases" class="lab-bg-alt relative px-6 py-14 md:px-12 md:py-20">`, заголовок «Кейсы», подзаголовок, кнопка «Все кейсы» → /cases/.
       - **Особенность:** карточки кейсов — это аспект 1:1 (`aspect-square`), фото с затемнением снизу, текст поверх. Это уже реализовано в текущем `CasesPreview.astro` (после правки в коммите 9935cf2 «карточки кейсов в стиле departament»). Свериться с текущей версией: если она уже использует аналогичный паттерн — оставить логику, поменять только обёртку (`bg-white` → тёмная палитра) и добавить `lab-cursor-wings`.
       - Карточка: `<a href={`/cases/${c.data.slug}/`} class="group lab-cursor-wings relative block aspect-square overflow-hidden rounded-2xl no-underline">` → `<img src={`/assets/cases/${c.data.slug}/${c.data.cover}`} alt={c.data.title} class="absolute inset-0 h-full w-full object-cover transition-transform duration-700 ease-out group-hover:scale-105" loading="lazy" />` → `<div class="absolute inset-0 bg-gradient-to-t from-black/85 via-black/35 to-transparent"></div>` → `<div class="absolute inset-x-0 bottom-0 p-5"><p class="text-[10px] uppercase tracking-[0.18em] text-[#FFF200]">{c.data.client}</p><h3 class="text-base font-semibold text-white">{c.data.title}</h3></div>`.

    3. **src/components/blocks/HowWeWork.astro** — переписать на основе design-lab строки 375-426.
       - Массив `steps` (6 элементов: Бриф, Концепция и смета, Правки, Продакшн, День Х, Отчёт) — копируем тексты.
       - Секция `<section id="how" class="relative px-6 py-14 md:px-12 md:py-20">`.
       - `<ol class="relative mx-auto max-w-3xl">` с вертикальной линией `<div class="absolute left-5 top-5 bottom-5 w-px bg-gradient-to-b from-[#FFF200]/0 via-white/15 to-[#FFF200]/0"></div>`.
       - Каждый шаг — `<li class="relative grid grid-cols-[2.5rem_1fr] gap-3 md:gap-5 pb-10 last:pb-0">`.
       - Маркер шага: круг 40×40 с номером «01», «02»... Если `s.title === "День Х"` — круг жёлтый `bg-[#FFF200] text-black shadow-[0_0_28px_-6px_rgba(255,242,0,0.7)]`; иначе тёмный с жёлтым текстом.
       - Заголовок шага: для «День Х» — в рамке `inline-block rounded-lg border border-[#FFF200]/55 bg-[#FFF200]/[0.06] px-3 py-1 text-[#FFF200]`; иначе — белый.

    4. **src/components/blocks/ContactBlock.astro** — переписать на основе design-lab строки 432-488.
       - Импорт `BorderBeam`, `business`. Импорт `ContactForm` (фолбэк) — оставляем, но обернём в тёмный контейнер. Альтернатива: НЕ использовать ContactForm в этом плане, а реализовать вариант из песочницы (две большие CTA-кнопки tel + Telegram + email). По CONTEXT.md форма временно отключена → используем песочничный вариант, ContactForm.astro не импортируем в этом блоке.
       - Секция `<section id="contact" class="lab-glow-top relative px-6 py-14 md:px-12 md:py-20">`.
       - Сетка `grid gap-12 lg:grid-cols-12`:
         - Левая `lg:col-span-5`: заголовок «Расскажите о мероприятии», подчёркивание, абзац, «Калининград и область».
         - Правая `lg:col-span-7`: карточка `relative overflow-hidden rounded-2xl border border-white/10 bg-white/[0.02] p-8 md:p-10` → `<BorderBeam duration={20} colorFrom="#FFF200" colorTo="#FFD040" />` → две кнопки (tel + Telegram) + email-ссылка + «Обычно отвечаем в течение часа» с пингующей точкой.

    5. **Важно:** временно сохранить старый текстовый контент во всех 4 блоках. После переноса проверить, что тексты совпадают с тем, что было в БЕЗ-тёмной версии (это контент, не дизайн).
  </action>
  <verify>
    <automated>grep -q "KaliningradSilhouette" src/components/blocks/LocalExpertise.astro && grep -q "aspect-square" src/components/blocks/CasesPreview.astro && grep -q "День Х" src/components/blocks/HowWeWork.astro && grep -q "BorderBeam" src/components/blocks/ContactBlock.astro && grep -q "lab-bg-feature" src/components/blocks/LocalExpertise.astro</automated>
  </verify>
  <acceptance_criteria>
    - LocalExpertise импортирует KaliningradSilhouette из dark/, использует lab-bg-feature, содержит 3 метрики
    - CasesPreview использует aspect-square + lab-cursor-wings, читает featured-кейсы (≤3), рендерит обложки из /assets/cases/{slug}/{cover}
    - HowWeWork содержит 6 шагов, у «День Х» жёлтая рамка и жёлтый круг
    - ContactBlock использует BorderBeam с colorFrom="#FFF200", 2 CTA-кнопки (tel + Telegram) и email-ссылку (форма НЕ импортируется)
  </acceptance_criteria>
  <done>Оставшиеся 4 блока главной переписаны. Главная готова к сборке.</done>
</task>

<task type="auto">
  <name>Task 3: Переключить src/pages/index.astro на DarkLayout + добавить разделители</name>
  <files>src/pages/index.astro</files>
  <read_first>
    - Текущий src/pages/index.astro (35 строк)
    - src/layouts/DarkLayout.astro — props (title, description, noindex)
    - Структура из design-lab/index.astro: после каждой секции `<div class="lab-divider-glow mx-auto max-w-6xl"></div>`
  </read_first>
  <action>
    1. Заменить `import BaseLayout from "../layouts/BaseLayout.astro"` → `import DarkLayout from "../layouts/DarkLayout.astro"`.
    2. Заменить `<BaseLayout>` / `</BaseLayout>` → `<DarkLayout>` / `</DarkLayout>`.
    3. После каждой секции (Hero, Audience, WhyTurnkey, ServicesPreview, LocalExpertise, CasesPreview, HowWeWork) вставить `<div class="lab-divider-glow mx-auto max-w-6xl"></div>` — это мерцающий разделитель из песочницы.
    4. **Изменить порядок секций**: в текущем index.astro: Hero → Audience → WhyTurnkey → LocalExpertise → ServicesPreview → CasesPreview → HowWeWork → ContactBlock. В песочнице: Hero → Audience → WhyTurnkey → ServicesPreview → LocalExpertise → CasesPreview → HowWeWork → ContactBlock. Переставляем под порядок песочницы (Services раньше LocalExpertise) — он логичнее: сначала «что делаем», потом «где делаем».
    5. Убрать атрибут `noindex` с DarkLayout? **НЕТ — оставить noindex**, потому что сейчас весь сайт под noindex (см. STATE.md, Phase 7 ещё не пройдена, DNS-переезд не сделан). Снимем noindex только в Phase 7.
    6. SEO + JsonLdGraph + MetrikaCounter — оставить как есть (через слоты `seo` и `analytics`).
    7. Запустить `npm run dev`, открыть `/`. Проверить визуально:
       - Тёмный фон везде
       - Hero с метеорами, spotlight, aurora
       - Все 7 разделителей мерцают между секциями
       - Силуэт города в Local Expertise
       - 3 квадратные карточки кейсов (АЭРО, ВКЛЮЧИ, ДОМ) с обложками
       - Шаги «Как работаем» с акцентным «День Х» в рамке
       - Тёмная шапка + подвал из DarkLayout
    8. Проверить в Safari (если есть локальный) и Chrome.
    9. `npm run build` — должен пройти.
  </action>
  <verify>
    <automated>grep -q "DarkLayout" src/pages/index.astro && ! grep -q "BaseLayout" src/pages/index.astro && grep -c "lab-divider-glow" src/pages/index.astro | grep -E "^[7-9]$|^1[0-9]$" && npm run build 2>&1 | tail -3 | grep -q "Complete\|built" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - src/pages/index.astro использует DarkLayout, НЕ BaseLayout
    - Между секциями ≥7 `<div class="lab-divider-glow ...">` разделителей
    - Порядок секций: Hero, Audience, WhyTurnkey, ServicesPreview, LocalExpertise, CasesPreview, HowWeWork, ContactBlock
    - SEO slot и analytics slot работают: в собранном HTML есть `<link rel="canonical"`, JSON-LD `@graph`, скрипт метрики 99532899
    - npm run build проходит без ошибок
  </acceptance_criteria>
  <done>Главная страница `/` на DarkLayout, визуально равна песочнице.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Маша смотрит главную</name>
  <what-built>
    Главная страница `/` переписана в тёмной палитре по образцу песочницы `/design-lab/`. 8 блоков переехали на vanilla-Astro компоненты из `src/components/dark/`. DarkLayout, DarkHeader, DarkFooter. Контент не менялся — только подача.
  </what-built>
  <how-to-verify>
    1. Запусти `npm run dev -- --host` и открой `http://localhost:4321/` (или сетевой адрес с айфона).
    2. На десктопе сравни рядом две вкладки: `/design-lab/` и `/`. Они должны выглядеть одинаково по структуре. Отличия:
       - На `/` шапка (общая `DarkHeader`) — с реальными ссылками на /services/, /cases/, /about/, /contacts/
       - На `/` подвал — с реквизитами ИП и микроразметкой
       - На `/` нет ссылки «вернуться на основной сайт» в подвале (это только в песочнице)
    3. На айфоне открой `/` — проверь:
       - Лого в шапке не слишком крупное
       - Hero помещается в экран
       - Карточки аудитории идут в столбец
       - Силуэт города не вызывает горизонтальный скролл
       - Карточки кейсов читаемы, текст поверх фото виден
       - Шаги «Как работаем» — нумерация и заголовки не наезжают
       - Контактный блок снизу — две большие жёлтые кнопки тапабельны
    4. Прокрути всю страницу до подвала. В подвале должны быть: лого, реквизиты (ИП Вострикова..., ИНН, ОГРНИП), ссылка «Политика обработки ПДн».
    5. **Контент-проверка** (важно): заголовки и тексты на главной должны быть теми же, что были раньше (до переноса). Не появилось ли «лорем ипсум»? Не пропали ли реальные тексты про «прозрачную смету» или «команду в день события»?
  </how-to-verify>
  <resume-signal>Маша пишет «главная норм, делай остальные» — переход к Wave 4 (планы 04, 05, 06 параллельно). Если что-то не так — описывает проблему, чиним и снова показываем.</resume-signal>
</task>

</tasks>

<verification>
- Главная визуально идентична песочнице по структуре и эффектам
- Контент не изменился
- Маша подтвердила на десктопе и мобиле
- npm run build проходит
- SEO/JsonLd/Метрика работают как раньше
</verification>

<success_criteria>
- Все 5 must_haves.truths достижимы и проверены Машей
- Утверждённый визуальный язык для остальных страниц
- Готово к параллельному переносу остальных 7 страниц в Wave 4
</success_criteria>

<output>
После завершения создать `.planning/phases/05.1-dark-redesign/05.1-03-SUMMARY.md`
</output>
