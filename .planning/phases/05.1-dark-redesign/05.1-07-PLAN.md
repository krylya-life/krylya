---
phase: 05.1-dark-redesign
plan: 07
type: execute
wave: 5
depends_on: ["05.1-04", "05.1-05", "05.1-06"]
files_modified:
  - src/components/Header.astro
  - src/components/Footer.astro
  - src/layouts/BaseLayout.astro
  - src/design-lab/
  - src/pages/design-lab/
  - src/pages/test-design.astro
  - src/pages/test-dark.astro
autonomous: false
requirements: []
must_haves:
  truths:
    - "Старые Header.astro и Footer.astro либо удалены, либо превратились в re-export DarkHeader/DarkFooter — никакая страница больше не использует светлые Header/Footer"
    - "BaseLayout.astro либо удалён, либо превратился в alias DarkLayout — все страницы используют единую тёмную тему"
    - "Песочница /design-lab/ полностью удалена: src/design-lab/, src/pages/design-lab/, и более не существует маршрута /design-lab/"
    - "Тестовые страницы /test-design/ и /test-dark/ удалены — это были временные артефакты разработки"
    - "Все 9 страниц сайта (/, /services/, /services/{6 slug}/, /cases/, /cases/{5 slug}/, /pricing/, /about/, /contacts/, /thanks/, /privacy/) открываются на dev-сервере, не падают, выглядят однообразно в тёмной палитре"
    - "Маша подтвердила финальный обход всех страниц на десктопе + мобиле + Safari"
    - "npm run build генерирует полный sitemap страниц без ошибок"
  artifacts:
    - path: src/components/dark/DarkHeader.astro
      provides: "Единственная шапка сайта"
      contains: "wings-logo-horizontal-white"
    - path: src/components/dark/DarkFooter.astro
      provides: "Единственный подвал сайта"
      contains: "Organization"
    - path: src/layouts/DarkLayout.astro
      provides: "Единственный layout сайта"
      contains: "DarkHeader"
  key_links:
    - from: src/pages/index.astro и все другие страницы
      to: src/layouts/DarkLayout.astro
      via: "import"
      pattern: "DarkLayout"
    - from: src/layouts/DarkLayout.astro
      to: src/components/dark/DarkHeader.astro
      via: "import"
      pattern: "DarkHeader"
---

<objective>
Финальный полишинг фазы 5.1: убрать дубликаты, удалить песочницу, прогнать все страницы и получить подтверждение от Маши, что сайт визуально един и готов к Phase 7 (DNS-переезд и SEO-запуск).

**Purpose:** на этот момент в репозитории сосуществуют: старый Header/Footer/BaseLayout (никем не используемые), новые DarkHeader/DarkFooter/DarkLayout (используются всеми), песочница /design-lab/ и две тестовые страницы. Это мусор, который путает в будущем. Чистим.

**Output:** репозиторий в состоянии «один сайт, одна тема, одна шапка». Удалены ~15 файлов песочницы. Маша подтвердила, что всё на месте.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.1-dark-redesign/05.1-CONTEXT.md
@.planning/phases/05.1-dark-redesign/05.1-03-SUMMARY.md
@.planning/phases/05.1-dark-redesign/05.1-04-SUMMARY.md
@.planning/phases/05.1-dark-redesign/05.1-05-SUMMARY.md
@.planning/phases/05.1-dark-redesign/05.1-06-SUMMARY.md
@src/layouts/BaseLayout.astro
@src/components/Header.astro
@src/components/Footer.astro
@src/layouts/DarkLayout.astro
@src/components/dark/DarkHeader.astro
@src/components/dark/DarkFooter.astro

<interfaces>
<!-- На входе плана:
     - Старые: src/layouts/BaseLayout.astro, src/components/Header.astro, src/components/Footer.astro — ИХ НИКТО НЕ ИМПОРТИРУЕТ (после планов 03-06 все страницы переехали на DarkLayout).
     - Песочница: src/design-lab/ (LabLayout + 6 файлов компонентов + lab.css), src/pages/design-lab/index.astro.
     - Тестовые: src/pages/test-design.astro (тёмный test от Phase 2), src/pages/test-dark.astro (от плана 02).

     На выходе:
     - Удалены: BaseLayout.astro, Header.astro, Footer.astro, вся src/design-lab/, src/pages/design-lab/, обе test-страницы.
     - Остаются: DarkLayout, DarkHeader, DarkFooter — единые для всего сайта.

     ВАЖНО — перед удалением: grep по проекту, убедиться, что никто не импортирует удаляемое.
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Проверить и удалить старые BaseLayout / Header / Footer</name>
  <files>src/layouts/BaseLayout.astro, src/components/Header.astro, src/components/Footer.astro</files>
  <read_first>
    - Прогнать `grep -rn "BaseLayout" src/ --include="*.astro" --include="*.ts"` — должно вернуть пусто после планов 03-06. Если что-то находится — это страница, которую забыли перевести.
    - `grep -rn "from.*components/Header" src/ --include="*.astro"` и `grep -rn "from.*components/Footer" src/ --include="*.astro"` — аналогично, должны быть пустыми (DarkHeader/DarkFooter — это `components/dark/Dark*`, путь другой).
  </read_first>
  <action>
    1. Прогнать три grep-проверки выше. Если есть остаточные импорты:
       - **Не удалять файлы**, а вернуться к соответствующему плану (04/05/06) и заменить там импорт на DarkLayout.
       - После исправления — снова grep, убедиться что пусто.

    2. Если grep чист — удалить три файла:
       - `rm src/layouts/BaseLayout.astro`
       - `rm src/components/Header.astro`
       - `rm src/components/Footer.astro`

    3. Если страницы или компоненты потенциально могут понадобиться позже (например, для светлой админки), а не для публичного сайта — **не удалять**, а переименовать в `BaseLayoutDeprecated.astro` и т.д. Но текущее решение CONTEXT.md: сайт целиком тёмный, светлая версия не нужна → удаляем.

    4. Запустить `npm run build` — должен пройти без ошибок (если что-то импортировало удалённый файл — build упадёт с понятной ошибкой, чиним).
  </action>
  <verify>
    <automated>! test -f src/layouts/BaseLayout.astro && ! test -f src/components/Header.astro && ! test -f src/components/Footer.astro && ! grep -rn "BaseLayout" src/ --include="*.astro" --include="*.ts" && npm run build 2>&1 | tail -5 | grep -E "(Complete|built)"</automated>
  </verify>
  <acceptance_criteria>
    - Файлов src/layouts/BaseLayout.astro, src/components/Header.astro, src/components/Footer.astro нет
    - grep по «BaseLayout» в src/ возвращает 0 совпадений
    - npm run build проходит
  </acceptance_criteria>
  <done>Светлые дубликаты удалены. На сайте — единая тёмная тема.</done>
</task>

<task type="auto">
  <name>Task 2: Удалить песочницу /design-lab/ и тестовые страницы</name>
  <files>src/design-lab/, src/pages/design-lab/, src/pages/test-design.astro, src/pages/test-dark.astro</files>
  <read_first>
    - `grep -rn "design-lab" src/ --include="*.astro" --include="*.ts" --include="*.css"` — найти все упоминания. Внутренние ссылки песочницы на саму себя — это норма, они уйдут вместе с песочницей. Если есть импорт из песочницы в production-код (например, кто-то всё ещё подтягивает KaliningradSilhouette из `src/design-lab/components/`, а не из `src/components/dark/`) — это надо исправить ПЕРЕД удалением.
    - `grep -rn "test-design\|test-dark" src/ --include="*.astro"` — должны быть только сами файлы test-страниц.
  </read_first>
  <action>
    1. Прогнать `grep -rn "from.*src/design-lab\|from.*design-lab/components\|from.*design-lab/styles" src/` (исключая саму папку design-lab). Если что-то находится — заменить на импорт из `src/components/dark/` или из `src/styles/dark.css`.

    2. Удалить папки и файлы:
       - `rm -rf src/design-lab/`
       - `rm -rf src/pages/design-lab/`
       - `rm src/pages/test-design.astro` (если существует)
       - `rm src/pages/test-dark.astro` (если существует)

    3. Файлы `public/cursors/wings-40.png` и `wings-80.png` — НЕ удалять, потому что их использует класс `.lab-cursor-wings` в `src/styles/dark.css`. Они на production-сайте.

    4. Прогнать `npm run build`. Должно пройти. В `dist/` не должно быть папок `design-lab/`, `test-design/`, `test-dark/`.

    5. Прогнать `npm run dev`, открыть `http://localhost:4321/design-lab/` — должна вернуться 404 (Astro: «404 Page Not Found»). Аналогично для `/test-dark/` и `/test-design/`.
  </action>
  <verify>
    <automated>! test -d src/design-lab && ! test -d src/pages/design-lab && ! test -f src/pages/test-design.astro && ! test -f src/pages/test-dark.astro && test -f public/cursors/wings-40.png && npm run build 2>&1 | tail -5 | grep -E "(Complete|built)"</automated>
  </verify>
  <acceptance_criteria>
    - src/design-lab/ удалён
    - src/pages/design-lab/ удалён
    - src/pages/test-*.astro удалены
    - public/cursors/ — на месте (нужны для .lab-cursor-wings)
    - npm run build проходит, dist/design-lab/ не создаётся
    - /design-lab/ возвращает 404 на dev-сервере
  </acceptance_criteria>
  <done>Песочница и тестовые страницы удалены. Репозиторий чист.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: финальный обход всех 9 страниц с Машей</name>
  <what-built>
    Все страницы сайта переведены на тёмную палитру по образцу песочницы. Удалены: старые светлые компоненты, песочница, тестовые страницы. Phase 5 (кейсы) и Phase 6 (О нас / Контакты / Политика) впитаны в 5.1 — отдельных фаз не будет.
  </what-built>
  <how-to-verify>
    1. Запусти `npm run dev -- --host` и открой на айфоне `http://192.168.1.X:4321/`.
    2. Пройди по этим адресам последовательно и для каждой проверь: (а) тёмный фон, (б) шапка с лого и навигацией, (в) подвал с реквизитами, (г) нет ошибок, (д) тексты на месте:

       - `/` — главная
       - `/services/` — витрина 6 услуг
       - `/services/corporate-parties/` — одна из подстраниц (любая)
       - `/services/business-events/` — ещё одна (проверь стили)
       - `/pricing/` — цены и 10%-модель
       - `/cases/` — витрина 5 кейсов
       - `/cases/aero-otkrytie/` — кейс АЭРО
       - `/cases/vklyuchi-partnerskij-vecher/` — кейс ВКЛЮЧИ
       - `/cases/dom-festival/` — кейс ДОМ-фестиваль
       - `/cases/rasscvet-ng-korporativ/` — кейс Расцвет
       - `/cases/60-zhemchuzhin-schastya/` — кейс «60 жемчужин»
       - `/about/` — о нас, команда
       - `/contacts/` — контакты, реквизиты
       - `/thanks/` — спасибо за заявку
       - `/privacy/` — политика обработки ПДн

    3. Открой `/design-lab/` — должна быть 404. Это правильно, песочница удалена.

    4. На десктопе в Safari и Chrome — открой `/` и `/cases/aero-otkrytie/`, проверь что:
       - Анимации работают (метеоры в hero, мерцающие разделители, glow за курсором на карточках)
       - При наведении на карточку — кастомный курсор «Крылья» (если включён в lab-cursor-wings)
       - BorderBeam в контактном блоке бежит по периметру

    5. **Контент-чек**: на каждой странице тексты — те же, что были раньше. Если где-то «исчез» абзац или «появился» лорем-ипсум — записать в комментарий чекпоинта.

    6. Если всё ОК — пишешь «принято, фаза 5.1 готова, можно делать Phase 7». Если нет — описываешь, что не так, мы фиксим.
  </how-to-verify>
  <resume-signal>Маша пишет «принято» — фаза 5.1 закрыта. Дальше — обновление STATE.md и ROADMAP.md (Phase 5 и Phase 6 помечаются как «впитано в 5.1») и переход к Phase 7 (DNS-переезд и SEO-запуск).</resume-signal>
</task>

</tasks>

<verification>
- 9 страниц сайта в тёмной палитре, без светлых артефактов
- /design-lab/ удалена (404 на dev)
- npm run build проходит
- Маша подтвердила финальный обход
- В роадмапе можно закрыть Phase 5, Phase 6 и саму Phase 5.1
</verification>

<success_criteria>
- Сайт визуально един
- Репозиторий чист от песочницы и тестовых артефактов
- Готовность к Phase 7 (DNS-переезд)
</success_criteria>

<output>
После завершения создать `.planning/phases/05.1-dark-redesign/05.1-07-SUMMARY.md`.

Также обновить:
- `.planning/STATE.md`: статус фазы 5.1 → completed, Phase 5 и Phase 6 → впитано в 5.1
- `.planning/ROADMAP.md`: пометить Phase 5 (Кейсы) и Phase 6 (О нас/Контакты/Политика) как «впитано в Phase 5.1»; пометить Phase 5.1 как complete
</output>
