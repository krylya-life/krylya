---
phase: 05.1-dark-redesign
plan: 05
type: execute
wave: 4
depends_on: ["05.1-03"]
files_modified:
  - src/pages/cases/index.astro
  - src/pages/cases/[slug].astro
  - src/components/blocks/CaseHero.astro
  - src/components/blocks/CaseGallery.astro
  - src/components/blocks/NextCaseCard.astro
autonomous: true
requirements: []
must_haves:
  truths:
    - "Витрина /cases/ оформлена в тёмной палитре: сетка карточек по дате, аспект 1:1, фото + затемнение + клиент + название поверх, lab-cursor-wings"
    - "5 страниц кейсов /cases/[slug]/ оформлены в тёмной палитре: hero-обложка с заголовком поверх, чипы фактов, текст «задача → решение → итог», сетка фото 3 колонки, CTA + следующий кейс"
    - "Блоки CaseHero, CaseGallery, NextCaseCard переписаны под тёмную тему — фон, типографика, акценты #FFF200"
    - "JSON-LD Event-schema на каждой странице кейса сохранён; BreadcrumbList работает"
    - "Все 5 кейсов открываются: aero-otkrytie, vklyuchi-partnerskij-vecher, dom-festival, rasscvet-ng-korporativ, 60-zhemchuzhin-schastya. У каждого видна обложка из /assets/cases/{slug}/{cover}"
    - "Грид-сетка фото читается на тёмном фоне: рамки или паддинги между фото не создают «грязи»"
  artifacts:
    - path: src/pages/cases/index.astro
      provides: "Витрина кейсов в тёмной палитре"
      contains: "DarkLayout"
    - path: src/pages/cases/[slug].astro
      provides: "Шаблон страницы кейса в тёмной палитре"
      contains: "DarkLayout"
    - path: src/components/blocks/CaseHero.astro
      provides: "Hero страницы кейса (обложка-фото + заголовок поверх + чипы)"
      contains: "aspect-"
    - path: src/components/blocks/CaseGallery.astro
      provides: "Сетка фото из топ-N изображений"
      contains: "grid"
    - path: src/components/blocks/NextCaseCard.astro
      provides: "Карточка «следующий кейс»"
      contains: "next"
  key_links:
    - from: src/pages/cases/index.astro
      to: src/layouts/DarkLayout.astro
      via: "import"
      pattern: "DarkLayout"
    - from: src/pages/cases/[slug].astro
      to: src/components/blocks/CaseHero.astro
      via: "import"
      pattern: "CaseHero"
    - from: src/pages/cases/[slug].astro
      to: src/components/blocks/CaseGallery.astro
      via: "import"
      pattern: "CaseGallery"
    - from: src/pages/cases/[slug].astro
      to: src/components/JsonLdEvent.astro
      via: "import + slot=seo"
      pattern: "JsonLdEvent"
---

<objective>
Доделать раздел «Кейсы» (Phase 5 в роадмапе) в рамках 5.1 — впитать его согласно решению D-2 в CONTEXT.md. Витрина и 5 страниц кейсов уже частично сделаны (commit 9935cf2 от 16.05), но в светлой/смешанной палитре. Переписываем под тёмную тему.

**Purpose:** кейсы — это «доказательная база» для холодных клиентов из поиска (сегмент C по audience-сегментации). Они должны выглядеть как премиум-портфолио на тёмном фоне (как у departament/Bureau Bessonnitsa), а не как карточки в стиле Tilda. Параллелится с планом 04 (услуги) и планом 06 (about/contacts).

**Output:** 5 файлов: витрина, динамический шаблон, 3 блока кейса. Все 5 кейсов открываются и выглядят однообразно. SEO сохранено.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.1-dark-redesign/05.1-CONTEXT.md
@.planning/phases/05.1-dark-redesign/05.1-03-SUMMARY.md
@.planning/phases/05-cases-portfolio/05-CONTEXT.md
@src/pages/cases/index.astro
@src/pages/cases/[slug].astro
@src/components/blocks/CaseHero.astro
@src/components/blocks/CaseGallery.astro
@src/components/blocks/NextCaseCard.astro
@src/content/cases/aero-otkrytie.md
@src/components/JsonLdEvent.astro
@src/layouts/DarkLayout.astro
@src/components/dark/MagicCard.astro

<interfaces>
<!-- Текущее состояние блоков кейсов (после Phase 5 wave 1):
     CaseHero, CaseGallery, NextCaseCard уже существуют — переписываем под тёмную палитру.

     Структура страницы кейса (из Phase 5 CONTEXT):
     1. Hero — большое фото-обложка, заголовок поверх, чипы фактов (клиент, дата, гости, формат) под H1
     2. Текст «Задача → Решение → Итог», ≥600 слов
     3. Сетка фото 3 колонки, топ-12 на кейс, без лайтбокса
     4. CTA «Обсудить мероприятие» + карточка следующего кейса (NextCaseCard)
-->

Frontmatter в src/content/cases/*.md (из Phase 2 + 5 CONTEXT):
- title, client (строка), slug, date, cover (имя файла обложки)
- chips: array of facts: количество гостей, формат, длительность, площадка
- featured: boolean
- service: reference к /services/{slug}/

Витрина /cases/index.astro (новая):
- DarkLayout title="Кейсы — мероприятия в Калининграде..."
- Breadcrumbs
- H1 «Кейсы», подзаголовок
- Сетка `grid gap-4 md:grid-cols-3` карточек (квадратных или соотношение 4:5):
  каждая = <a class="lab-cursor-wings group relative block aspect-[4/5] overflow-hidden rounded-2xl">
    <img cover />
    <div bg-gradient-to-t from-black/85>...</div>
    <div absolute bottom-0>{client} / {title} / 1 chip фактов</div>
  </a>
- ContactBlock внизу
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CaseHero, CaseGallery, NextCaseCard в тёмной палитре</name>
  <files>src/components/blocks/CaseHero.astro, src/components/blocks/CaseGallery.astro, src/components/blocks/NextCaseCard.astro</files>
  <read_first>
    - Текущие CaseHero.astro (45 строк), CaseGallery.astro (32 строки), NextCaseCard.astro (51 строка) — переписываем
    - 05-CONTEXT.md секция «Hero/Текст/Сетка фото/Конец страницы» — что должно быть в каждом блоке
    - src/content/cases/aero-otkrytie.md — образец frontmatter (cover, chips, client, date, gallery)
  </read_first>
  <action>

    1. **CaseHero.astro** — большое фото-обложка с заголовком поверх и чипами фактов.
       - Props: `caseData` (объект с title, client, cover, slug, chips, date).
       - Структура:
         ```astro
         ---
         interface Chip { label: string; value?: string; }
         interface Props {
           title: string;
           client: string;
           slug: string;
           cover: string;
           chips: Chip[];
         }
         const { title, client, slug, cover, chips } = Astro.props;
         ---
         <section class="relative w-full overflow-hidden">
           <!-- Полноширинная обложка -->
           <div class="relative aspect-[16/10] md:aspect-[16/8] w-full">
             <img
               src={`/assets/cases/${slug}/${cover}`}
               alt={title}
               class="absolute inset-0 h-full w-full object-cover"
               loading="eager"
               fetchpriority="high"
             />
             <!-- Затемнение снизу 60-70% высоты для читаемости текста -->
             <div class="absolute inset-0 bg-gradient-to-t from-black via-black/55 to-transparent"></div>
             <div class="absolute inset-x-0 bottom-0 px-6 pb-10 md:px-12 md:pb-16">
               <div class="mx-auto max-w-6xl">
                 <p class="mb-3 text-xs font-semibold uppercase tracking-[0.25em] text-[#FFF200]">
                   {client}
                 </p>
                 <h1 class="text-3xl font-semibold leading-tight tracking-tight text-white md:text-5xl lg:text-6xl">
                   {title}
                 </h1>
                 <!-- Чипы фактов -->
                 <ul class="mt-6 flex flex-wrap gap-3">
                   {chips.map((c) => (
                     <li class="inline-flex items-center gap-2 rounded-full border border-white/15 bg-white/[0.08] backdrop-blur px-4 py-1.5 text-xs text-white/85">
                       {c.label}{c.value && <><span class="text-white/40">·</span><span class="text-white">{c.value}</span></>}
                     </li>
                   ))}
                 </ul>
               </div>
             </div>
           </div>
         </section>
         ```
       - **Важно:** аспект 16:10 на десктопе, 16:8 на широких экранах — обложка должна быть высокой и кинематографичной, не просто баннер.

    2. **CaseGallery.astro** — сетка фото 3 колонки, топ-12 на кейс, без лайтбокса.
       - Props: `images: string[]` (массив имён файлов), `slug: string`.
       - Структура:
         ```astro
         ---
         interface Props { images: string[]; slug: string; }
         const { images, slug } = Astro.props;
         ---
         <section class="relative px-6 py-14 md:px-12 md:py-20">
           <div class="mx-auto max-w-6xl">
             <h2 class="mb-10 text-2xl font-semibold tracking-tight text-white md:text-3xl">
               Фото с&nbsp;площадки
             </h2>
             <span class="mb-10 inline-block h-1 w-12 bg-[#FFF200]"></span>
             <div class="grid gap-3 grid-cols-2 md:grid-cols-3">
               {images.slice(0, 12).map((img) => (
                 <figure class="relative aspect-square overflow-hidden rounded-xl">
                   <img
                     src={`/assets/cases/${slug}/${img}`}
                     alt=""
                     class="absolute inset-0 h-full w-full object-cover transition-transform duration-500 hover:scale-105"
                     loading="lazy"
                   />
                 </figure>
               ))}
             </div>
           </div>
         </section>
         ```
       - На мобиле — 2 колонки, на md+ — 3. Аспект 1:1, gap 3 (12px).
       - **Без лайтбокса** — это решение из 05-CONTEXT.md, не реализовываем модальное окно.

    3. **NextCaseCard.astro** — карточка «следующий кейс» в подвале страницы кейса.
       - Props: `nextCase` (объект с title, client, slug, cover) — или null если кейсов больше нет.
       - Структура:
         ```astro
         ---
         interface Props {
           nextCase: { title: string; client: string; slug: string; cover: string } | null;
         }
         const { nextCase } = Astro.props;
         ---
         {nextCase && (
           <section class="lab-bg-alt relative px-6 py-14 md:px-12 md:py-20">
             <div class="mx-auto max-w-6xl">
               <p class="mb-4 text-xs font-semibold uppercase tracking-[0.25em] text-white/55">Следующий кейс</p>
               <a
                 href={`/cases/${nextCase.slug}/`}
                 class="group lab-cursor-wings relative block aspect-[16/7] overflow-hidden rounded-2xl no-underline"
               >
                 <img
                   src={`/assets/cases/${nextCase.slug}/${nextCase.cover}`}
                   alt={nextCase.title}
                   class="absolute inset-0 h-full w-full object-cover transition-transform duration-700 group-hover:scale-105"
                   loading="lazy"
                 />
                 <div class="absolute inset-0 bg-gradient-to-t from-black/85 via-black/35 to-transparent"></div>
                 <div class="absolute inset-x-0 bottom-0 p-8 md:p-12">
                   <p class="mb-2 text-xs font-semibold uppercase tracking-[0.25em] text-[#FFF200]">{nextCase.client}</p>
                   <h3 class="text-2xl font-semibold tracking-tight text-white md:text-4xl">{nextCase.title}</h3>
                   <span class="mt-4 inline-flex items-center gap-2 text-sm font-medium text-white">
                     Смотреть кейс
                     <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M5 12h14M13 5l7 7-7 7"/></svg>
                   </span>
                 </div>
               </a>
             </div>
           </section>
         )}
         ```
  </action>
  <verify>
    <automated>grep -q "aspect-" src/components/blocks/CaseHero.astro && grep -q "grid" src/components/blocks/CaseGallery.astro && grep -q "FFF200" src/components/blocks/CaseHero.astro && grep -q "lab-cursor-wings" src/components/blocks/NextCaseCard.astro && grep -q "text-white" src/components/blocks/CaseGallery.astro</automated>
  </verify>
  <acceptance_criteria>
    - CaseHero: полноширинное фото 16:10, текст в нижней трети поверх затемнения, чипы фактов с backdrop-blur
    - CaseGallery: 2 колонки на мобиле / 3 на десктопе, ≤12 фото, hover-scale-105
    - NextCaseCard: широкий баннер 16:7 со следующим кейсом, lab-cursor-wings
    - Все три блока используют #FFF200 для акцентов и text-white для главного текста
  </acceptance_criteria>
  <done>Три ключевых блока кейса в тёмной палитре готовы.</done>
</task>

<task type="auto">
  <name>Task 2: Витрина /cases/ и шаблон /cases/[slug] в тёмной палитре</name>
  <files>src/pages/cases/index.astro, src/pages/cases/[slug].astro</files>
  <read_first>
    - Текущий src/pages/cases/index.astro целиком
    - Текущий src/pages/cases/[slug].astro целиком (логика getStaticPaths, рендер контента, JsonLdEvent, next-case)
    - src/components/blocks/CaseHero, CaseGallery, NextCaseCard — что мы переписали в Task 1
    - src/content/cases/*.md — 5 файлов, чтобы убедиться, что у всех есть поля для chips и gallery
  </read_first>
  <action>

    1. **src/pages/cases/index.astro:**
       - Заменить `BaseLayout` → `DarkLayout`.
       - Hero-секция страницы: тёмная, breadcrumbs, H1 «Кейсы», подчёркивание #FFF200, подзаголовок про портфолио.
       - Сетка кейсов:
         ```astro
         <section class="relative px-6 py-14 md:px-12 md:py-20">
           <div class="mx-auto max-w-6xl">
             <div class="grid gap-5 md:grid-cols-2 lg:grid-cols-3">
               {cases.map((c) => (
                 <a
                   href={`/cases/${c.id}/`}
                   class="group lab-cursor-wings relative block aspect-[4/5] overflow-hidden rounded-2xl no-underline"
                 >
                   <img
                     src={`/assets/cases/${c.id}/${c.data.cover}`}
                     alt={c.data.title}
                     class="absolute inset-0 h-full w-full object-cover transition-transform duration-700 group-hover:scale-105"
                     loading="lazy"
                   />
                   <div class="absolute inset-0 bg-gradient-to-t from-black/90 via-black/40 to-transparent"></div>
                   <div class="absolute inset-x-0 bottom-0 p-6">
                     <p class="mb-2 text-[10px] font-semibold uppercase tracking-[0.2em] text-[#FFF200]">{c.data.client}</p>
                     <h2 class="text-lg font-semibold leading-tight tracking-tight text-white md:text-xl">{c.data.title}</h2>
                     {c.data.chips?.[0] && (
                       <p class="mt-2 text-xs text-white/70">{c.data.chips[0].label}{c.data.chips[0].value && ` · ${c.data.chips[0].value}`}</p>
                     )}
                   </div>
                 </a>
               ))}
             </div>
           </div>
         </section>
         ```
       - Если в frontmatter chips нет — fallback: показать только client + title (защита от падения).
       - ContactBlock в подвале страницы.

    2. **src/pages/cases/[slug].astro:**
       - Заменить `BaseLayout` → `DarkLayout`.
       - Сохранить JsonLdEvent (Event-schema для Яндекса/Google) и Breadcrumbs.
       - Логика «следующего кейса»: после сортировки кейсов по дате `desc`, для текущего находим индекс и берём `cases[currentIndex + 1]` (или `cases[0]` если текущий — последний, циклический возврат к самому свежему).
       - Структура:
         ```astro
         <DarkLayout title={seoTitle} description={seoDescription} noindex>
           <Seo slot="seo" .../>
           <JsonLdGraph slot="seo" />
           <JsonLdEvent slot="seo" caseData={caseData.data} />

           <!-- Breadcrumbs над hero -->
           <div class="absolute top-24 left-0 right-0 z-20 px-6 md:px-12">
             <div class="mx-auto max-w-6xl">
               <Breadcrumbs crumbs={crumbs} />
             </div>
           </div>

           <CaseHero
             title={caseData.data.title}
             client={caseData.data.client}
             slug={caseData.id}
             cover={caseData.data.cover}
             chips={caseData.data.chips || []}
           />

           <!-- Текст: задача → решение → итог -->
           <section class="relative px-6 py-14 md:px-12 md:py-20">
             <article class="mx-auto max-w-3xl text-white/80
               [&_h2]:mt-12 [&_h2]:text-2xl [&_h2]:font-semibold [&_h2]:text-white md:[&_h2]:text-3xl
               [&_h3]:mt-8 [&_h3]:text-xl [&_h3]:font-semibold [&_h3]:text-white
               [&_p]:mt-5 [&_p]:leading-relaxed md:[&_p]:text-lg
               [&_strong]:text-white
               [&_a]:text-[#FFF200] [&_a]:underline [&_a]:underline-offset-4">
               <Content />
             </article>
           </section>

           <div class="lab-divider-glow mx-auto max-w-6xl"></div>

           {/* Галерея, если есть массив gallery в frontmatter */}
           {caseData.data.gallery?.length > 0 && (
             <CaseGallery images={caseData.data.gallery} slug={caseData.id} />
           )}

           <div class="lab-divider-glow mx-auto max-w-6xl"></div>

           <!-- CTA «Обсудить мероприятие» -->
           <section class="relative px-6 py-14 md:px-12 md:py-20">
             <div class="mx-auto max-w-3xl text-center">
               <h2 class="text-2xl font-semibold text-white md:text-4xl">
                 Хотите такое&nbsp;же&nbsp;— или совсем другое?
               </h2>
               <p class="mt-6 text-white/65 md:text-lg">
                 Расскажите о&nbsp;вашем мероприятии — обсудим формат и&nbsp;смету.
               </p>
               <a href="/contacts/" class="mt-8 inline-flex items-center justify-center rounded-full bg-[#FFF200] px-6 py-3.5 text-sm font-semibold text-black no-underline transition-colors hover:bg-[#FFF566]">
                 Обсудить мероприятие
               </a>
             </div>
           </section>

           <NextCaseCard nextCase={nextCase} />

           <MetrikaCounter slot="analytics" />
         </DarkLayout>
         ```

    3. Если в Content Collections схема не поддерживает поле `chips` или `gallery` — это поправится в Phase 5-1 (Phase 5 впитывается). На данном плане: если поля нет — fallback (chips = пустой массив, gallery — пропускаем секцию).

    4. После реализации `npm run dev` и пройти все 5 кейсов:
       - /cases/aero-otkrytie/
       - /cases/vklyuchi-partnerskij-vecher/
       - /cases/dom-festival/
       - /cases/rasscvet-ng-korporativ/
       - /cases/60-zhemchuzhin-schastya/
       Каждая должна показывать: обложку с заголовком поверх, breadcrumbs, текст, фото (если есть в frontmatter), CTA, следующий кейс.

    5. `npm run build` создаёт `dist/cases/{slug}/index.html` для каждого из 5 кейсов + `dist/cases/index.html` для витрины.
  </action>
  <verify>
    <automated>grep -q "DarkLayout" src/pages/cases/index.astro && grep -q "DarkLayout" src/pages/cases/\[slug\].astro && grep -q "CaseHero" src/pages/cases/\[slug\].astro && grep -q "JsonLdEvent" src/pages/cases/\[slug\].astro && grep -q "NextCaseCard" src/pages/cases/\[slug\].astro && npm run build 2>&1 | tail -5 | grep -E "(Complete|built)"</automated>
  </verify>
  <acceptance_criteria>
    - /cases/ показывает 5 карточек кейсов в сетке (1col mob / 2col md / 3col lg) с обложками
    - Каждая карточка ведёт на /cases/{slug}/ и имеет lab-cursor-wings
    - /cases/{slug}/ показывает CaseHero + текст + (опционально) CaseGallery + CTA + NextCaseCard
    - JsonLdEvent работает (slot="seo" → попадает в head)
    - npm run build генерирует HTML для 5 кейсов + витрины
  </acceptance_criteria>
  <done>Раздел «Кейсы» в тёмной палитре. Phase 5 впитан в 5.1.</done>
</task>

</tasks>

<verification>
- Все must_haves.truths достижимы
- 5 кейсов открываются и выглядят однообразно
- JSON-LD Event сохранён
- npm run build проходит
</verification>

<success_criteria>
- Раздел «Кейсы» готов в тёмной палитре
- Phase 5 в роадмапе можно закрыть после успешного завершения 5.1
</success_criteria>

<output>
После завершения создать `.planning/phases/05.1-dark-redesign/05.1-05-SUMMARY.md`
</output>
