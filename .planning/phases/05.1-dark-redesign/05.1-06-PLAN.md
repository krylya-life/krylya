---
phase: 05.1-dark-redesign
plan: 06
type: execute
wave: 4
depends_on: ["05.1-03"]
files_modified:
  - src/pages/about.astro
  - src/pages/contacts.astro
  - src/pages/thanks.astro
  - src/pages/privacy.astro
autonomous: true
requirements: []
must_haves:
  truths:
    - "Страница /about/ существует и оформлена в тёмной палитре: история, миссия, 3 опоры бренда (надёжность → локальная экспертиза → креатив), упоминание команды (Маша + Кристина), ≥600 слов"
    - "Страница /contacts/ существует и оформлена в тёмной палитре: телефон, Telegram, email, реквизиты ИП, упоминание региона; форма временно отключена (показ контактных кнопок согласно STATE.md)"
    - "Страница /thanks/ перенесена в тёмную палитру, кнопка возврата на главную работает"
    - "Страница /privacy/ перенесена в тёмную палитру, текст политики 152-ФЗ читается на тёмном"
    - "Все 4 страницы используют DarkLayout, имеют Breadcrumbs, SEO, MetrikaCounter"
    - "Phase 6 в роадмапе помечается как 'впитано в 5.1' — отдельная Phase 6 не требуется"
  artifacts:
    - path: src/pages/about.astro
      provides: "Страница «О нас» (создаётся с нуля)"
      contains: "DarkLayout"
    - path: src/pages/contacts.astro
      provides: "Страница «Контакты» (создаётся с нуля)"
      contains: "DarkLayout"
    - path: src/pages/thanks.astro
      provides: "Страница «Спасибо» в тёмной палитре"
      contains: "DarkLayout"
    - path: src/pages/privacy.astro
      provides: "Политика обработки ПДн в тёмной палитре"
      contains: "DarkLayout"
  key_links:
    - from: src/pages/contacts.astro
      to: src/config/business.ts
      via: "import"
      pattern: "business"
    - from: src/pages/about.astro
      to: src/layouts/DarkLayout.astro
      via: "import"
      pattern: "DarkLayout"
---

<objective>
Создать недостающие страницы и перевести существующие на тёмную палитру: `/about/`, `/contacts/`, `/thanks/`, `/privacy/`. По CONTEXT.md решено впитать Phase 6 (информационные страницы) в эту фазу — чтобы после 5.1 у сайта был ПОЛНЫЙ комплект тёмных страниц.

**Purpose:** /about/ и /contacts/ — обязательные страницы для коммерческого сайта (упомянуты в footer-ссылках Header, ведут на 404 без них). /thanks/ и /privacy/ уже существуют в светлой палитре — переводим на тёмную. После плана все 9 страниц основного сайта будут в одной палитре, останется только перенос Header/Footer и удаление песочницы (план 07).

**Output:** 4 файла страниц в `src/pages/`. /about/ и /contacts/ — создаются с нуля, /thanks/ и /privacy/ — переписываются. Контент для /about/ — берём из `.business/company/about.md` и `.business/company/team.md` (через `cat`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.1-dark-redesign/05.1-CONTEXT.md
@.planning/phases/05.1-dark-redesign/05.1-03-SUMMARY.md
@krylya/CLAUDE.md
@src/layouts/DarkLayout.astro
@src/pages/thanks.astro
@src/pages/privacy.astro
@src/components/Breadcrumbs.astro
@src/components/blocks/ContactBlock.astro
@src/config/business.ts

<interfaces>
<!-- /about/ и /contacts/ сейчас НЕ существуют (404). Phase 6 их планировала, но впитываем в 5.1.

     Для /about/ контент берётся из:
     - .business/company/about.md (история, миссия)
     - .business/company/values.md (3 опоры: надёжность, локальная экспертиза, креатив)
     - .business/company/team.md (Маша, Кристина и координаторы)

     Решение: контент НЕ читаем через Content Collections (там нет коллекции pages), а пишем inline в .astro как массивы данных. Это согласуется с тем, как сейчас сделана главная (текст массивами в блоках).

     Для /contacts/ контент берётся из business config (phone, email, telegramPersonal, legalName, taxId, ogrnip).
     Я.Карта — добавляется как iframe (упоминается в Phase 6 ROADMAP), но в 5.1 — опционально (не блокер).

     Микроразметка:
     - /about/ — Organization
     - /contacts/ — Organization + PostalAddress + ContactPoint
     - /privacy/ — без особой разметки
     - /thanks/ — noindex (уже было)
-->

DarkLayout props: { title: string; description?: string; noindex?: boolean }
business config (src/config/business.ts) поля: phone, phoneDisplay, email, telegramPersonal, legalName, name, taxId, ogrnip, locality, region, foundingDate, urlCyrillic

Опоры бренда (D-2 в CONTEXT Phase 0 / values.md):
1. Надёжность (партнёр под ключ, всё на нашей стороне)
2. Локальная экспертиза (Калининград + область, проверенные подрядчики)
3. Креатив (вторичен после первых двух, но есть)

Команда (team.md / CLAUDE.md):
- Мария Вострикова (основатель, ИП Вострикова Мария Валерьевна)
- Кристина (со-основатель, координатор)
- ключевые координаторы (имена в team.md)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Создать /about/ в тёмной палитре</name>
  <files>src/pages/about.astro</files>
  <read_first>
    - .business/company/about.md (через `cat /Users/mariiavostrikova/claude/.business/company/about.md`) — история, миссия
    - .business/company/values.md (через `cat ...`) — 3 опоры
    - .business/company/team.md (через `cat ...`) — состав команды, имена, роли
    - src/components/blocks/LocalExpertise.astro (после плана 03) — образец секции с двумя колонками для «опор бренда»
  </read_first>
  <action>
    1. Создать `src/pages/about.astro` с нуля.
    2. Структура:
       ```astro
       ---
       import DarkLayout from "../layouts/DarkLayout.astro";
       import Seo from "../components/Seo.astro";
       import JsonLdGraph from "../components/JsonLdGraph.astro";
       import Breadcrumbs from "../components/Breadcrumbs.astro";
       import MetrikaCounter from "../components/MetrikaCounter.astro";
       import ContactBlock from "../components/blocks/ContactBlock.astro";

       const seoTitle = "О нас — ивент-агентство Крылья в Калининграде";
       const seoDescription = "Команда Крыльев: организуем мероприятия в Калининграде и области. Надёжность, локальная экспертиза, прозрачная 10%-модель. Маша Вострикова и Кристина — основатели агентства.";

       const crumbs = [
         { label: "Главная", href: "/" },
         { label: "О нас", href: "/about/" },
       ];

       const pillars = [
         {
           title: "Надёжность",
           text: "Мы не пропадаем после подписания договора. Подрядчики идут в смете отдельной строкой по реальной цене, мы держим связь с площадкой и командой в день события. У вас один контакт вместо десяти телефонов.",
         },
         {
           title: "Локальная экспертиза",
           text: "Калининград и область — наш единственный регион работы. Знакомы с площадками от исторических особняков и замков до индустриальных лофтов и побережья. Подрядчики проверены десятками проектов.",
         },
         {
           title: "Креатив",
           text: "Концепция — не самоцель, а ответ на задачу клиента. Сначала разбираем бизнес-цель и формат, потом предлагаем идею. Не делаем «вау ради вау» — делаем то, что работает на ваше мероприятие.",
         },
       ];

       const teamHighlights = [
         { name: "Мария Вострикова", role: "Основатель, главный продюсер", bio: "Десять лет в индустрии. Личное руководство ключевыми проектами, общение с клиентами на этапе концепции и брифа." },
         { name: "Кристина", role: "Со-основатель, ведущий координатор", bio: "Отвечает за тайминг, площадки, подрядчиков. На связи с командой и клиентом в день события." },
       ];
       ---

       <DarkLayout title={seoTitle} description={seoDescription} noindex>
         <Seo slot="seo" title={seoTitle} description={seoDescription} pathname="/about/" />
         <JsonLdGraph slot="seo" />

         <!-- Hero -->
         <section class="relative overflow-hidden px-6 pt-32 pb-14 md:px-12 md:pt-40 md:pb-20">
           <div class="lab-aurora pointer-events-none"></div>
           <div class="relative mx-auto max-w-4xl">
             <Breadcrumbs crumbs={crumbs} />
             <h1 class="mt-6 text-4xl font-semibold tracking-tight text-white md:text-6xl">
               Кто мы
             </h1>
             <span class="mt-4 inline-block h-1 w-12 bg-[#FFF200]"></span>
             <p class="mt-8 text-lg leading-relaxed text-white/75 md:text-xl">
               Крылья — ивент-агентство в&nbsp;Калининграде. Работаем с&nbsp;корпоративными клиентами,
               девелоперами и&nbsp;иногородними заказчиками. Организуем мероприятия под&nbsp;ключ —
               от&nbsp;концепции до&nbsp;команды на&nbsp;площадке в&nbsp;день события.
             </p>
           </div>
         </section>

         <div class="lab-divider-glow mx-auto max-w-6xl"></div>

         <!-- 3 опоры бренда -->
         <section class="relative px-6 py-14 md:px-12 md:py-20">
           <div class="mx-auto max-w-6xl">
             <h2 class="text-3xl font-semibold tracking-tight text-white md:text-5xl">На&nbsp;чём стоим</h2>
             <span class="mt-4 inline-block h-1 w-12 bg-[#FFF200]"></span>
             <div class="mt-12 grid gap-5 md:grid-cols-3">
               {pillars.map((p, i) => (
                 <div class="rounded-2xl border border-white/10 bg-white/[0.03] p-6">
                   <p class="mb-4 font-mono text-xs text-[#FFF200]">{String(i + 1).padStart(2, '0')}</p>
                   <h3 class="text-xl font-semibold text-white">{p.title}</h3>
                   <p class="mt-3 text-sm leading-relaxed text-white/65">{p.text}</p>
                 </div>
               ))}
             </div>
           </div>
         </section>

         <div class="lab-divider-glow mx-auto max-w-6xl"></div>

         <!-- Команда -->
         <section class="lab-bg-alt relative px-6 py-14 md:px-12 md:py-20">
           <div class="mx-auto max-w-6xl">
             <h2 class="text-3xl font-semibold tracking-tight text-white md:text-5xl">Команда</h2>
             <span class="mt-4 inline-block h-1 w-12 bg-[#FFF200]"></span>
             <p class="mt-6 max-w-2xl text-white/65 md:text-lg leading-relaxed">
               Основатели агентства лично ведут ключевые проекты. На больших мероприятиях привлекаем проверенных координаторов из нашей команды.
             </p>
             <div class="mt-12 grid gap-6 md:grid-cols-2">
               {teamHighlights.map((m) => (
                 <div class="rounded-2xl border border-white/10 bg-white/[0.02] p-6">
                   <h3 class="text-xl font-semibold text-white">{m.name}</h3>
                   <p class="mt-1 text-sm text-[#FFF200]">{m.role}</p>
                   <p class="mt-4 text-sm leading-relaxed text-white/65">{m.bio}</p>
                 </div>
               ))}
             </div>
           </div>
         </section>

         <div class="lab-divider-glow mx-auto max-w-6xl"></div>

         <ContactBlock />

         <MetrikaCounter slot="analytics" />
       </DarkLayout>
       ```
    3. **Контент-проверка:** перед фиксацией прочитать `.business/company/about.md` и `values.md`. Если там есть факты, которых нет в шаблоне выше (например, год основания, упоминание конкретных кейсов, общее количество проектов) — добавить их в текст hero или в подзаголовки. Тон — «интеллигентный, изящный, без минусов конкурентов» (см. feedback_krylya_tone в памяти).
    4. **Длина:** ≥600 слов суммарно. Если в шаблоне меньше — расширить тексты опор бренда + добавить ещё абзацы в hero и команду.
    5. Проверить `npm run dev` → `/about/` загружается, выглядит как остальные тёмные страницы.
  </action>
  <verify>
    <automated>test -f src/pages/about.astro && grep -q "DarkLayout" src/pages/about.astro && grep -q "Надёжность" src/pages/about.astro && grep -q "Локальная экспертиза" src/pages/about.astro && grep -q "Мария Вострикова" src/pages/about.astro</automated>
  </verify>
  <acceptance_criteria>
    - /about/ открывается на dev-сервере, не падает в 404
    - Видны 3 опоры бренда: надёжность, локальная экспертиза, креатив
    - Видны имена основателей: Мария Вострикова, Кристина
    - Hero, опоры, команда, ContactBlock — каждая секция на своём фоне (тёмный / lab-bg-alt чередуются)
    - Breadcrumbs сверху
  </acceptance_criteria>
  <done>/about/ создан в тёмной палитре, ≥600 слов, факты из .business/.</done>
</task>

<task type="auto">
  <name>Task 2: Создать /contacts/ в тёмной палитре</name>
  <files>src/pages/contacts.astro</files>
  <read_first>
    - src/config/business.ts — точные значения phone, email, telegramPersonal, legalName, taxId, ogrnip
    - src/components/blocks/ContactBlock.astro (тёмный после плана 03) — переиспользуем как основной блок страницы
  </read_first>
  <action>
    1. Создать `src/pages/contacts.astro`:
       ```astro
       ---
       import DarkLayout from "../layouts/DarkLayout.astro";
       import Seo from "../components/Seo.astro";
       import JsonLdGraph from "../components/JsonLdGraph.astro";
       import Breadcrumbs from "../components/Breadcrumbs.astro";
       import MetrikaCounter from "../components/MetrikaCounter.astro";
       import { business } from "../config/business.ts";

       const seoTitle = "Контакты — ивент-агентство Крылья, Калининград";
       const seoDescription = "Свяжитесь с агентством Крылья: телефон, Telegram, email. Организация мероприятий в Калининграде и области. ИП Вострикова Мария Валерьевна.";

       const crumbs = [
         { label: "Главная", href: "/" },
         { label: "Контакты", href: "/contacts/" },
       ];
       ---

       <DarkLayout title={seoTitle} description={seoDescription} noindex>
         <Seo slot="seo" title={seoTitle} description={seoDescription} pathname="/contacts/" />
         <JsonLdGraph slot="seo" />

         <section class="relative overflow-hidden px-6 pt-32 pb-14 md:px-12 md:pt-40 md:pb-20">
           <div class="lab-aurora pointer-events-none"></div>
           <div class="relative mx-auto max-w-4xl">
             <Breadcrumbs crumbs={crumbs} />
             <h1 class="mt-6 text-4xl font-semibold tracking-tight text-white md:text-6xl">
               Контакты
             </h1>
             <span class="mt-4 inline-block h-1 w-12 bg-[#FFF200]"></span>
             <p class="mt-8 text-lg leading-relaxed text-white/75 md:text-xl">
               Позвоните, напишите в&nbsp;Telegram или на&nbsp;почту — обычно отвечаем
               в&nbsp;течение&nbsp;рабочего&nbsp;дня. На&nbsp;этапе брифа разбираем формат,
               бюджет и&nbsp;ограничения; решение по&nbsp;концепции и&nbsp;смете присылаем
               за&nbsp;3–5 рабочих дней.
             </p>
           </div>
         </section>

         <div class="lab-divider-glow mx-auto max-w-6xl"></div>

         <!-- Основные каналы связи -->
         <section
           class="relative px-6 py-14 md:px-12 md:py-20"
           itemscope
           itemtype="https://schema.org/Organization"
         >
           <meta itemprop="name" content={business.name} />
           <meta itemprop="legalName" content={business.legalName} />
           <meta itemprop="taxID" content={business.taxId} />
           <meta itemprop="vatID" content={business.ogrnip} />
           <meta itemprop="url" content={business.urlCyrillic} />

           <div class="mx-auto max-w-6xl">
             <div class="grid gap-5 md:grid-cols-2">
               <a
                 href={`tel:${business.phone}`}
                 class="group flex flex-col gap-3 rounded-2xl border border-white/10 bg-white/[0.03] p-8 no-underline transition-colors hover:border-[#FFF200]/40"
               >
                 <span class="text-xs font-semibold uppercase tracking-[0.25em] text-[#FFF200]">Телефон</span>
                 <span class="text-2xl font-semibold text-white md:text-3xl" itemprop="telephone">{business.phoneDisplay}</span>
                 <span class="text-sm text-white/55">с&nbsp;9:00 до&nbsp;21:00, ежедневно</span>
               </a>
               <a
                 href={`https://t.me/${business.telegramPersonal.replace("@", "")}`}
                 target="_blank"
                 rel="noopener noreferrer"
                 class="group flex flex-col gap-3 rounded-2xl border border-white/10 bg-white/[0.03] p-8 no-underline transition-colors hover:border-[#FFF200]/40"
               >
                 <span class="text-xs font-semibold uppercase tracking-[0.25em] text-[#FFF200]">Telegram</span>
                 <span class="text-2xl font-semibold text-white md:text-3xl">{business.telegramPersonal}</span>
                 <span class="text-sm text-white/55">обычно отвечаем в&nbsp;течение часа</span>
               </a>
             </div>
             <a
               href={`mailto:${business.email}`}
               class="mt-5 flex items-center gap-3 rounded-2xl border border-white/10 bg-white/[0.02] p-6 no-underline transition-colors hover:border-[#FFF200]/30"
             >
               <span class="text-xs font-semibold uppercase tracking-[0.25em] text-[#FFF200]">Email</span>
               <span class="text-lg text-white" itemprop="email">{business.email}</span>
             </a>
           </div>
         </section>

         <div class="lab-divider-glow mx-auto max-w-6xl"></div>

         <!-- Реквизиты + регион -->
         <section
           class="lab-bg-alt relative px-6 py-14 md:px-12 md:py-20"
         >
           <div class="mx-auto max-w-6xl">
             <div class="grid gap-12 md:grid-cols-2">
               <div itemscope itemtype="https://schema.org/PostalAddress">
                 <h2 class="text-xs font-semibold uppercase tracking-[0.25em] text-[#FFF200]">Где работаем</h2>
                 <p class="mt-4 text-2xl font-semibold text-white md:text-3xl">
                   <span itemprop="addressLocality">{business.locality}</span>
                 </p>
                 <p class="mt-2 text-white/65" itemprop="addressRegion">{business.region}</p>
                 <p class="mt-6 text-white/65 leading-relaxed">
                   Калининград и&nbsp;область — единственный регион работы.
                   От&nbsp;центра города до&nbsp;побережья и&nbsp;замков в&nbsp;области.
                 </p>
               </div>
               <div>
                 <h2 class="text-xs font-semibold uppercase tracking-[0.25em] text-[#FFF200]">Реквизиты</h2>
                 <div class="mt-4 space-y-2 text-white/80">
                   <p>{business.legalName}</p>
                   <p>ИНН&nbsp;{business.taxId}</p>
                   <p>ОГРНИП&nbsp;{business.ogrnip}</p>
                 </div>
                 <p class="mt-6 text-sm text-white/55">
                   <a href="/privacy/" class="underline decoration-[#FFF200]/40 underline-offset-4 hover:decoration-[#FFF200]">
                     Политика обработки персональных данных
                   </a>
                 </p>
               </div>
             </div>
           </div>
         </section>

         <MetrikaCounter slot="analytics" />
       </DarkLayout>
       ```
    2. **Я.Карта** — не делаем в 5.1 (требует API-ключ, отдельная задача). Если в `.business/company/about.md` есть упоминание Я.Карты — оставить как followup, не добавлять в этом плане.
    3. Тон — изящный, без жаргона. Тексты «обычно отвечаем в течение рабочего дня», «9:00–21:00» — реалистичные, не «24/7 на связи».
  </action>
  <verify>
    <automated>test -f src/pages/contacts.astro && grep -q "DarkLayout" src/pages/contacts.astro && grep -q "telegramPersonal" src/pages/contacts.astro && grep -q "schema.org/Organization" src/pages/contacts.astro && grep -q "schema.org/PostalAddress" src/pages/contacts.astro</automated>
  </verify>
  <acceptance_criteria>
    - /contacts/ открывается, видны 3 канала (телефон, Telegram, email)
    - Реквизиты ИП на месте (ИНН, ОГРНИП, legalName)
    - Микроразметка Organization + PostalAddress сохранена
    - Ссылка на /privacy/ работает
  </acceptance_criteria>
  <done>/contacts/ создан в тёмной палитре с реальными контактами и реквизитами.</done>
</task>

<task type="auto">
  <name>Task 3: Перевести /thanks/ и /privacy/ в тёмную палитру</name>
  <files>src/pages/thanks.astro, src/pages/privacy.astro</files>
  <read_first>
    - Текущий src/pages/thanks.astro целиком
    - Текущий src/pages/privacy.astro целиком
    - DarkLayout
  </read_first>
  <action>

    1. **/thanks/:**
       - Заменить BaseLayout → DarkLayout.
       - Hero: центрированный блок «Спасибо! Мы получили вашу заявку» (заголовок), подзаголовок «Свяжемся в течение рабочего дня — обычно быстрее», большая кнопка «На главную» → /. Можно добавить пингующую жёлтую точку рядом с заголовком (как в hero главной).
       - Без формы, без breadcrumbs (это конечная страница после отправки).
       - Padding `pt-32 pb-14 md:pt-48`.
       - noindex обязательно (как было).
       - Шаблон:
         ```astro
         ---
         import DarkLayout from "../layouts/DarkLayout.astro";
         import MetrikaCounter from "../components/MetrikaCounter.astro";
         ---
         <DarkLayout title="Спасибо — Крылья" description="Заявка получена" noindex>
           <section class="relative flex min-h-[70vh] items-center overflow-hidden px-6 py-14 md:px-12 md:py-20">
             <div class="lab-aurora pointer-events-none"></div>
             <div class="relative mx-auto max-w-2xl text-center">
               <span class="relative mx-auto mb-8 inline-flex h-3 w-3">
                 <span class="absolute inline-flex h-full w-full animate-ping rounded-full bg-[#FFF200] opacity-75"></span>
                 <span class="relative inline-flex h-3 w-3 rounded-full bg-[#FFF200]"></span>
               </span>
               <h1 class="text-4xl font-semibold tracking-tight text-white md:text-6xl">
                 Спасибо!
               </h1>
               <p class="mt-6 text-lg text-white/75 md:text-xl">
                 Мы получили вашу заявку. Обычно отвечаем в&nbsp;течение рабочего дня —
                 часто быстрее.
               </p>
               <a
                 href="/"
                 class="mt-10 inline-flex items-center justify-center rounded-full bg-[#FFF200] px-6 py-3.5 text-sm font-semibold text-black no-underline transition-colors hover:bg-[#FFF566]"
               >
                 На главную
               </a>
             </div>
           </section>
           <MetrikaCounter slot="analytics" />
         </DarkLayout>
         ```

    2. **/privacy/:**
       - Заменить BaseLayout → DarkLayout.
       - Hero: breadcrumbs, H1 «Политика обработки персональных данных», подчёркивание.
       - Контент: текст политики 152-ФЗ — **сохранить дословно из текущего файла** (это юридический текст, его нельзя править). Завернуть в article с теми же arbitrary-классами тёмной типографики, что и в /services/[slug].astro (см. план 04, task 3).
       - Шаблон:
         ```astro
         ---
         import DarkLayout from "../layouts/DarkLayout.astro";
         import Seo from "../components/Seo.astro";
         import JsonLdGraph from "../components/JsonLdGraph.astro";
         import Breadcrumbs from "../components/Breadcrumbs.astro";
         import MetrikaCounter from "../components/MetrikaCounter.astro";

         const seoTitle = "Политика обработки персональных данных — Крылья";
         const seoDescription = "Политика ИП Вострикова Мария Валерьевна о работе с персональными данными согласно 152-ФЗ.";

         const crumbs = [
           { label: "Главная", href: "/" },
           { label: "Политика обработки ПДн", href: "/privacy/" },
         ];
         ---
         <DarkLayout title={seoTitle} description={seoDescription} noindex>
           <Seo slot="seo" title={seoTitle} description={seoDescription} pathname="/privacy/" />
           <JsonLdGraph slot="seo" />

           <section class="relative overflow-hidden px-6 pt-32 pb-14 md:px-12 md:pt-40 md:pb-20">
             <div class="lab-aurora pointer-events-none"></div>
             <div class="relative mx-auto max-w-3xl">
               <Breadcrumbs crumbs={crumbs} />
               <h1 class="mt-6 text-3xl font-semibold tracking-tight text-white md:text-5xl">
                 Политика обработки персональных данных
               </h1>
               <span class="mt-4 inline-block h-1 w-12 bg-[#FFF200]"></span>
             </div>
           </section>

           <section class="relative px-6 pb-20 md:px-12">
             <article class="mx-auto max-w-3xl text-white/80
               [&_h2]:mt-12 [&_h2]:text-xl [&_h2]:font-semibold [&_h2]:text-white md:[&_h2]:text-2xl
               [&_h3]:mt-8 [&_h3]:text-lg [&_h3]:font-semibold [&_h3]:text-white
               [&_p]:mt-5 [&_p]:leading-relaxed
               [&_ol]:mt-5 [&_ol]:list-decimal [&_ol]:pl-6
               [&_ul]:mt-5 [&_ul]:list-disc [&_ul]:pl-6
               [&_strong]:text-white
               [&_a]:text-[#FFF200] [&_a]:underline">
               <!-- ВСТАВИТЬ СЮДА содержимое тела текущего privacy.astro (которое после --- кода) -->
             </article>
           </section>

           <MetrikaCounter slot="analytics" />
         </DarkLayout>
         ```
       - **Содержимое** — скопировать байт-в-байт из текущего privacy.astro (всё, что между `</section>` старого hero и закрывающим `</BaseLayout>`). Если в старом файле использовались классы вроде `prose` или `text-gray-dark` — убрать их (стили теперь через arbitrary-селекторы на `<article>`).
  </action>
  <verify>
    <automated>grep -q "DarkLayout" src/pages/thanks.astro && grep -q "DarkLayout" src/pages/privacy.astro && ! grep -q "BaseLayout" src/pages/thanks.astro && ! grep -q "BaseLayout" src/pages/privacy.astro && npm run build 2>&1 | tail -5 | grep -E "(Complete|built)"</automated>
  </verify>
  <acceptance_criteria>
    - /thanks/ показывает «Спасибо!» с пингующей точкой и кнопкой «На главную»
    - /privacy/ показывает текст политики (тот же, что был) в тёмной типографике с читаемыми H2/H3 и нумерованными списками
    - Обе страницы — noindex
    - npm run build проходит, страницы генерируются
  </acceptance_criteria>
  <done>/thanks/ и /privacy/ — в тёмной палитре. Все 9 страниц основного сайта теперь в одной палитре.</done>
</task>

</tasks>

<verification>
- /about/, /contacts/, /thanks/, /privacy/ работают на тёмной палитре
- npm run build проходит
- Все must_haves.truths выполнены
- Phase 6 в роадмапе можно помечать как «впитано»
</verification>

<success_criteria>
- 4 страницы созданы/переписаны в тёмной палитре
- Используются реальные контакты и реквизиты
- Микроразметка Organization + PostalAddress сохранена на /contacts/
- Готово к Wave 5 (финальный полишинг: общие Header/Footer)
</success_criteria>

<output>
После завершения создать `.planning/phases/05.1-dark-redesign/05.1-06-SUMMARY.md`
</output>
