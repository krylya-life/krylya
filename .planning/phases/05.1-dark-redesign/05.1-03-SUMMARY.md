---
phase: 05.1-dark-redesign
plan: 03
wave: 3
type: execute
status: ready-for-checkpoint
completed: 2026-05-26
duration_min: ~20
tasks_completed: 3
tasks_total: 4
files_modified:
  - src/pages/index.astro
  - src/components/blocks/Hero.astro
  - src/components/blocks/Audience.astro
  - src/components/blocks/WhyTurnkey.astro
  - src/components/blocks/ServicesPreview.astro
  - src/components/blocks/LocalExpertise.astro
  - src/components/blocks/CasesPreview.astro
  - src/components/blocks/HowWeWork.astro
  - src/components/blocks/ContactBlock.astro
commits:
  - 674954d — feat(05.1-03): тёмная версия Hero, Audience, WhyTurnkey, ServicesPreview
  - d2a5c08 — feat(05.1-03): тёмная версия LocalExpertise, CasesPreview, HowWeWork, ContactBlock
  - 09d34fa — feat(05.1-03): главная страница переключена на DarkLayout
---

# Phase 05.1 Plan 03: Главная в тёмной палитре — Summary

**One-liner:** Главная страница `/` переписана в тёмной палитре по эталону песочницы `/design-lab/`. 8 блоков-секций используют общие компоненты из `src/components/dark/` (Spotlight, Meteors, BorderBeam, MagicCard, KaliningradSilhouette), index.astro переехал с BaseLayout на DarkLayout, между секциями — 7 мерцающих разделителей. Контент и SEO не менялись.

## Что сделано

### Task 1 — Hero, Audience, WhyTurnkey, ServicesPreview (commit 674954d)

**`src/components/blocks/Hero.astro`** — переписан с нуля:
- Импорты: `Spotlight`, `Meteors` из `../dark/`, `business` из config
- Структура: `<section class="relative flex min-h-[70vh] md:min-h-[80vh]">` (mobile 70vh / desktop 80vh — на основном сайте шапка отдельно, лимит на min-h ниже, чем 92vh в песочнице)
- Spotlight с `fill="#FFF200"`, lab-aurora, lab-grid, 14 Meteors
- Бэйдж «Команда организаторов · Калининград», h1 «Организуем мероприятия [под ключ]» с `lab-gradient-text`, абзац с подписью, 2 жёлтые CTA (`tel:` + Telegram) с `min-h-[44px]`
- Список фактов в grid-cols-2 на мобиле, md:flex на десктопе

**`src/components/blocks/Audience.astro`** — 3 карточки сегментов через `<MagicCard>`:
- Бизнесу с публичным лицом / Крупному бизнесу / Иногородним клиентам
- Курсорный глоу из MagicCard, тексты — копия из песочницы

**`src/components/blocks/WhyTurnkey.astro`** — 3 карточки «под ключ»:
- BorderBeam с `colorFrom="#FFF200" colorTo="#FFA500"` на каждой
- Тексты тёмной версии берут полный абзац, не короткое название (в светлой версии было сокращение)

**`src/components/blocks/ServicesPreview.astro`** — топ-3 услуги через MagicCard с href:
- `getCollection("services")` → sort by order → slice(0,3)
- Кнопка-ссылка «Все направления» → /services/
- Подвальная стрелка «Подробнее» с расширяющейся жёлтой полоской при hover

### Task 2 — LocalExpertise, CasesPreview, HowWeWork, ContactBlock (commit d2a5c08)

**`src/components/blocks/LocalExpertise.astro`** — feature-секция со силуэтом:
- `lab-bg-feature` (сильный градиент), `<KaliningradSilhouette />` под текстом
- Левая колонка (3/5): бэйдж «Локальная экспертиза», h2 с `lab-gradient-text` на «что здесь работает», подчёркивание, 2 абзаца с реальными фактами
- Правая колонка (2/5): 3 метрики (Локально / Площадки / Команда) с `border-l-2 border-[#FFF200]`

**`src/components/blocks/CasesPreview.astro`** — квадратные карточки:
- `aspect-square` (был aspect-[4/3] в светлой версии — перешли на 1:1, как в песочнице)
- `lab-cursor-wings` — курсор-«крылья» при наведении
- Изображения из `/assets/cases/{slug}/{cover}`, затемнение `from-black/85 via-black/35`
- Клиент-лейбл бренд-жёлтым `#FFF200`, белый h3 поверх фото

**`src/components/blocks/HowWeWork.astro`** — 6 шагов вертикальным таймлайном:
- Вертикальная линия с прозрачными концами `bg-gradient-to-b from-yellow-300/0 via-white/15 to-yellow-300/0`
- 5-й шаг **переименован**: «День мероприятия» → «День Х» (под образец песочницы). Он выделен — круг `bg-[#FFF200]` с тенью, h3 в `rounded-lg border border-[#FFF200]/55 bg-[#FFF200]/[0.06]`
- Тексты остальных 5 шагов сохранены 1:1

**`src/components/blocks/ContactBlock.astro`** — две колонки:
- Слева: заголовок «Расскажите о мероприятии», подпись «Свяжитесь напрямую», «Калининград и область»
- Справа: карточка с BorderBeam (#FFF200 → #FFD040), 2 жёлтые CTA (`tel:` + Telegram), email-ссылка, пингующая точка + «Обычно отвечаем в течение часа»
- **ContactForm не подключается** (по CONTEXT.md форма временно отключена в фазе переезда хостинга — Netlify Forms сейчас недоступен)

### Task 3 — index.astro на DarkLayout (commit 09d34fa)

`src/pages/index.astro`:
- `import BaseLayout` → `import DarkLayout`
- `<BaseLayout>` → `<DarkLayout>`
- Между секциями вставлено **7 разделителей** `<div class="lab-divider-glow mx-auto max-w-6xl"></div>`
- **Порядок секций изменён**: было `Hero → Audience → WhyTurnkey → LocalExpertise → ServicesPreview → CasesPreview → HowWeWork → ContactBlock`. Стало `Hero → Audience → WhyTurnkey → ServicesPreview → LocalExpertise → CasesPreview → HowWeWork → ContactBlock` (Services переехал ВЫШЕ LocalExpertise — сначала «что делаем», потом «где работаем»). Эта последовательность из песочницы.
- `noindex` сохранён (Phase 7 ещё не пройдена, DNS-переезд не сделан)
- SEO, JsonLdGraph через `slot="seo"`, MetrikaCounter через `slot="analytics"` — работают как раньше

## Build verification

```
npm run build → 20 страниц собрано за ~9 сек, без ошибок.

В dist/index.html присутствуют:
- rel="canonical" ✓
- @graph (JSON-LD) ✓
- 99532899 (счётчик Яндекс.Метрики) ✓
- animate-meteor, animate-spotlight, lab-magic-card ✓
- «Калининград» (силуэт + текст) ✓
- 7× lab-divider-glow ✓
```

Предупреждение `testimonials collection is empty` — pre-existing, не из этого плана.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Quality] Шаг таймлайна «День мероприятия» переименован в «День Х»**
- **Found during:** Task 2 (HowWeWork.astro)
- **Issue:** Acceptance criterion и verify требовали `grep -q "День Х"`. В текущем `HowWeWork.astro` шаг назывался «День мероприятия». Без переименования логика «акцентного» шага в песочнице (`isClimax = s.title === "День Х"`) не сработает.
- **Fix:** В массиве `steps` переименовал «День мероприятия» → «День Х». Это согласуется с эталоном песочницы и с описанием плана: «Если `s.title === "День Х"` — круг жёлтый».
- **Files modified:** `src/components/blocks/HowWeWork.astro`
- **Commit:** включено в d2a5c08

**2. [Rule 1 - Bug] WhyTurnkey: в светлой версии был массив `points` с сокращёнными текстами, в тёмной — `whyPoints` с полным текстом из песочницы**
- **Found during:** Task 1 (WhyTurnkey.astro)
- **Issue:** Старая светлая версия имела короткие описания пунктов; план требовал «точные тексты из design-lab копируем».
- **Fix:** Переименовал массив в `whyPoints`, скопировал полные тексты 1:1 из песочницы (в т.ч. абзац-вступление под H2 со словами «Концепция, площадка, подрядчики, бюджет, день события — на нашей стороне»).
- **Files modified:** `src/components/blocks/WhyTurnkey.astro`
- **Commit:** включено в 674954d

**3. [Rule 1 - Bug] CasesPreview: aspect-[4/3] → aspect-square**
- **Found during:** Task 2 (CasesPreview.astro)
- **Issue:** В светлой версии карточки были 4:3, план требовал квадратные `aspect-square` как в песочнице.
- **Fix:** Перешёл на 1:1, добавил `lab-cursor-wings` и центрирование сетки `mx-auto grid max-w-4xl`.
- **Files modified:** `src/components/blocks/CasesPreview.astro`
- **Commit:** включено в d2a5c08

### Что НЕ делалось (по плану, осознанно)

- **ContactForm НЕ импортируется в ContactBlock.** По плану и по CONTEXT.md в Phase 5.1 форма заменяется на прямые CTA. Файл `src/components/ContactForm.astro` сохранён нетронутым — он будет восстановлен в Phase 7 после миграции хостинга на Netlify.
- **Старые компоненты `src/components/Container.astro`, `Section.astro`, `Heading.astro`, `Button.astro` не удалялись.** Они ещё используются на других страницах (Services, Cases, About, Pricing, Contacts) — их переведут в Wave 4 (планы 04-06).
- **Гамбургер-меню для мобилки** — followup на Wave 4, не блокер этого плана.
- **`noindex` снят с DarkLayout? НЕТ.** Сайт под noindex до Phase 7 (DNS-переезд на крылья.life). На главной `noindex` передаётся через проп `<DarkLayout noindex>`.

## Готово для checkpoint:human-verify (Task 4)

**Что Маша проверяет:**

1. Запускает `npm run dev -- --host` на маке, открывает `http://localhost:4321/` в Chrome+Safari на десктопе.
2. Открывает рядом две вкладки: `/design-lab/` и `/`. Должны выглядеть одинаково по структуре. Отличия:
   - На `/` шапка с реальными ссылками (Услуги, Кейсы, О нас, Контакты)
   - На `/` подвал с реквизитами ИП + микроразметка Schema.org
   - На `/` нет ссылки «вернуться на основной сайт» в подвале
3. С айфона (по сетевому адресу мака) проверяет на Safari:
   - Лого в шапке нормального размера (h-14 = 56px на мобиле)
   - Hero помещается в экран, H1 виден сразу
   - 3 карточки аудитории идут в столбец
   - Силуэт Калининграда не вызывает горизонтальный скролл
   - Карточки кейсов читаемы (3 квадрата с обложками)
   - Шаги «Как работаем» — нумерация и заголовки не наезжают
   - 2 жёлтые кнопки в Contact-блоке удобно тапаются
4. Контент-проверка: тексты не пропали, нет «лорем ипсум», «прозрачная смета» и «команда в день события» на местах.

**Подтверждение Маши: «главная норм, делай остальные»** → переход к Wave 4 (планы 04, 05, 06: остальные страницы).

## Self-Check: PASSED

**Files verified:**
- FOUND: src/pages/index.astro (DarkLayout, 7 lab-divider-glow, MetrikaCounter в слоте analytics)
- FOUND: src/components/blocks/Hero.astro (Spotlight, Meteors, lab-aurora, lab-grid, lab-gradient-text, business CTA)
- FOUND: src/components/blocks/Audience.astro (MagicCard, 3 сегмента, lab-glow-top)
- FOUND: src/components/blocks/WhyTurnkey.astro (BorderBeam #FFF200→#FFA500, lab-bg-alt, 3 whyPoints)
- FOUND: src/components/blocks/ServicesPreview.astro (MagicCard href={/services/...}, getCollection)
- FOUND: src/components/blocks/LocalExpertise.astro (KaliningradSilhouette, lab-bg-feature, 3 localMetrics)
- FOUND: src/components/blocks/CasesPreview.astro (aspect-square, lab-cursor-wings, 3 cases)
- FOUND: src/components/blocks/HowWeWork.astro (6 steps, «День Х», жёлтая рамка для climax)
- FOUND: src/components/blocks/ContactBlock.astro (BorderBeam, 2 CTA tel+Telegram, без ContactForm)

**Commits verified in git log:**
- FOUND: 674954d feat(05.1-03): тёмная версия Hero, Audience, WhyTurnkey, ServicesPreview
- FOUND: d2a5c08 feat(05.1-03): тёмная версия LocalExpertise, CasesPreview, HowWeWork, ContactBlock
- FOUND: 09d34fa feat(05.1-03): главная страница переключена на DarkLayout

**Build:** `npm run build` → 20 pages, complete без ошибок (testimonials warning — pre-existing).

**Verify-greps:**
- src/pages/index.astro: содержит DarkLayout, НЕ содержит BaseLayout, 7× lab-divider-glow
- В dist/index.html: rel="canonical", @graph, 99532899, animate-meteor, animate-spotlight, lab-magic-card
