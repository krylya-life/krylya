---
phase: 14-semantika-kontent-hab
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/content.config.ts
  - src/content/ideas/korporativ-iz-drugogo-goroda.md
  - src/layouts/IdeaLayout.astro
  - src/components/ArticleSchema.astro
  - src/components/blocks/IdeaCard.astro
  - src/pages/идеи/index.astro
  - src/pages/идеи/[slug].astro
  - src/pages/rss.xml.ts
  - src/pages/sitemap.xml.ts
  - src/components/dark/DarkHeader.astro
  - src/components/dark/DarkFooter.astro
  - .planning/phases/14-semantika-kontent-hab/checks.sh
autonomous: true
requirements: [HUB-02, HUB-03]

must_haves:
  truths:
    - "Пользователь видит витрину /идеи/ с карточками статей по рубрикам, в тёмном стиле сайта"
    - "Пользователь открывает статью по URL /идеи/<slug>/"
    - "Canonical каждой статьи — в кириллице, не Punycode"
    - "RSS /rss.xml отдаёт статьи со ссылками item.link в кириллице"
    - "Каждая статья присутствует в sitemap.xml"
    - "В шапке и футере есть пункт «Идеи»"
    - "Страница статьи содержит JSON-LD BlogPosting + BreadcrumbList + микроразметку Article"
  artifacts:
    - path: "src/content.config.ts"
      provides: "Zod-схема коллекции ideas (5-я коллекция, паттерн cases)"
      contains: "ideas"
    - path: "src/layouts/IdeaLayout.astro"
      provides: "Единый шаблон статьи с мостиком на услугу (D-08)"
      min_lines: 40
    - path: "src/pages/идеи/index.astro"
      provides: "Витрина /идеи/ карточками по рубрикам"
    - path: "src/pages/идеи/[slug].astro"
      provides: "Страница статьи /идеи/<slug>/"
    - path: "src/pages/rss.xml.ts"
      provides: "RSS-лента с обходом Punycode-бага @astrojs/rss"
    - path: "src/components/ArticleSchema.astro"
      provides: "JSON-LD BlogPosting (author=Organization, D-02)"
    - path: ".planning/phases/14-semantika-kontent-hab/checks.sh"
      provides: "Регресс-скрипт grep-проверок IDN/RSS/sitemap/JSON-LD"
  key_links:
    - from: "src/pages/идеи/[slug].astro"
      to: "src/layouts/IdeaLayout.astro"
      via: "рендер <IdeaLayout idea={...}><Content/></IdeaLayout>"
      pattern: "IdeaLayout"
    - from: "src/layouts/IdeaLayout.astro"
      to: "src/components/Seo.astro"
      via: "canonical в кириллице через явный pathname literal"
      pattern: "pathname=.*идеи"
    - from: "src/pages/rss.xml.ts"
      to: "astro:content getCollection ideas"
      via: "item.link — абсолютный кириллический URL"
      pattern: "getCollection\\(\"ideas\"\\)"
    - from: "src/pages/sitemap.xml.ts"
      to: "astro:content getCollection ideas"
      via: "динамический блок маршрутов идей"
      pattern: "getCollection\\(\"ideas\"\\)"
---

<objective>
Реализовать раздел `/идеи/` на сайте по отработанному паттерну коллекции `cases`: коллекция `ideas` с Zod-схемой, `IdeaLayout` с мостиком на услугу, витрина, страница статьи, RSS `/rss.xml`, JSON-LD BlogPosting + BreadcrumbList + микроразметка Article, кириллический canonical, пункт «Идеи» в навигации, гибридный sitemap. Зафиксировать технологию контент-хаба как нативный Astro Content Collection в подпапке (HUB-02).

Purpose: HUB-02 + HUB-03 — работающая инфраструктура блога, в которую 14-04 просто добавляет `.md`-файлы.
Output: раздел `/идеи/` собирается (`npm run build` зелёный), все grep-проверки IDN/RSS/sitemap/JSON-LD проходят, регресс-скрипт `checks.sh` создан.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-semantika-kontent-hab/14-CONTEXT.md
@.planning/phases/14-semantika-kontent-hab/14-RESEARCH.md
@.planning/phases/09-seo/09-01-SUMMARY.md

<interfaces>
<!-- Прямые аналоги в репозитории — исполнитель повторяет их, не исследует заново. -->

Из src/content.config.ts (образец cases — reference("services"), z.coerce.date, cover:z.string):
  collections = { services, cases, testimonials, team }  // добавить ideas 5-й

Из src/components/Seo.astro — КЛЮЧЕВОЙ паттерн IDN (Phase 09-01):
  const base = business.urlCyrillic.replace(/\/$/, "");
  const canonical = base + pathname;   // строковая конкатенация, НЕ new URL()
  Props: { title, description, pathname?, image?, ogType?: "website"|"article" }
  ВСЕГДА передавать pathname явным литералом для /идеи/ (Astro.url.pathname вернёт percent-encoded)

Из src/components/Breadcrumbs.astro — использовать КАК ЕСТЬ (содержит и JSON-LD BreadcrumbList, и itemscope/itemprop):
  Props: { crumbs: {label, href}[] }  // последний без ссылки

Из src/components/JsonLdGraph.astro — узел Organization для author статьи:
  orgId = `${business.url}/#organization`   // ArticleSchema.author ссылается на этот @id

Из src/config/business.ts:
  business.urlCyrillic = "https://крылья.life"; business.url = "https://крылья.life"; business.name = "Крылья"

Из src/pages/sitemap.xml.ts — РУЧНОЙ массив routes (НЕ @astrojs/sitemap, удалён в Phase 09-01):
  const base = business.urlCyrillic.replace(/\/$/, "");
  routes.map(r => `  <url><loc>${base}${r}</loc></url>`)

DarkHeader navItems (src/components/dark/DarkHeader.astro:11): массив {href,label} — одна правка на десктоп+мобайл
DarkLayout слоты: <slot name="seo"/>, <slot/>, <slot name="analytics"/>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Установить @astrojs/rss, завести коллекцию ideas + флагманский seed-файл, создать IdeaLayout + ArticleSchema</name>
  <read_first>
    - src/content.config.ts (образец схемы cases — reference, z.coerce.date, cover:z.string)
    - src/pages/cases/[slug].astro (образец getStaticPaths + render + Seo + Breadcrumbs)
    - src/components/Seo.astro (IDN-паттерн: base+pathname, ogType="article")
    - src/components/JsonLdGraph.astro (orgId для author статьи)
    - src/layouts/DarkLayout.astro (слоты seo/analytics)
    - .planning/phases/09-seo/09-01-SUMMARY.md (почему конкатенация, не new URL())
  </read_first>
  <action>
    1. `npm install @astrojs/rss` (версия 4.0.19). Сразу прогнать `npm run build` как smoke-тест (RESEARCH A1 — проверить ESM-совместимость с Astro 6.1.9). Билд обязан быть зелёным.

    2. В `src/content.config.ts` добавить коллекцию `ideas` рядом с существующими (папка контента ASCII — `./src/content/ideas`, НЕ кириллица):
       ```ts
       const ideas = defineCollection({
         loader: glob({ pattern: "**/*.md", base: "./src/content/ideas" }),
         schema: z.object({
           title: z.string(),
           slug: z.string().regex(/^[a-z0-9-]+$/),
           description: z.string().max(160),
           rubric: z.enum(["kak-organizovat", "formaty", "kaliningrad", "trendy"]),
           intentCluster: z.string(),
           publishDate: z.coerce.date(),
           updatedDate: z.coerce.date().optional(),
           cover: z.string(),
           coverAlt: z.string(),
           relatedService: reference("services"),
           relatedCase: reference("cases").optional(),
           draft: z.boolean().default(false),
         }),
       });
       ```
       Обновить `export const collections = { services, cases, testimonials, team, ideas };`

    3. Создать seed-файл `src/content/ideas/korporativ-iz-drugogo-goroda.md` (флагман D-03) — реальный, но короткий (2–3 абзаца вступления, полное тело допишет 14-04). Frontmatter:
       - title: «Как организовать корпоратив в Калининграде, если вы в другом городе» (обязательно «как»-фрейминг — anti-cannibalization, RESEARCH Pitfall 1)
       - slug: korporativ-iz-drugogo-goroda
       - description: до 160 симв., информационная (не «закажите»)
       - rubric: kak-organizovat
       - intentCluster: «удалённая организация / другой город»
       - publishDate: 2026-07-04
       - cover: cover.jpg  (плейсхолдер; реальную обложку добавят в 14-04/Мария — путь public/assets/ideas/korporativ-iz-drugogo-goroda/cover.jpg)
       - coverAlt: осмысленный alt
       - relatedService: corporate-parties
       - draft: false
       Тело — голос агентства «мы в Крыльях» (D-02), без цен и «закажите». Создавать файл через Write-инструмент (не Finder) — NFC-нормализация (RESEARCH Pitfall 3).

    4. Создать `src/components/ArticleSchema.astro` — JSON-LD BlogPosting. Props: `idea` (CollectionEntry<"ideas">). author — **Organization** со ссылкой `{"@id": business.url + "/#organization"}` (D-02, НЕ Person). Поля: `@type:"BlogPosting"`, headline=title, description, datePublished=publishDate.toISOString(), dateModified=(updatedDate||publishDate), mainEntityOfPage=кириллический URL статьи, image=кириллический абсолютный URL обложки, publisher со ссылкой на orgId. Вывод — `<script type="application/ld+json" is:inline set:html={JSON.stringify(json)} />` (как в Breadcrumbs.astro).

    5. Создать `src/layouts/IdeaLayout.astro` по образцу из RESEARCH (раздел «Pattern 2»). Внутри: DarkLayout + `<Seo slot="seo" ... pathname={`/идеи/${d.slug}/`} ogType="article"/>` + JsonLdGraph + ArticleSchema + `<Breadcrumbs crumbs={[...]}/>` + `<article itemscope itemtype="https://schema.org/Article">` с `<meta itemprop="headline/datePublished/author(Organization)">` + `<slot/>` (тело) + мостик D-08 (мягкая ссылка `/services/${service.data.slug}/`, текст без «закажите/дешевле») + `<ContactBlock/>` + `<MetrikaCounter slot="analytics"/>`. relatedService резолвить через `getEntry(d.relatedService)`.
  </action>
  <verify>
    <automated>npm run build && grep -q "ideas" src/content.config.ts && test -f src/layouts/IdeaLayout.astro && test -f src/components/ArticleSchema.astro && test -f src/content/ideas/korporativ-iz-drugogo-goroda.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` завершается кодом 0 (Zod-схема валидна, @astrojs/rss совместим)
    - `grep -c "ideas" src/content.config.ts` ≥ 2 (определение + export)
    - `src/layouts/IdeaLayout.astro` существует, ≥40 строк, содержит `pathname={`/идеи/`
    - `grep -q "Organization" src/components/ArticleSchema.astro` — author не Person (D-02)
    - `grep -q "relatedService\|corporate-parties" src/content/ideas/korporativ-iz-drugogo-goroda.md` — мостик на услугу задан
    - Заголовок seed-файла начинается с «Как» (anti-cannibalization)
  </acceptance_criteria>
  <done>Коллекция ideas собирается, флагман-seed рендерится, IdeaLayout + ArticleSchema готовы. Технология контент-хаба зафиксирована как нативный Astro Content Collection (HUB-02).</done>
</task>

<task type="auto">
  <name>Task 2: Витрина /идеи/, страница /идеи/[slug]/, пункт «Идеи» в навигации</name>
  <read_first>
    - src/pages/cases/index.astro (образец витрины — сетка карточек, Seo, ContactBlock)
    - src/pages/cases/[slug].astro (образец getStaticPaths + render(current))
    - src/layouts/IdeaLayout.astro (создан в Task 1 — потребитель)
    - src/components/dark/DarkHeader.astro (navItems:11)
    - src/components/dark/DarkFooter.astro (список ссылок ~58–61)
  </read_first>
  <action>
    1. Создать `src/components/blocks/IdeaCard.astro` — карточка статьи для витрины (образец карточки из cases/index.astro: aspect-[4/3], обложка `/assets/ideas/${slug}/${cover}`, eyebrow=рубрика (человекочитаемая метка), title). Тёмный стиль, `lab-cursor-wings`.

    2. Создать `src/pages/идеи/index.astro` (кириллическая папка — создавать через Write-инструмент, NFC). `getCollection("ideas", ({data}) => !data.draft)`, сортировка по publishDate desc. Витрина карточками (D-06), сгруппированными по 4 рубрикам D-07 (заголовок рубрики + сетка карточек этой рубрики; пустые рубрики скрывать). Hero с хлебными крошками (Главная · Идеи), Seo (`pathname="/идеи/"`), JsonLdGraph, ContactBlock, MetrikaCounter. Пустое состояние — «Статьи появятся скоро».
       Метки рубрик: kak-organizovat→«Как организовать», formaty→«Форматы», kaliningrad→«Калининград», trendy→«Тренды».

    3. Создать `src/pages/идеи/[slug].astro`. `getStaticPaths` из `getCollection("ideas", ({data}) => !data.draft)`, `params: { slug: entry.data.slug }`. В теле: `const { Content } = await render(entry)` и рендер `<IdeaLayout idea={entry}><Content/></IdeaLayout>`. Вся вёрстка/SEO — внутри IdeaLayout; страница только маршрутизирует.

    4. Навигация: в `src/components/dark/DarkHeader.astro` добавить в массив `navItems` строку `{ href: "/идеи/", label: "Идеи" }` после «Кейсы». В `src/components/dark/DarkFooter.astro` добавить `<li><a href="/идеи/" class="text-white/75 hover:text-white transition-colors">Идеи</a></li>` после ссылки «Кейсы».
  </action>
  <verify>
    <automated>npm run build && test -f dist/идеи/index.html && test -f dist/идеи/korporativ-iz-drugogo-goroda/index.html && grep -q "идеи" src/components/dark/DarkHeader.astro && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` код 0; `dist/идеи/index.html` существует; `dist/идеи/korporativ-iz-drugogo-goroda/index.html` существует
    - `grep -c "xn--" dist/идеи/korporativ-iz-drugogo-goroda/index.html` в теге `<link rel="canonical">` = 0 (кириллический canonical)
    - `grep -q "BlogPosting" dist/идеи/korporativ-iz-drugogo-goroda/index.html` и `grep -q "BreadcrumbList" dist/идеи/korporativ-iz-drugogo-goroda/index.html`
    - Витрина `dist/идеи/index.html` содержит карточку статьи (`grep -q "korporativ-iz-drugogo-goroda" dist/идеи/index.html`)
    - `grep -q "/идеи/" src/components/dark/DarkHeader.astro` и то же в DarkFooter.astro
  </acceptance_criteria>
  <done>Витрина и страница статьи рендерятся в тёмном стиле, canonical в кириллице, JSON-LD BlogPosting+BreadcrumbList на месте, пункт «Идеи» в шапке и футере.</done>
</task>

<task type="auto">
  <name>Task 3: RSS /rss.xml (обход Punycode), гибридный sitemap, регресс-скрипт checks.sh</name>
  <read_first>
    - src/pages/sitemap.xml.ts (ручной массив routes — расширяем гибридом)
    - src/config/business.ts (urlCyrillic)
    - .planning/phases/14-semantika-kontent-hab/14-RESEARCH.md (раздел «Code Examples» — точный код RSS + grep-набор)
    - .planning/phases/09-seo/09-01-SUMMARY.md (grep-паттерн xn-- из Phase 09)
  </read_first>
  <action>
    1. Создать `src/pages/rss.xml.ts` строго по коду из RESEARCH «Code Examples»:
       - `import rss from "@astrojs/rss"`, `getCollection("ideas", ({data}) => !data.draft)`, сортировка по publishDate desc.
       - `const base = business.urlCyrillic.replace(/\/$/, "");`
       - **КРИТИЧНО:** `item.link` — АБСОЛЮТНЫЙ кириллический URL `` `${base}/идеи/${post.data.slug}/` `` (НЕ относительный путь). Тогда `isValidURL()` внутри @astrojs/rss пропустит его без нормализации в Punycode (RESEARCH Pitfall 2).
       - `site: business.urlCyrillic` — канальный `<link>` всё равно уйдёт в Punycode: задокументировать комментарием как известное ограничение пакета (не блокер), это единственное допустимое совпадение `xn--` в dist/rss.xml.

    2. Переписать `src/pages/sitemap.xml.ts` на гибрид (RESEARCH Pitfall 4 — «лучшее решение фазы»): статичный массив `routes` для существующих страниц ОСТАЁТСЯ; добавить `"/идеи/"` (витрина) в статичный список; секцию статей строить ДИНАМИЧЕСКИ через `getCollection("ideas", ({data}) => !data.draft)` → для каждой статьи `"/идеи/" + entry.data.slug + "/"`. Так новые статьи 14-04 попадают в sitemap автоматически, без ручного добавления строки. Оставить комментарий: «если понадобится ручной маршрут — добавить строку в routes».
       Endpoint становится async: `export const GET: APIRoute = async () => {...}`.

    3. Создать `.planning/phases/14-semantika-kontent-hab/checks.sh` — исполняемый bash-регресс-скрипт (VALIDATION.md Wave 0). Содержит проверки из RESEARCH «Code Examples», каждая с понятным сообщением и ненулевым exit при провале:
       - `grep -c "xn--" dist/rss.xml` ≤ 1 (только канальный link)
       - в canonical статей: `for f in dist/идеи/*/index.html; do grep -o 'rel="canonical" href="[^"]*"' "$f" | grep -c "xn--"; done` == 0
       - `grep -c "xn--" dist/sitemap.xml` == 0
       - число `/идеи/<slug>/` в dist/sitemap.xml == число не-draft статей
       - каждая статья содержит `"@type":"BlogPosting"` и `BreadcrumbList`
       Скрипт запускать после `npm run build`.

    4. Прогнать `npm run build && bash .planning/phases/14-semantika-kontent-hab/checks.sh` — всё зелёное.
  </action>
  <verify>
    <automated>npm run build && test -f dist/rss.xml && bash .planning/phases/14-semantika-kontent-hab/checks.sh && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `dist/rss.xml` существует; `grep -c "xn--" dist/rss.xml` ≤ 1 (только канальный `<link>`)
    - `grep -q "идеи/korporativ-iz-drugogo-goroda" dist/rss.xml` — ссылка статьи в кириллице (обход сработал)
    - `grep -c "xn--" dist/sitemap.xml` == 0
    - `grep -c "/идеи/korporativ-iz-drugogo-goroda/" dist/sitemap.xml` == 1 (статья в sitemap через getCollection)
    - `bash checks.sh` завершается кодом 0
  </acceptance_criteria>
  <done>RSS отдаёт статьи в кириллице (Punycode только в канальном link), sitemap авто-включает статьи через getCollection, регресс-скрипт checks.sh зелёный. HUB-03 технически закрыт.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Автор→сборка (frontmatter статьи) | Markdown-контент авторов проходит Zod-валидацию на этапе билда |
| Сборка→краулер/читатель (RSS/HTML) | Статический XML/HTML отдаётся Cloudflare Pages |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-02 | Tampering | Zod-схема ideas (frontmatter) | mitigate | Zod-валидация в `content.config.ts` — билд падает при невалидном/лишнем поле (ASVS V5); slug ограничен `regex(/^[a-z0-9-]+$/)` |
| T-14-03 | Tampering | RSS `<description>` / тело статьи | mitigate | `@astrojs/rss` авто-эскейпит спецсимволы; Astro Markdown экранирует HTML по умолчанию; не использовать `set:html` на авторском вводе |
| T-14-04 | Information Disclosure | Публичный статический раздел /идеи/ | accept | Весь контент публичный по замыслу, PII нет |
</threat_model>

<verification>
- `npm run build` код 0
- `bash .planning/phases/14-semantika-kontent-hab/checks.sh` зелёный
- `grep -c "xn--" dist/sitemap.xml` == 0; в canonical статей 0; в rss.xml ≤ 1
- Ручной просмотр `npm run dev` → `/идеи/` витрина и статья открываются в тёмном стиле
</verification>

<success_criteria>
Решение по технологии зафиксировано (нативный Astro Content Collection, HUB-02) и раздел /идеи/ реализован: коллекция+Zod, IdeaLayout, витрина, страница статьи, RSS, JSON-LD BlogPosting+микроразметка Article+BreadcrumbList, кириллический canonical, пункт «Идеи» в навигации (Success Criteria #2, #3 фазы).
</success_criteria>

<output>
After completion, create `.planning/phases/14-semantika-kontent-hab/14-02-SUMMARY.md`
</output>
