---
phase: 10-konkurenty-mira
plan: 02
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - .planning/research/competitors/competitor-world.md
autonomous: false
requirements: [COMP-01]
must_haves:
  truths:
    - "В файле есть прикладная матрица «Что украсть / что не повторять» из 5–7 конкретных идей"
    - "Каждая идея сформулирована как «что сделать Крыльям → почему работает → в какую фазу ложится (13/14/16)»"
    - "Есть отдельный блок «SEO-практики топ-агентств, которых нет у Крыльев» — прямой вход в Фазу 14"
    - "Помечены мировые тактики, которые НЕ портируются на Крылья (например Instagram с ценами/CTA под 38-ФЗ)"
    - "Мария подтвердила финальный список агентств и матрицу"
  artifacts:
    - path: ".planning/research/competitors/competitor-world.md"
      provides: "Матрица «украсть/не повторять» (5–7 идей) + блок SEO-практик, которых нет у Крыльев"
      contains: "Что украсть / что не повторять"
  key_links:
    - from: ".planning/research/competitors/competitor-world.md"
      to: ".planning/ROADMAP.md"
      via: "каждая идея матрицы привязана к будущей фазе (13 форматы / 14 контент / 16 SMM)"
      pattern: "Фаза 1[346]"
---

<objective>
Достроить `competitor-world.md` двумя прикладными блоками, которые превращают
досье в рабочий материал:
1. `## Что украсть / что не повторять` — прикладная матрица из 5–7 конкретных
   идей (D-04). Каждая идея в формате «что именно сделать „Крыльям“ → почему это
   работает → в какую будущую фазу ложится (13 форматы / 14 контент / 16 SMM)».
   Не абстрактное вдохновение, а готовые к внедрению шаги.
2. `## SEO-практики топ-агентств, которых нет у «Крыльев»` — обязательный
   отдельный блок (D-05, Success Criterion #3): конкретные кластеры, типы
   контента, приёмы перелинковки, которых у «Крыльев» сейчас нет. Прямой вход в
   Фазу 14 (семантическое ядро + контент-хаб).

Выводы формулируем через ценность и применимость к «Крыльям» (D-11); мировые
тактики, которые НЕ портируются (Instagram с ценами/CTA — незаконно у нас под
38-ФЗ в режиме «информирование») — явно помечаем.

Назначение: Мария получает не просто обзор, а список готовых шагов с адресом
(в какую фазу). Результат: те же два новых раздела в `competitor-world.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-konkurenty-mira/10-CONTEXT.md
@../.business/marketing/inspiration.md
@.planning/research/competitors/competitor-world.md
@.planning/research/competitors/differentiation.md
</context>

<tasks>

<task type="auto">
  <name>Задача 1: Прикладная матрица «Что украсть / что не повторять» (5–7 идей)</name>
  <read_first>
    - .planning/research/competitors/competitor-world.md (досье из плана 10-01 — источник идей)
    - .planning/phases/10-konkurenty-mira/10-CONTEXT.md (D-04, D-11)
    - .planning/ROADMAP.md (§Phase 13 форматы, §Phase 14 контент-хаб, §Phase 16 SMM — куда ложатся идеи)
    - .planning/research/competitors/differentiation.md (отстройка Фазы 11 — не противоречить)
  </read_first>
  <files>.planning/research/competitors/competitor-world.md</files>
  <action>
  Дополнить `competitor-world.md` разделом `## Что украсть / что не повторять`.
  Внутри — пронумерованный список из 5–7 идей (`1.`, `2.`, ...). Каждая идея
  ОБЯЗАТЕЛЬНО в формате трёх частей (D-04):
  - **Что сделать «Крыльям»** — конкретный, готовый к внедрению шаг (не «сделать
    сайт лучше», а например «завести раздел /идеи/ с гайдами под форматы»).
  - **Почему это работает** — механика, взятая из досье конкретного агентства.
  - **Куда ложится** — явно назвать целевую фазу: `Фаза 13` (форматы),
    `Фаза 14` (контент) или `Фаза 16` (SMM).

  Обязательно включить и «что НЕ повторять» (D-11): среди идей или отдельным
  подсписком `### Что не повторять` пометить мировые тактики, которые не
  портируются на «Крылья» — минимум одну явно про Instagram с ценами/CTA
  (незаконно под 38-ФЗ в режиме «информирование»), и про бюджетоёмкие приёмы,
  недоступные при бюджете ≈0. Тон без противопоставления (Pitfall #8), не
  копировать вслепую. Идеи не должны противоречить отстройке из
  `differentiation.md` (надёжность + локальная экспертиза, не через цену).
  </action>
  <verify>
    <automated>grep -q "Что украсть / что не повторять" .planning/research/competitors/competitor-world.md && [ "$(grep -cE '^[0-9]+\.' .planning/research/competitors/competitor-world.md)" -ge 5 ] && grep -qE "Фаза 1[346]" .planning/research/competitors/competitor-world.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "Что украсть / что не повторять"` — раздел есть
    - `grep -cE "^[0-9]+\."` ≥ 5 (и ≤ 7 идей по смыслу) — пронумерованная матрица из 5–7 идей
    - `grep -qE "Фаза 1[346]"` — минимум одна идея явно привязана к Фазе 13/14/16
    - `grep -ci "Фаза 1[346]"` ≥ 5 — каждая идея адресована в фазу (D-04)
    - `grep -qi "38-ФЗ"` и `grep -qi "не повторять"` — есть явная пометка непортируемых тактик (D-11)
  </acceptance_criteria>
  <done>Файл содержит прикладную матрицу из 5–7 идей, каждая с тремя частями и
  адресом в будущую фазу, плюс явные пометки «что не повторять».</done>
</task>

<task type="auto">
  <name>Задача 2: Блок «SEO-практики топ-агентств, которых нет у «Крыльев»» (вход в Фазу 14)</name>
  <read_first>
    - .planning/research/competitors/competitor-world.md (поля «SEO-подход» в досье — источник)
    - .planning/phases/10-konkurenty-mira/10-CONTEXT.md (D-05 — это Success Criterion #3)
    - .planning/ROADMAP.md (§Phase 14 — HUB-01..HUB-05, как используется блок)
  </read_first>
  <files>.planning/research/competitors/competitor-world.md</files>
  <action>
  Дополнить `competitor-world.md` отдельным разделом
  `## SEO-практики, которых нет у «Крыльев»` (D-05). Это НЕ повтор матрицы, а
  сфокусированный на SEO вход в Фазу 14. Пройтись по полям «SEO-подход» из досье
  (план 10-01) и выделить конкретные приёмы, которых у «Крыльев» сейчас нет,
  сгруппировав минимум по трём осям:
  - **Тематические кластеры / типы контента** — какие кластеры и форматы контента
    (гайды, чек-листы, «идеи под форматы», индустриальные страницы) берут топы и
    которых нет у «Крыльев».
  - **Перелинковка** — приёмы внутренней перелинковки (хаб-страницы, связки
    услуга↔кейс↔статья, breadcrumbs, related), которых не хватает.
  - **Посадочные под форматы / структура URL** — как топы делают посадочные под
    именованные форматы и как строят URL-структуру раздела идей.

  По каждой оси — список конкретных практик с короткой пометкой «→ Фаза 14»
  (куда ложится). Явно указать во вступлении блока, что это прямой вход в HUB-01
  (семантическое ядро) и HUB-03/HUB-04 (контент-хаб + рубрикатор). Не превращать
  кластеры в готовое семядро — это работа Фазы 14 (deferred в CONTEXT); здесь
  только фиксируем, что брать.
  </action>
  <verify>
    <automated>grep -qi "SEO-практики, которых нет у" .planning/research/competitors/competitor-world.md && grep -qi "кластер" .planning/research/competitors/competitor-world.md && grep -qi "перелинков" .planning/research/competitors/competitor-world.md && grep -q "Фаза 14" .planning/research/competitors/competitor-world.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -qi "SEO-практики, которых нет у"` — обязательный блок D-05 присутствует
    - `grep -qi "кластер"`, `grep -qi "перелинков"`, `grep -qi "посадочн\|структур"` — все три оси раскрыты
    - `grep -q "Фаза 14"` — блок явно адресован в Фазу 14 (вход в HUB-*)
  </acceptance_criteria>
  <done>Файл содержит отдельный блок SEO-практик, которых нет у «Крыльев»,
  сгруппированный по кластерам/типам контента, перелинковке и посадочным, с
  адресацией в Фазу 14.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Задача 3: Утвердить финальный список агентств и матрицу с Машей (D-06, D-04)</name>
  <read_first>
    - .planning/research/competitors/competitor-world.md (весь файл: досье + матрица + SEO-блок)
    - .planning/phases/10-konkurenty-mira/10-CONTEXT.md (D-04, D-06)
  </read_first>
  <action>Показать Маше собранный `competitor-world.md` целиком и получить
  подтверждение: устраивает ли финальный список агентств (баланс регионов) и
  прикладная матрица идей. Это чекпоинт-проверка: артефакт уже написан,
  исполнение — получить «ок» или правки и записать результат в SUMMARY плана.</action>
  <what-built>Собран файл `competitor-world.md`: 10–15 досье мировых агентств с
  балансом Европа/Америка/Азия, прикладная матрица «украсть/не повторять» из 5–7
  идей (каждая адресована в Фазу 13/14/16) и блок SEO-практик, которых нет у
  «Крыльев» (вход в Фазу 14).</what-built>
  <how-to-verify>
    1. Открой `.planning/research/competitors/competitor-world.md`.
    2. Пролистай список агентств — устраивает ли выбор и баланс по регионам
       (Европа / Америка / Азия), нет ли важного пропущенного игрока.
    3. Прочитай раздел «Что украсть / что не повторять» — понятны ли идеи и
       согласна ли ты с тем, куда они ложатся (Фаза 13 форматы / 14 контент /
       16 SMM).
    4. Проверь блок «SEO-практики, которых нет у Крыльев» — выглядит ли он как
       понятный список того, что стоит добрать в контенте.
  </how-to-verify>
  <resume-signal>Напиши «ок» или укажи, что добавить/убрать (агентство или идею)</resume-signal>
  <acceptance_criteria>
    - Мария подтвердила финальный список агентств и баланс регионов (или дала правку)
    - Мария подтвердила матрицу идей и их привязку к фазам (или дала правку)
    - Результат (ок / список правок) зафиксирован в SUMMARY плана
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| публичные источники → файл досье | в матрицу и SEO-блок попадают только выводы из публично доступных данных |
| внутренний документ → публичные тексты сайта | мировые конкуренты в публичных текстах сайта «Крыльев» не называются |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03 | Information Disclosure | competitor-world.md → сайт | mitigate | Матрица и SEO-блок — внутренний материал; в публичных текстах сайта конкуренты не называются. Выводы через ценность «Крыльев» (D-11). |
| T-10-04 | Tampering | непортируемые тактики (38-ФЗ) | mitigate | Мировые тактики с ценами/CTA в Instagram явно помечены как непортируемые под 38-ФЗ (режим «информирование»), чтобы их случайно не внедрили в Фазе 16. |

Фаза пишет только Markdown, не трогает код/рантайм/секреты — иной поверхности атаки нет.
</threat_model>

<verification>
- В `competitor-world.md` есть раздел «Что украсть / что не повторять» с 5–7 идеями
- Каждая идея адресована в Фазу 13/14/16
- Есть отдельный блок «SEO-практики, которых нет у Крыльев» (D-05 / Success Criterion #3)
- Непортируемые тактики (38-ФЗ) явно помечены
- Мария подтвердила финальный список агентств и матрицу
</verification>

<success_criteria>
- Прикладная матрица из 5–7 идей, каждая с адресом в будущую фазу (COMP-01, D-04)
- Блок SEO-практик, которых нет у «Крыльев» — вход в Фазу 14 (D-05)
- Мировые тактики, не портируемые на «Крылья», помечены (D-11)
- Финальный список агентств и матрица утверждены Машей
</success_criteria>

<output>
После завершения создать `.planning/phases/10-konkurenty-mira/10-02-SUMMARY.md`
</output>
