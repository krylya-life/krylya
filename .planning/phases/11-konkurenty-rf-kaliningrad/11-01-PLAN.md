---
phase: 11-konkurenty-rf-kaliningrad
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/research/competitors/competitor-kaliningrad.md
autonomous: false
requirements: [COMP-02, COMP-04]
must_haves:
  truths:
    - "Мария видит досье на 8 местных агентств Калининграда в одном файле"
    - "Фабрика и Марченко разобраны глубоко, остальные шесть — сжато"
    - "Каждое агентство оценено по двум осям: SEO-видимость и SMM"
    - "Внутренний инсайд по Фабрике и Марченко зафиксирован как внутренняя заметка"
  artifacts:
    - path: ".planning/research/competitors/competitor-kaliningrad.md"
      provides: "Досье 8 местных конкурентов по единому шаблону"
      contains: "## Фабрика"
  key_links:
    - from: ".planning/research/competitors/competitor-kaliningrad.md"
      to: "../.business/marketing/competitors.md"
      via: "расширяет исходный каркас местных конкурентов"
      pattern: "Фабрика"
---

<objective>
Собрать досье на 8 местных ивент-агентств Калининграда в файле
`.planning/research/competitors/competitor-kaliningrad.md`. По «Фабрике» и
«Марченко» — расширенный разбор (с ними реально сравнивают «Крылья»), по
остальным шести — сжатый. Каждое агентство оценивается по двум осям:
SEO-видимость и SMM. Это расширение существующего каркаса из
`../.business/marketing/competitors.md`, а не написание с нуля.

Назначение: Мария точно понимает, кто рядом в городе, насколько они сильны и
по каким запросам видны — это фундамент для отстройки (план 11-03) и аутрича
(Фаза 15).
Результат: файл `competitor-kaliningrad.md` с 8 секциями-досье.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-konkurenty-rf-kaliningrad/11-CONTEXT.md
@../.business/marketing/competitors.md
@.planning/research/SUMMARY.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Задача 1: Согласовать бюджет на инструменты (D-07)</name>
  <read_first>
    - .planning/phases/11-konkurenty-rf-kaliningrad/11-CONTEXT.md (D-06, D-07)
    - .planning/research/SUMMARY.md (раздел инструментов: Keys.so/SpyWords разово)
  </read_first>
  <action>Задать Маше вопрос о бюджете на инструменты (см. <decision> ниже) и
  зафиксировать её выбор. Это чекпоинт-решение: код не пишется, исполнение —
  получить ответ «free» или «paid» и записать его в SUMMARY плана.</action>
  <decision>По умолчанию весь ресёрч этой фазы делается БЕСПЛАТНЫМИ инструментами
  (Яндекс.Wordstat, ручная проверка выдачи Яндекс/Google по ключам, визуальный
  осмотр сайтов, TGStat бесплатный, просмотр соцсетей, Rusprofile / Контур.Компас
  бесплатный лимит). Бюджет фазы по умолчанию = 0 ₽. Платный разовый доступ
  (Keys.so / SpyWords, ~990–1200 ₽ на сутки) для точной SEO-видимости —
  ТОЛЬКО опционально и ТОЛЬКО если бесплатных прикидок не хватит для честной
  оценки оси SEO.</decision>
  <context>Это решение D-07. Перед любой тратой денег нужно явно спросить Машу.
  Если Мария говорит «делаем бесплатно» — оценка SEO-видимости даётся как
  приблизительная (видно/не видно в выдаче, по каким запросам всплывает,
  примерный порядок органики), без точных цифр трафика. Если Мария готова
  потратить 990–1200 ₽ — можно докупить разовый доступ для точных данных.</context>
  <options>
    <option id="free">
      <name>Только бесплатно (бюджет 0 ₽) — по умолчанию</name>
      <pros>Ноль затрат; для отстройки и аутрича достаточно качественной прикидки</pros>
      <cons>SEO-видимость оценивается приблизительно, без точных цифр трафика</cons>
    </option>
    <option id="paid">
      <name>Докупить разовый доступ Keys.so/SpyWords (~990–1200 ₽)</name>
      <pros>Точные цифры по органике и кластерам конкурентов</pros>
      <cons>Трата денег; для целей фазы (отстройка, аутрич) обычно избыточно</cons>
    </option>
  </options>
  <resume-signal>Напиши: «free» (бесплатно) или «paid» (докупить доступ)</resume-signal>
  <acceptance_criteria>
    - Мария выбрала вариант (free / paid); выбор зафиксирован в SUMMARY этого плана
    - Если paid — зафиксирован факт траты и какой инструмент куплен
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Задача 2: Создать папку и расширенные досье — Фабрика и Марченко</name>
  <read_first>
    - ../.business/marketing/competitors.md (исходный каркас: Фабрика, Апполонов, Марченко, Корпус)
    - .planning/phases/11-konkurenty-rf-kaliningrad/11-CONTEXT.md (D-01, D-02, D-03, D-05)
  </read_first>
  <files>.planning/research/competitors/competitor-kaliningrad.md</files>
  <action>
  Создать папку `.planning/research/competitors/` (`mkdir -p`, она пока не
  существует) и файл `competitor-kaliningrad.md`. Файл начинается с заголовка
  `# Конкуренты Калининграда` и короткого вступления (1–2 абзаца): зачем досье,
  тон без противопоставления, бесплатные инструменты по умолчанию.

  Для КАЖДОГО агентства использовать ЕДИНЫЙ шаблон досье (D-05), каждая секция —
  заголовок второго уровня `## Название`, внутри поля:
  - **Название**
  - **Сайт** — есть/нет, ссылка (если есть)
  - **Специализация / позиционирование**
  - **SEO-видимость** — индексируется ли, по каким запросам/кластерам виден,
    примерный объём/качество органики (приблизительно, если бюджет 0 ₽)
  - **SMM** — площадки, активность, форматы, приблизительные охваты
  - **Сильные стороны**
  - **Слабые стороны**
  - **Внутренний инсайд** — если есть (внутренняя заметка, НЕ для публичных текстов)

  В этой задаче заполнить РАСШИРЕННО два главных ориентира:
  - `## Фабрика` — главный ориентир. Внутренний инсайд (D-02): Мария раньше
    работала в «Фабрике», знает их кухню изнутри. ЗАФИКСИРОВАТЬ это в поле
    «Внутренний инсайд» с явной пометкой «внутренняя заметка, в публичных текстах
    сайта НЕ упоминать». Разобрать SEO и SMM детально.
  - `## Марченко` — второй ориентир. Зафиксировать инсайд D-03: работает
    фактически одна, много проектов — это и сила (личный бренд), и уязвимость
    (ограниченная ёмкость). Разобрать SEO и SMM детально.

  Инструменты (D-06): Wordstat, ручная проверка выдачи Яндекс/Google по ключам
  («ивент агентство Калининград», «организация корпоратива Калининград» и
  производные), визуальный осмотр сайтов, TGStat бесплатный, соцсети.
  НЕ называть конкурентов уничижительно, тон ровный (Pitfall #8).
  </action>
  <verify>
    <automated>test -f .planning/research/competitors/competitor-kaliningrad.md && grep -q "^## Фабрика" .planning/research/competitors/competitor-kaliningrad.md && grep -q "^## Марченко" .planning/research/competitors/competitor-kaliningrad.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -d .planning/research/competitors` — папка создана
    - `test -f .planning/research/competitors/competitor-kaliningrad.md`
    - `grep -q "^## Фабрика"` и `grep -q "^## Марченко"` — обе секции есть
    - `grep -i "внутренн" ...` присутствует в секции Фабрика (инсайд D-02 зафиксирован)
    - `grep -i "SEO-видимость" ...` и `grep -i "SMM" ...` присутствуют в обеих секциях (две оси, D-05 / COMP-04)
  </acceptance_criteria>
  <done>Папка создана; файл содержит расширенные досье Фабрики и Марченко по
  единому шаблону с обеими осями оценки и зафиксированными инсайдами.</done>
</task>

<task type="auto">
  <name>Задача 3: Сжатые досье — остальные шесть местных агентств</name>
  <read_first>
    - .planning/research/competitors/competitor-kaliningrad.md (созданный в задаче 2 — дополняем его)
    - .planning/phases/11-konkurenty-rf-kaliningrad/11-CONTEXT.md (D-01, D-05)
  </read_first>
  <files>.planning/research/competitors/competitor-kaliningrad.md</files>
  <action>
  Дополнить тот же файл шестью сжатыми досье (тот же шаблон полей, но компактно —
  по 1–2 строки на поле): `## Апполонов`, `## Корпус`, `## Балтма турс`,
  `## Киселёва`, `## Гранат`, `## Море`. Каждая секция — заголовок `## Название`
  с полями: Сайт; Специализация/позиционирование; SEO-видимость; SMM; Сильные
  стороны; Слабые стороны.

  Список открыт (D-01): если в ходе ресёрча всплывут новые заметные местные
  игроки — добавить их секциями `## Название` в конец файла.

  Инструменты бесплатные (D-06): визуальный осмотр сайтов, ручная проверка
  выдачи, соцсети, TGStat. Тон ровный, без противопоставления (Pitfall #8).
  </action>
  <verify>
    <automated>test "$(grep -c '^## ' .planning/research/competitors/competitor-kaliningrad.md)" -ge 8</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^## " .planning/research/competitors/competitor-kaliningrad.md` возвращает ≥ 8 (по секции на агентство)
    - `grep -q "^## Апполонов"`, `^## Корпус`, `^## Балтма турс`, `^## Киселёва`, `^## Гранат`, `^## Море` — все шесть секций есть
    - `grep -ci "SEO-видимость" ...` ≥ 8 и `grep -ci "SMM" ...` ≥ 8 (обе оси в каждом досье, D-05 / COMP-04)
  </acceptance_criteria>
  <done>Файл содержит 8 досье местных агентств (2 расширенных + 6 сжатых), каждое
  оценено по осям SEO-видимость и SMM.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| публичные источники → файл досье | в досье попадают только публично доступные данные о компаниях |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Information Disclosure | competitor-kaliningrad.md | mitigate | Только публично доступные данные (сайты, открытые соцсети, выдача). Никаких частных/персональных данных (152-ФЗ). Внутренний инсайд по Фабрике — пометка «не для публичных текстов сайта». |
| T-11-02 | Repudiation | публичные тексты сайта | accept | Конкуренты в публичных текстах сайта не называются (D-08/D-09); досье — внутренний документ. Низкий риск. |

Фаза пишет только Markdown, не трогает код/рантайм/секреты — иной поверхности атаки нет.
</threat_model>

<verification>
- Файл `.planning/research/competitors/competitor-kaliningrad.md` существует
- ≥ 8 секций `## Название`
- Обе оси (SEO-видимость, SMM) присутствуют в каждом досье
- Инсайды D-02 (Фабрика) и D-03 (Марченко) зафиксированы как внутренние заметки
- Решение по бюджету (D-07) зафиксировано в SUMMARY
</verification>

<success_criteria>
- 8 досье местных агентств по единому шаблону (Фабрика/Марченко расширенно)
- Каждый конкурент оценён по SEO-видимости и SMM
- Тон без противопоставления, бюджет согласован с Машей
</success_criteria>

<output>
После завершения создать `.planning/phases/11-konkurenty-rf-kaliningrad/11-01-SUMMARY.md`
</output>
