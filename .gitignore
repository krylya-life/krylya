# build artifacts
dist/
.astro/
.output/
.cache/

# dependencies
node_modules/
pnpm-lock.yaml
yarn.lock

# Cloudflare / упакованные деплой-артефакты
.wrangler/
krylya-site.zip
*.zip

# environment
.env
.env.local
.env.*.local

# OS / editor
.DS_Store
.idea/
.vscode/
*.swp
*.swo

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log

# фотоархив оригиналов (3+ ГБ) — только в локальной папке assets/cases/, не в git.
# В public/assets/cases/ лежат только обложки (по 1 фото на кейс) — они в git
# и попадают в production-сборку Cloudflare Pages.
/assets/cases/
!public/assets/cases/

# временный скриншот главной (для контекста)
krylya-home-2026-05-01.png

# скриншоты итераций дизайн-песочницы — рабочий материал, не в git
design-lab-*.png

# скриншоты автоматизации браузера (Playwright MCP) — рабочий материал, не в git
cf-*.png

# playwright-mcp служебные файлы
.playwright-mcp/
